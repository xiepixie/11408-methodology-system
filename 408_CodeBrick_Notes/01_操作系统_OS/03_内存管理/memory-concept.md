---
title: 内存管理基本概念 ​
source: https://www.codebrick.tech/os-blog/posts/memory/memory-concept.html
---

# 内存管理基本概念

2026 大纲 **三（一）1 内存管理的基本概念**：逻辑地址空间与物理地址空间、地址变换、内存共享、内存保护、内存分配与回收。

## 速查

| **五项功能同源** | 地址变换、分配与回收、内存保护、内存共享、内存扩充，全部源自一件事：**多道程序要在同一片物理内存上共存** |
| --- | --- |
| 🔴 **两个地址空间大小互不决定** | 逻辑地址空间大小由**地址位数**决定（32 位机恒为 4GB），物理地址空间大小由**实际安装的内存容量**决定。正因不相等，才必须有映射而不能简单平移 |
| **三种装入方式** | 同一件事被推迟得越来越晚：绝对装入（编译时）→ 静态重定位（装入时）→ 动态重定位（每次访存） |
| 🔴 **只有动态重定位允许进程搬家** | 判据是**代码里留下的是逻辑地址还是物理地址**。静态重定位装入时把地址写死进代码，搬家后全部作废；动态重定位代码里始终是逻辑地址，改一个寄存器整段程序就换了位置。也因此**必须由硬件支持** |
| **动态重定位公式** | 物理地址 = 逻辑地址 + 重定位寄存器（基址寄存器）的值 |
| **"能搬家"解锁了三件事** | **紧凑**、**对换**、分区分配中的**动态可重定位分区分配** |
| 🔴 **描述内存布局的寄存器随进程走** | 重定位寄存器与限长寄存器的值平时存在 **PCB** 里，[[01_操作系统_OS/02_进程管理/context-switch.md|上下文切换]]时装入。[[01_操作系统_OS/03_内存管理/paging.md|分页]]的页表寄存器、[[01_操作系统_OS/03_内存管理/segmentation.md|分段]]的段表寄存器同理 |
| 🔴 **基址与限长：先限长后基址** | 越界的地址不能被算出来——先加基址，越界的逻辑地址就变成了一个看似合法的物理地址，再比较就分不清它属于谁 |
| 🔴 **保护检查必须每次访存都做** | 越界访问可以发生在任何一条指令上，抽查等于不查；正因频率高才交给硬件，比较与加法可并行、不增加延迟 |
| **共享与保护不冲突** | 保护的本质是"**按权限访问**"而非"禁止访问"，靠段表/页表项里的**存取控制字段**统一表达；共享是保护的一个特例 |
| 🔴 **共享的是内容，不是地址** | 同一共享段在不同进程中**可以有不同的段号**，共享指最终落到同一片物理内存 |
| 🔴 **覆盖 vs 交换看谁做决策** | **覆盖**＝程序员编程时编排、作用在**一个进程内部**、对用户不透明、属程序设计技术；**交换**＝操作系统运行时决定、作用在**进程之间**、对用户透明、属**中级调度** |
| 🔴 **段页式仍有内碎片** | 段内还要分页、末页照样填不满；它消除的只是外碎片。"段页式既无内碎片又无外碎片"是错的 |

## 一、五项功能从哪来

顺着"多道程序共存"这一句就能推出全部五项：编译时不可能知道自己被放到哪里 ⇒ 程序只能用**从 0 开始的逻辑地址** ⇒ 必须有人翻译成物理地址（**地址变换**）；谁占了哪块必须有人记账（**分配与回收**）；一个进程写错地址就可能踩坏别人（**内存保护**）；但库代码、通信缓冲区**应该**被多个进程同时看到，保护不能一刀切死（**内存共享**）；所有进程加起来超过物理内存，得在内外存之间倒腾（**内存扩充**）。

| 功能 | 做什么 | 详见 |
| --- | --- | --- |
| 地址变换（重定位） | 把逻辑地址翻译成物理地址 | 本篇二 |
| 内存分配与回收 | 记录空闲/已分配情况，按算法切分与合并 | 本篇四、[[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配]] |
| 内存保护 | 限定进程只能访问自己的地址范围 | 本篇三 |
| 内存共享 | 让指定的内存区被多个进程同时访问 | 本篇五 |
| 内存扩充 | 覆盖、对换、虚拟内存 | 本篇六、[[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|虚拟内存]] |

## 二、地址重定位

| 方式 | 转换时机 | 谁来做 | 装入后能否移动 | 适用场合 |
| --- | --- | --- | --- | --- |
| 绝对装入 | 编译/汇编时 | 编译程序（或程序员直接写绝对地址） | 不可 | 只能单道程序 |
| 可重定位装入（静态重定位） | 装入时 | 装入程序，一次性改写全部地址 | 不可 | 多道程序，但进程位置固定 |
| 动态运行时装入（动态重定位） | 每次访存时 | **硬件**（重定位寄存器 + 加法器） | **可以** | 多道程序 + 对换 + 紧凑 |

## 三、内存保护

两套硬件方案的差别只在**拿什么去比**：

| 方案 | 寄存器 | 比什么 |
| --- | --- | --- |
| **上下限寄存器** | 上限＝进程占用区域的最大物理地址；下限＝最小物理地址 | 比**变换后的物理地址**：下限 ≤ 物理地址 ≤ 上限 |
| **基址 + 限长寄存器** | 基址（重定位寄存器）＝进程起始物理地址；限长＝逻辑地址空间长度 | 比**变换前的逻辑地址**：逻辑地址 < 限长，再加基址 |

方案二更常用，因为它把"越界检查"和"地址变换"合并进同一套硬件——限长比较与基址加法可以并行做。后面分页的"页号 < 页表长度"、分段的"段号 < 段表长度"，都是它的直系后代。

## 四、内存分配与回收

| 组成 | 内容 | 典型形式 |
| --- | --- | --- |
| **数据结构** | 记录哪些区域空闲、哪些已分配、各自的始址与大小 | 空闲分区表 / 空闲分区链 / 位示图 |
| **分配算法** | 有多块空闲区可选时，选哪一块 | 首次适应、最佳适应、最坏适应、邻近适应 |
| **回收处理** | 进程释放后，把归还的区域并回空闲结构 | 与前后相邻空闲区**合并**，避免越回收越碎 |

三样里最容易被漏掉的是**回收**：只登记不合并，内存会被切得越来越碎，最终大进程无处安放。完整算法与四种邻接情况见[[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配]]。

离散分配下这项工作会变形但不消失：分页时分配的是**页框**，回收只需把位示图对应位清零，天然不需要合并。**"要不要合并"取决于分配单位是否等大。**

## 五、内存共享

保护要求"互相看不见"、共享要求"能一起看见"，操作系统靠三件事同时满足：

- **粒度可控**——共享以逻辑上完整的单位进行。分段系统中共享一个函数只需在两个进程的段表里各放一个指向同一段的表项；分页系统中同一个函数可能横跨多个页面、还可能与别的内容挤在同一页里，共享就麻烦得多。**共享是否方便，取决于划分依据是逻辑的还是物理的。**
- **权限分级**——段表/页表项里的**存取控制字段**给每个进程单独规定读、写、执行权限，文件主可读写、其他进程只读甚至只执行。
- **可重入代码（纯代码）**——被共享的代码在执行中**绝对不允许被修改**。真实代码难免有变量、指针、计数器，做法是把这些会变的部分**拷贝一份到各进程私有的局部数据区**，共享的那份只读不写。

## 六、内存扩充：覆盖与交换

|  | 覆盖（Overlay） | 交换 / 对换（Swapping） |
| --- | --- | --- |
| **作用范围** | **同一个进程内部**的若干程序段 | **进程之间**，以整个进程为单位换进换出 |
| **谁来划分** | **程序员**，编程时就要指明哪些段互斥、共用哪块覆盖区 | **操作系统**（对换进程） |
| **对用户** | **不透明**，属于一种程序设计技术 | **透明**，用户程序无感 |
| **换到哪里** | 内存中预留的覆盖区，段来自外存 | 外存上专门的**对换区**（连续分配，读写速度优先） |

**覆盖**把程序划成一个常驻的**固定区**和若干**覆盖区**，不会同时执行的模块共用同一块覆盖区。内存需求被压到固定区大小+∑每个覆盖区(该区内最大那个模块的大小)

🔴 **不是"固定区 + 最大的那一个覆盖区"**——不同覆盖区里的模块之间并不互斥、可能同时驻留，所以每个覆盖区都要按它自己那组互斥模块中最大的那个留够空间。代价是程序员必须清楚模块间的调用关系，这也是它被虚拟内存取代的原因。

**交换**由操作系统的**对换进程**执行，属[[01_操作系统_OS/02_进程管理/scheduling-concept.md|进程调度]]里的**中级调度**：

- **换出选谁**——先在驻留内存的进程里找**阻塞或睡眠**状态的；有多个时选**优先级最低**的；有的系统还要看**在内存中已驻留多久**，避免低优先级进程刚调入又立刻被换出
- **换入选谁**——在"就绪但已换出"的进程里，选**在磁盘上待得最久**的换回来

两条规则背后是同一个判据：**换出要挑"眼下最不可能用 CPU 的"，换入要挑"已经等得最委屈的"。**

三点边界：换出的进程**并不释放 PCB**，PCB 常驻内存（否则系统就找不到它了）；对换区采用**连续分配**，分配与回收方法和内存的动态分区完全一样（同样有四种邻接情况的合并）；交换以**整个进程**为单位，而虚拟内存以**页/段**为单位——后者才是"部分装入"，前者仍要求进程全部在内存才能运行。

## 七、内碎片与外碎片

| 类型 | 说明 | 产生原因 | 出现在 |
| --- | --- | --- | --- |
| 内碎片 | 已分配给进程但**未被使用**的空间 | 分配单位大于实际需要 | 固定分区、分页、段页式 |
| 外碎片 | 内存中空闲但**太小无法利用**的区域 | 多次分配回收后被切碎 | 动态分区、分段 |

判据是"这块空间是否已经分出去了"：已分出去而没用满 ⇒ 内碎片；还没分出去但小到分不出去 ⇒ 外碎片。

## 本节小结

- 内存管理的五项功能同源于"多道程序共存"；**逻辑地址空间由地址位数决定、物理地址空间由内存容量决定**，两者互不决定，所以必须有映射层。
- 三种装入方式是同一件事被推迟得越来越晚，**只有动态重定位（物理地址 = 逻辑地址 + 重定位寄存器）允许进程搬家**，进而解锁紧凑、对换与动态可重定位分区分配；描述内存布局的寄存器一律"平时存 PCB、切换时装入"。
- 保护有上下限寄存器与"基址 + 限长"两套方案，后者**先限长后基址**、每次访存都查；共享靠存取控制字段与可重入代码，与保护并不冲突。**覆盖由程序员在进程内编排、交换由操作系统在进程间执行**，覆盖的内存需求是固定区加上各覆盖区之和。教材出处

- 汤小丹《计算机操作系统》4.2.1 程序的装入（绝对装入、可重定位装入与静态重定位、动态运行时装入），p123–p124
- 同上 4.3.6 动态可重定位分区分配（紧凑必须配合动态重定位、重定位寄存器），p133–p134
- 同上 4.4 对换（对换的引入、整体对换与页面/分段对换、对换区的分配与回收、进程的换出与换入），p135–p138
- 孙钟秀、费翔林《操作系统教程》（第 6 版）3.1 存储管理基础（主存空间的分配与去配、共享、存储保护、主存空间的扩充：对换技术与虚拟技术；段覆盖技术是一种程序设计技术），p83、p85–p86

## 相关知识

[[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配]]｜[[01_操作系统_OS/01_操作系统概述/link-load.md|程序的链接与装入]]（三种链接时机；动态链接库在内存中只存一份，是内存共享的典型用例）｜[[01_操作系统_OS/01_操作系统概述/memory-image.md|运行时内存映像与地址空间]]｜[[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|虚拟内存]]

## 真题练习

### 相关真题（7题）
[2023Q282分](https://www.codebrick.tech/practice/q/os-2023-28?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[2021Q282分](https://www.codebrick.tech/practice/q/os-2021-28?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[2019Q312分](https://www.codebrick.tech/practice/q/os-2019-31?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[2018Q458分](https://www.codebrick.tech/practice/q/os-2018-45?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[2014Q282分](https://www.codebrick.tech/practice/q/os-2014-28?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[2011Q302分](https://www.codebrick.tech/practice/q/os-2011-30?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[2009Q262分](https://www.codebrick.tech/practice/q/os-2009-26?ctx=os-2023-28,os-2021-28,os-2019-31,os-2018-45,os-2014-28,os-2011-30,os-2009-26)[[01_操作系统_OS/02_进程管理/deadlock-detection.md|上一篇死锁检测与解除]][[01_操作系统_OS/03_内存管理/contiguous-allocation.md|下一篇连续分配（首次/最佳/最坏适应）]]

### 相关文章

- [[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配（首次/最佳/最坏适应）]]
- [[01_操作系统_OS/03_内存管理/paging.md|基本分页（地址变换）]]
- [[01_操作系统_OS/03_内存管理/segmentation.md|基本分段]]
- [[01_操作系统_OS/03_内存管理/seg-paging.md|段页式管理]]
- [[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|虚拟内存基本概念]]
- [[01_操作系统_OS/03_内存管理/demand-paging.md|请求页式管理]]
- [[01_操作系统_OS/03_内存管理/fifo-replace.md|FIFO 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/lru-replace.md|LRU 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/opt-replace.md|OPT 最佳置换算法]]
- [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 与改进 CLOCK 算法]]
- [[01_操作系统_OS/03_内存管理/page-replace-simulator.md|页面置换算法对比模拟器]]
- [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]
- [[01_操作系统_OS/03_内存管理/memory-mapped-file.md|内存映射文件]]
- [[01_操作系统_OS/03_内存管理/virtual-memory-perf.md|虚拟存储性能与改进]]