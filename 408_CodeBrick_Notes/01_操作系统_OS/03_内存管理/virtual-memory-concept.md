---
title: 虚拟内存基本概念 ​
source: https://www.codebrick.tech/os-blog/posts/memory/virtual-memory-concept.html
---

# 虚拟内存基本概念

2026 大纲 **三（二）1 虚拟内存的基本概念**。

## 速查

| **要打破的那条前提** | 传统方式要求进程**全部装入内存**，由此卡死单进程规模与并发度；而进程里有大量**很少甚至从不执行**的代码全程占着内存——这说明"全部装入"本身就是浪费的 |
| --- | --- |
| **理论基础** | **局部性原理**：**时间局部性**（刚访问过的位置很快还会访问——循环、递归、热点数据）+ **空间局部性**（刚访问位置的**附近**很快也会访问——顺序执行、数组遍历、栈的连续压入） |
| **推导链** | 有局部性 ⇒ 一段时间内访问集中在少数几个页（即[[01_操作系统_OS/03_内存管理/page-allocation.md|工作集]]）⇒ 只装这少数几页也能连续跑很久 ⇒ 缺页是**偶发**的 ⇒ **平摊下来性能损失可接受** ⇒ 部分装入可行 |
| 🔴 **落点是"平摊可接受"，不是"没有代价"** | 虚拟内存永远比全部装入慢，它换来的是能跑更大的程序、跑更多的进程。**可行性建立在"真实程序有局部性"这个经验事实上，不是逻辑必然** |
| **三个特征是一条链** | **多次性**（进得来：不必等全部装完就能开始跑）+ **对换性**（出得去：腾空间给新页面，于是分多次调入可以无限进行）⇒ 才产生**虚拟性**（目的，也是最本质的特征）。前两者是可观察的机制，虚拟性是用户感受到的效果 |
| **三项硬件支持各补一个缺口** | **页表机制**回答"在不在内存 / 去哪取 / 要不要写回 / 该淘汰谁"（状态位 P、外存地址、修改位 M、访问字段 A）；**中断机制**提供缺页中断；**地址变换机构**插入"P=0 → 调页 → 更新页表快表 → 重新变换"的回路。一句话：**页表机制负责"知道"，中断机制负责"救援"，地址变换机构负责"重来"** |
| 🔴 **缺页中断不能用普通中断顶替** | 普通中断在一条指令**执行完毕后**才检测、返回执行**下一条**；缺页是指令**执行到一半**（正取操作数）才发现的，那条指令根本没做完，处理完必须**重新执行它**。满足这两条的是内中断里的**故障（Fault）** |
| **三种实现方式** | **请求分页**（换入换出单位是**定长页**，最常用）；**请求分段**（单位是**变长段**，换入时要在内存里凑一块够大的连续空间，可能引发紧凑）；**请求段页式**（段表管保护与共享，调度仍以页为单位） |
| 🔴 **请求分段特有的两项** | 段表项在基本分段之外增六项，其中**存取方式**（段是逻辑单位才谈得上属性）与**增补位**（记录本段运行中是否做过动态增长）是请求分页没有的 |
| 🔴 **只有分页会"跨块"** | 段是**信息的逻辑单位**，不可能出现一条指令或一组信息被分割在两个段中；页是按定长机械切分的，指令与操作数都可能跨页。这直接决定了"最少需要几个存储块"的算法差异 |
| 🔴 **必须建立在离散分配之上** | 连续分配要求整个进程占一整块连续物理空间，"部分装入"在它那里根本不成立 |
| **容量上限** | =min( **内存 + 外存总容量**，**地址位数决定的寻址空间** )。所以"虚拟内存 = 外存大小"是错的：32 位地址决定单进程寻址上限 4GB，哪怕装 1TB 硬盘也不会超过 4GB |

## 一、局部性不成立会怎样：一个反例

推导链的每一环都靠局部性撑着，把这个前提抽掉，结论立刻反向。

假设有一个程序，每次访存都**均匀随机**落在整个地址空间的任意一页上（例如对一个远大于内存的表做完全随机的散列查找）。设驻留内存的页面只占全部页面的比例 r，则每次访存命中的概率就是 r，**缺页率 f=1−r**。取 tmem=100ns、一次缺页处理 tfault=5ms：

| 情形 | 缺页率 f | EAT=(1−f)tmem+ftfault | 相当于内存访问的多少倍 |
| --- | --- | --- | --- |
| 无局部性，驻留 50% | 0.5 | 2 500 050 ns ≈ **2.5 ms** | **25 000 倍** |
| 无局部性，驻留 10% | 0.9 | 4 500 010 ns ≈ **4.5 ms** | **45 000 倍** |
| 有局部性（典型） | 10−5 | 150 ns | **1.5 倍** |

前两行意味着：**没有局部性，虚拟内存会退化成"用磁盘当内存"，慢到完全不可用**。这也解释了为什么后面所有置换算法（LRU、CLOCK）都在赌局部性，以及为什么一个访问模式反常的程序会把系统拖进[[01_操作系统_OS/03_内存管理/virtual-memory-perf.md|抖动]]。

## 二、虚拟内存是什么

虚拟内存让每个进程拥有一个**很大的、连续的虚拟地址空间**，实际上只有一部分页面在物理内存中，其余存放在外存（磁盘）上。

虚拟地址空间**内部怎么划分**（代码段、数据段、堆向上长、栈向下长、中间留空洞）不属于本节，见[[01_操作系统_OS/01_操作系统概述/memory-image.md|运行时内存映像与地址空间]]。本节只关心一件事：**这个空间里的页可以不全在内存**。三个特征为什么是一条链而不是三个并列项（想弄清多次性、对换性、虚拟性谁是手段谁是目的时展开）

| 特征 | 说明 | 是手段还是目的 |
| --- | --- | --- |
| 多次性 | 程序不需要一次全部装入，可以**分多次**调入内存 | 手段 |
| 对换性 | 暂时不用的部分可以换出到外存，需要时再换入 | 手段 |
| 虚拟性 | 从逻辑上扩充了内存容量，用户感觉内存比实际大得多 | **目的** |

把它们当并列的三条去背，就说不清"为什么必须三条都有"。实际上它们是一条推进链：

**多次性**解决"进得来"——不必等全部装完就能开始跑；但只有多次性还不够，内存迟早会被填满，装到第 k 批就走不动了。

**对换性**解决"出得去"——把暂时不用的部分换到外存，腾出空间给新的页面，于是"分多次调入"这件事可以**无限进行下去**，而不是只能进行有限的几轮。

两者合起来，进程实际用到的地址空间就不再受物理内存限制 ⇒ 这才产生了**虚拟性**。

**判据：多次性和对换性是可观察的机制，虚拟性是用户感受到的效果。** 所以虚拟性是最本质的特征——去掉它，前两者就没有存在的理由；反过来，缺了前两者中的任何一个，虚拟性都产生不出来。

## 三、三项硬件支持，按缺口顺序推出来

**缺口一：怎么知道某一页在不在内存？** ⇒ **页表机制**。基本分页的页表项只有"页号 → 页框号"，无法表达"这一页不在内存"这种状态，于是扩出四个字段：

| 新增字段 | 补上的缺口 |
| --- | --- |
| **状态位 P** | 回答"在不在内存"——地址变换时先查它 |
| **外存地址** | 回答"不在内存的话，去磁盘的哪个块取" |
| **修改位 M** | 回答"换出时要不要写回"（M=0 可直接丢弃，省一次磁盘写） |
| **访问字段 A** | 回答"置换算法该淘汰谁" |

**缺口二：发现 P=0 时，怎么把控制权交给 OS，处理完还能接着算？** ⇒ **中断机制**。这里要求的不是普通中断：它必须能在**指令执行期间**触发，且保存的现场要足以让这条指令**从头重新执行**。满足这两条的正是内中断里的**故障（Fault）**类——它的返回语义就是"修复后重新执行当前指令"。详见[[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|中断和异常的处理]]。

**缺口三：调入之后，怎么接着完成这次地址变换？** ⇒ **地址变换机构**。基本分页的变换是一条直路（拆页号 → 查表 → 拼物理地址），请求分页要在中间插一条**回路**：查到 P=0 就转去缺页中断处理，调入页面、更新页表和快表，再**回到起点重新变换一次**。所以它不只是"多查一位"，而是流程本身多了一条分支和一次重入。

## 四、请求分段比请求分页多了什么

| 字段 | 作用 | 与请求分页对应吗 |
| --- | --- | --- |
| 存取方式 | 按段的逻辑属性做保护（只执行 / 只读 / 读写） | **请求分页没有**——段是逻辑单位才谈得上属性 |
| 访问字段 A | 供置换算法参考 | 对应 |
| 修改位 M | 换出时要不要写回 | 对应 |
| 存在位 P | 该段是否已调入内存 | 对应 |
| **增补位** | 本段运行中是否做过**动态增长** | **请求分段特有**——段长可变，栈段、堆段会长大 |
| 外存始址 | 该段在外存的起始盘块号 | 对应 |

缺段中断与缺页中断的机制一致（都在指令执行期间产生、一条指令可能触发多次），差别只在"跨块"这一条：段是信息的逻辑单位，一条指令不会被切到两个段里；页则可能跨页。这条差别的直接后果，就是两者对"最少需要几个存储块"的要求不同——见[[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]。

## 五、虚拟内存 vs 基本分页

| 比较 | 基本分页 | 请求分页（虚拟内存） |
| --- | --- | --- |
| 装入方式 | 一次性全部装入 | 按需调入（缺页时调入） |
| 是否需要全部在内存 | 是 | 否 |
| 页表项 | 页号 → 页框号 | 增加状态位 P、访问字段 A、修改位 M、外存地址 |
| 缺页处理 | 无此概念 | 触发缺页中断（内中断·故障类），调入后重新执行该指令 |
| 页面置换 | 无 | 内存满时需要置换 |
| 逻辑地址空间上限 | ≤ 物理内存 | ≤ min(内存+外存, 地址位数决定的寻址空间) |

## 本节小结

- 虚拟内存的可行性来自**局部性原理**这一经验事实，落点是"缺页偶发、平摊下来可接受"；抽掉局部性，EAT 会退化到毫秒级，比纯内存慢几万倍。
- 三个特征是链条而非并列：**多次性 + 对换性**（手段）⇒ **虚拟性**（目的）；三项硬件支持各补一个缺口——页表机制"知道"、中断机制"救援"、地址变换机构"重来"，其中缺页中断必须是能在指令执行期间触发、返回后重执行的**故障类内中断**。
- 三种实现里**请求分页**最常用，因为换入换出单位定长；请求分段特有**存取方式**与**增补位**，且段是逻辑单位、不会跨块。虚拟内存必须建立在**离散分配**上，容量上限取"内存+外存"与"地址位数"的较小值。教材出处

- 请求分段所需的三项硬件支持（请求段表机制、缺段中断机构、地址变换机构）、段表项的六个新增字段（存取方式、访问字段 A、修改位 M、存在位 P、**增补位**、外存始址）、以及"分段是信息的逻辑单位，不可能出现一条指令被分割在两个分段中"：汤小丹《计算机操作系统》5.5.1 节「请求分段中的硬件支持」，p173

## 相关知识

[[01_操作系统_OS/03_内存管理/seg-paging.md|段页式管理]]｜[[01_操作系统_OS/03_内存管理/demand-paging.md|请求页式管理]]｜[[01_操作系统_OS/01_操作系统概述/memory-image.md|运行时内存映像与地址空间]]｜[[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|中断和异常的处理]]

## 真题练习

### 相关真题（6题）
[2026Q242分](https://www.codebrick.tech/practice/q/os-2026-24?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2023Q282分](https://www.codebrick.tech/practice/q/os-2023-28?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2022Q302分](https://www.codebrick.tech/practice/q/os-2022-30?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2020Q282分](https://www.codebrick.tech/practice/q/os-2020-28?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2012Q457分](https://www.codebrick.tech/practice/q/os-2012-45?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2009Q468分](https://www.codebrick.tech/practice/q/os-2009-46?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[[01_操作系统_OS/03_内存管理/seg-paging.md|上一篇段页式管理]][[01_操作系统_OS/03_内存管理/demand-paging.md|下一篇请求页式管理]]

### 相关文章

- [[01_操作系统_OS/03_内存管理/memory-concept.md|内存管理基本概念]]
- [[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配（首次/最佳/最坏适应）]]
- [[01_操作系统_OS/03_内存管理/paging.md|基本分页（地址变换）]]
- [[01_操作系统_OS/03_内存管理/segmentation.md|基本分段]]
- [[01_操作系统_OS/03_内存管理/seg-paging.md|段页式管理]]
- [[01_操作系统_OS/03_内存管理/demand-paging.md|请求页式管理]]
- [[01_操作系统_OS/03_内存管理/fifo-replace.md|FIFO 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/lru-replace.md|LRU 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/opt-replace.md|OPT 最佳置换算法]]
- [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 与改进 CLOCK 算法]]
- [[01_操作系统_OS/03_内存管理/page-replace-simulator.md|页面置换算法对比模拟器]]
- [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]
- [[01_操作系统_OS/03_内存管理/memory-mapped-file.md|内存映射文件]]
- [[01_操作系统_OS/03_内存管理/virtual-memory-perf.md|虚拟存储性能与改进]]