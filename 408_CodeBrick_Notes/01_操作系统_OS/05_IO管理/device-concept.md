---
title: 设备的基本概念与分类 ​
source: https://www.codebrick.tech/os-blog/posts/io/device-concept.html
---

# 设备的基本概念与分类

2026 大纲 **五（一）1 设备**，补充说明点名四项：设备的基本概念、设备的分类、**I/O 接口**、**I/O 端口**。

## 速查

| **I/O 设备** | 执行 I/O 操作的**机械部件**（键盘的按键、磁盘的盘片与磁头）。粒度：一台设备 |
| --- | --- |
| **设备控制器** | 执行 I/O 控制的**电子部件**，插在 CPU 与设备之间。**它就是硬件意义上的"I/O 接口"**（适配器、接口卡）。粒度：一块控制器，可挂 1~多台设备 |
| **I/O 端口** | 控制器内部**一个可被 CPU 寻址的寄存器**。粒度：一个寄存器 |
| 🔴 **"端口"与"寄存器"是同一批硬件的两种站位** | 从控制器内部看是**寄存器**（有位宽、有含义），从 CPU 地址空间看是**端口**（有地址、可被寻址）。"有几个端口"＝"有几个可被 CPU 寻址的寄存器"，通常至少数据、状态、控制三个 |
| 🔴 **与另一个"接口"划清界限** | [[01_操作系统_OS/05_IO管理/io-api.md|应用程序 I/O 接口]]是**软件 API**。判据：**本篇的接口插在主板上，那篇的接口写在头文件里** |
| **按使用特性分** | 存储 / 输入 / 输出 / 交互式设备 → 决定归哪个子系统管（存储设备走文件系统，其余走设备管理） |
| **按传输速率分** | 低速（几~数百 B/s）/ 中速 / 高速（数十万 B/s 以上）→ 决定选哪种 [[01_操作系统_OS/05_IO管理/io-control.md|I/O 控制方式]]与缓冲策略（低速可轮询，高速必须 DMA） |
| **按信息交换单位分** | **块设备** / **字符设备** → 决定用哪套[[01_操作系统_OS/05_IO管理/io-api.md|设备接口]]与驱动模型；**块设备才谈得上磁盘调度** |
| **按共享属性分** | **独占**（打印机、磁带机）/ **共享**（磁盘）/ **虚拟**设备（[[01_操作系统_OS/05_IO管理/spooling.md|SPOOLing]] 改造出的虚拟打印机）→ 决定[[01_操作系统_OS/05_IO管理/device-allocation.md|设备分配]]时能不能同时给多个进程 |
| 🔴 **四套标准不是重复分类** | 每一套服务于操作系统的一个**不同决策**；同一台打印机既是"字符设备"又是"独占设备"，两句话不冲突 |
| 🔴 **块 / 字符的分界是"能不能寻址"，不是快慢** | 块设备以固定大小数据块为单位、每块有地址、可随机访问；字符设备以单个字符为单位、不可寻址、只能顺序收发。**打印机不快，但你无法对它说"把第 37 块重打一遍"，所以它是字符设备** |
| **为什么必须隔一层控制器** | 设备的电气与时序差异（模拟电压、串行位流、出错率高）若由 CPU 直接对付，**每换一种外设就得换一次 CPU**。插一层后 CPU 只面对"若干个寄存器"这一种统一形态——这是软件层**设备独立性**的硬件前提 |
| **控制器六项功能** | 接收和识别命令、数据交换、标识和报告设备状态、地址识别、数据缓冲、差错控制 |
| **控制器三部分组成** | **与处理机的接口**（数据线/地址线/控制线，连数据寄存器与控制/状态寄存器）、**与设备的接口**（数据/控制/状态三种信号线）、**I/O 逻辑**（译码与控制） |
| 🔴 **独立编址 vs 统一编址的判据** | 描述里说"用普通 load/store 就能访问设备寄存器"→**统一编址**；说"用 in/out 这类专用 I/O 指令"→**独立编址** |
| 🔴 **两者各付什么代价** | 独立编址**省地址、费指令**（要一根 M/IO# 类控制线，访问内存与设备两套写法）；统一编址**省指令、费地址**（内存地址空间被切走一块） |

## 一、四个词先分清

考纲这一条点了四个词，其中三个指的是同一条链上不同粒度的东西。教材把控制器的两侧分别称为"设备与控制器之间的接口"与"控制器与处理机之间的接口"——**凡是"接口"，指的都是硬件这一层**；一个 I/O 接口内部有若干个 I/O 端口。

## 二、设备控制器（I/O 接口）

六项功能不是罗列，而是"要把 CPU 从设备事务里解脱出来"这一目标逐项推出来的：

| 功能 | 为什么非有不可 | 对应的硬件 |
| --- | --- | --- |
| **接收和识别命令** | CPU 只发一句 `read`，控制器得知道这是 15 条命令中的哪一条、带什么参数 | 控制寄存器 + 命令译码器 |
| **数据交换** | 数据要在 CPU↔控制器、控制器↔设备两段之间搬 | 数据寄存器 |
| **标识和报告设备状态** | CPU 必须先知道设备忙不忙才敢启动它 | 状态寄存器（每一位表示一种状态） |
| **地址识别** | 一块控制器可能挂多台设备，且它自己的每个寄存器也要有唯一地址 | 地址译码器 |
| **数据缓冲** | 设备速率远低于 CPU 与内存，不缓冲就只能让快的一方等慢的一方 | 控制器内的缓冲区 |
| **差错控制** | 设备传输易错，错误要在离硬件最近的地方发现 | 差错检测码 + 出错置位报告 |

I/O 逻辑对 CPU 送来的地址译码、选中某个设备接口，再按译出的命令控制设备。

## 三、I/O 端口的编址方式

|  | **独立编址**（专用 I/O 指令） | **统一编址**（内存映像 I/O） |
| --- | --- | --- |
| 地址空间 | 端口自成一套地址空间，与内存地址互不干扰 | 端口占用内存地址空间的高端一段（地址 < n 是内存，≥ n 是端口） |
| 指令 | 必须用专用 I/O 指令，如 `io-store cpu-reg, dev-no, dev-reg` | 普通访存指令即可，如 `store cpu-reg, n` |
| 硬件代价 | 需要一根 M/IO# 之类的控制线区分本次访问的是内存还是端口 | 不需要额外控制线，但**内存地址空间被切走一块** |
| 编程代价 | 访问内存与访问设备要写两套指令，高级语言难以直接表达 | 一套指令通吃，**指针可直接指向设备寄存器**，编程灵活 |
| 保护 | 专用 I/O 指令是特权指令，天然只能内核态执行 | 靠页表权限位来挡，需要额外配置 |

**统一编址的代价是实打实的**：把端口映射进内存地址空间，意味着这段地址不能再装物理内存。早期 32 位机器实际可用内存常常卡在 3.2~3.5 GB 而不是 4 GB，原因之一就是显存和各类设备寄存器占走了地址空间的高端——这不是"内存坏了"，是地址被设备占用了。I/O 通道：DMA 之上的那一层，以及三种通道类型（本条已不在现行大纲内，想看清 DMA 的能力边界时展开）

现行 408 大纲 **五（一）2 I/O 控制方式**只列了三条：轮询方式、中断方式、DMA 方式，**没有通道方式**。保留这一节是因为通道处在 DMA 之上的一层，看清"DMA 只搬一段参数固定的数据、通道能自己取指令执行一串 I/O 任务"这个层级关系，反过来能把仍在纲内的 **DMA 的能力边界**看得更准。了解即可，不必记忆细节。

**通道**是专门用于 I/O 操作的处理器，可以独立于 CPU 执行 I/O 操作。它与一般处理机的两点不同：**指令类型单一**（只有与 I/O 有关的指令）、**没有自己的内存**（通道程序放在主机内存里，与 CPU 共享内存）。

| 类型 | 特点 |
| --- | --- |
| 字节多路通道 | 含许多非分配型子通道，按字节交叉、时间片轮转为多个慢速设备服务 |
| 数组选择通道 | 只含一个分配型子通道，一段时间内被一台高速设备独占，传完才让给下一台 |
| 数组多路通道 | 结合以上两种：多个非分配型子通道，以数据块为单位轮流服务 |

## 本节小结

- **设备（机械部件）→ 控制器 / I/O 接口（电子部件）→ I/O 端口（可寻址的寄存器）** 是同一条链上的三级粒度；硬件 I/O 接口与[[01_操作系统_OS/05_IO管理/io-api.md|应用程序 I/O 接口]]只是中文同名。
- **四套分类标准各自决定 OS 的一个决策**，不是重复分类；其中块 / 字符的分界是**能不能寻址**而非速度快慢。
- 插一层控制器是为了把设备的电气与时序差异挡在 CPU 之外，六项功能都由这一目标推出；端口编址二选一的差别是**省地址费指令**（独立编址）还是**省指令费地址**（统一编址）。教材出处

- 设备控制器的六项基本功能、三部分组成：汤小丹《计算机操作系统》6.2.2 节，印刷 p184–185
- 利用特定 I/O 指令与内存映像 I/O 两种设备寻址形式：同书 6.2.3 节，印刷 p185–186
- I/O 通道的引入与三种通道类型：同书 6.2.4 节，印刷 p186–187

## 相关知识

[[01_操作系统_OS/05_IO管理/io-control.md|I/O 控制方式]]｜[[01_操作系统_OS/05_IO管理/io-software-layer.md|I/O 软件层次结构]]｜[[01_操作系统_OS/05_IO管理/device-allocation.md|设备分配与回收]]｜[[01_操作系统_OS/05_IO管理/io-api.md|输入输出应用程序接口]]｜[[01_操作系统_OS/05_IO管理/spooling.md|SPOOLing 技术]]

## 真题练习

### 相关真题（1题）
[2026Q312分](https://www.codebrick.tech/practice/q/os-2026-31)[[01_操作系统_OS/04_文件管理/fs-mount.md|上一篇文件系统挂载]][[01_操作系统_OS/05_IO管理/io-control.md|下一篇I/O控制方式]]

### 相关文章

- [[01_操作系统_OS/05_IO管理/io-control.md|I/O控制方式]]
- [[01_操作系统_OS/05_IO管理/io-software-layer.md|I/O软件层次结构]]
- [[01_操作系统_OS/05_IO管理/io-api.md|输入输出应用程序接口]]
- [[01_操作系统_OS/05_IO管理/buffer.md|缓冲区管理]]
- [[01_操作系统_OS/05_IO管理/device-allocation.md|设备分配与回收]]
- [[01_操作系统_OS/05_IO管理/spooling.md|假脱机技术（SPOOLing）]]
- [[01_操作系统_OS/05_IO管理/driver-interface.md|设备驱动程序接口]]
- [[01_操作系统_OS/05_IO管理/disk.md|磁盘结构与调度]]
- [[01_操作系统_OS/05_IO管理/hdd-simulator.md|机械硬盘模拟器使用指南]]
- [[01_操作系统_OS/05_IO管理/ssd.md|固态硬盘]]