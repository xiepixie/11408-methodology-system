---
title: 操作系统基本概念与发展历程 ​
source: https://www.codebrick.tech/os-blog/posts/overview/os-concept.html
---

# 操作系统基本概念与发展历程

2026 大纲 **一（一）操作系统的基本概念** 与 **一（二）操作系统发展历程**。

## 速查

| **OS 的三个身份** | **资源管理者**（管 CPU/内存/设备/文件）、**用户接口**（命令接口、程序接口、GUI）、**扩充机器**（把裸机接口抬高一层） |
| --- | --- |
| **"扩充机器"扩充了什么** | 扩充的是**接口的抽象层次**：裸机只提供**指令级**接口（向端口写一个字节、等状态位变 1），OS 包一层后向上提供**系统调用级**接口（打开文件、读 100 字节）。结果是复杂度被吸收、设备差异被抹平 |
| 🔴 **并发 vs 并行** | 判据是看**时间段**还是**时刻**。并发＝同一时间段内交替执行，**单核也能做到**；并行＝同一时刻同时执行，**必须多核**。所以"单核不能并发"是错的（单核不能的是并行），"多道程序需要多核"也是错的 |
| 🔴 **"多道"多的是什么** | 多的是**同时驻留在内存中的作业道数**，不是 CPU 数、也不是同一时刻在跑的程序数。**内存里有几道程序就是几道**；单核下同一时刻占着 CPU 的仍只有一道 |
| 🔴 **虚拟的两种复用方式** | 判据是**被切分的维度是时间还是空间**。**时分复用**（虚拟处理器、SPOOLing 虚拟打印机）、**空分复用**（虚拟存储器、磁盘分区）。CPU 只能时分——它无法被劈成两半同时执行两个进程 |
| 🔴 **异步不止是"速度不确定"** | 异步是**现象**，它真正的分量在于逼出一条底线：不论各进程实际以什么速度推进，**只要输入相同，计算结果就必须相同**（结果的可再现性）。同步互斥机制的全部理由就是守住它 |
| 🔴 **三类系统的核心指标** | 批处理求**吞吐量**（周转时间反而很长）；分时求**交互性 + 平均响应时间**；实时求**最坏响应时间**（不是平均）。另有两处常搞反：**实时并非都用抢占式**（硬实时几乎必须抢占，软实时可非抢占）；**分时的时间片并非越短越好**（太短则切换开销吃掉有效时间） |
| **互斥共享 vs 同时共享** | 判据是**同一时刻是否允许第二个进程持有**。不允许＝互斥共享（打印机），允许＝同时共享（磁盘文件，"同时"是宏观口径，微观仍可交替） |
| 🔴 **多道程序不加快任何单个作业** | 它只是把 CPU 原本空转的时间填给别的作业——**分子（CPU 忙的时间）不变、分母（总时间）变小**，利用率因此提高 |

## 一、定义、身份与功能

操作系统是**一组能有效地组织和管理计算机硬件和软件资源、合理地对各类作业进行调度、以及方便用户使用的程序的集合**。定义里的三个动词恰好对应它的三个身份：

| 身份 | 干什么 | 缺了它会怎样 |
| --- | --- | --- |
| **资源管理者** | 管理处理器、存储器、I/O 设备、文件 | 多道程序会互相踩踏：抢 CPU、覆盖内存、争设备 |
| **用户接口** | 提供命令接口、程序接口（系统调用）、GUI | 用户只能对着裸机写机器码 |
| **扩充机器**（虚拟机器） | 把裸机接口**抬高一层** | 每写一个程序都要自己驱动磁盘、自己排布内存 |

裸机：指令级接口→ OS 包了一层 虚拟机器：系统调用级接口

所谓"虚拟机器"，指的就是"在用户看来，这台机器好像天生就能直接执行 `read`/`write` 这类操作"。四大功能则对应四类资源：

| 功能 | 管理对象 | 典型机制 |
| --- | --- | --- |
| 处理器管理 | CPU | 进程调度、进程同步与互斥 |
| 存储器管理 | 内存 | 地址映射、内存分配与回收、内存保护、虚拟内存 |
| 设备管理 | I/O 设备 | 缓冲管理、设备分配、设备处理（驱动程序）、虚拟设备 |
| 文件管理 | 文件 | 目录管理、存储空间管理、文件保护 |

## 二、四个特征：从并发一路推下去

四个特征不是并列的四条，而是一条推导链。**并发是起点**：多道程序同时驻留内存、交替执行 ⇒ 必须共用这一台机器的 CPU、内存、设备 ⇒ **共享**成为必然；反过来，若资源不能被共享（比如内存只能整块给一个程序），多道程序根本装不进来 ⇒ 并发也无从谈起。**所以并发与共享互为存在条件，是最基本的两个特征。**

| 特征 | 含义 | 由谁支撑 |
| --- | --- | --- |
| **并发** | 多个事件在同一**时间段**内发生（宏观同时推进、微观交替执行）——就像一个厨师在多个灶台之间来回切换，同一时刻只炒一个菜，但一段时间内几道菜都在推进 | 起点 |
| **共享** | 资源可供多个并发进程共同使用 | 与并发互为条件 |
| **虚拟** | 把一个物理实体变成多个逻辑上的对应物 | 建立在并发+共享上 |
| **异步** | 进程以不可预知的速度向前推进 | 建立在并发+共享上 |

后两条只有在并发与共享之上才谈得上：只有当一份物理资源被多个程序分着用，才有必要让每个程序"以为"自己独占了一份（**虚拟**）；只有当多个程序抢同一批资源、走走停停，推进速度才会变得不可预知（**异步**）。

异步逼出的那条底线值得单独记一句：**异步是现象，可再现性是必须守住的底线，同步互斥机制是守住它的手段**。只要程序之间存在未加保护的共享变量，异步就会把可再现性破坏掉——这正是[[01_操作系统_OS/02_进程管理/sync-mutex-concept.md|同步与互斥]]那一章要解决的问题。

## 三、发展历程：每一代都被上一代的具体矛盾逼出来

这一节的价值不在于记住五个阶段的名字，而在于记住四条驱动力：

图上补三处图里说不清的：

- **脱机 I/O** 是先用外围机把纸带内容转到磁带上，主机再从高速磁带读入。好处有两条：**装带卸带不占用主机时间**，且从磁带读入比从纸带快得多。**单道批处理还浪费内存**——为了能装下大作业配了大内存，可八成以上的作业都是中小型的。
- **多道程序设计**指把多个作业同时调入内存共享资源，一道因 I/O 暂停就把 CPU 转去执行另一道。它需要两项硬件技术才成立：**中断**（I/O 完成时主动通知 CPU，CPU 才不必轮询）与**通道**（I/O 传输由通道独立完成，不占 CPU）。**"中断与通道使 CPU 和 I/O 得以重叠"就是多道程序的物理基础。** 它同时带来一张新问题清单——处理机争用、内存分配与保护、I/O 设备分配、文件组织管理、作业管理、用户接口，**现代操作系统的几乎全部主题都是从这张清单上长出来的**。
- **分时的动力与多道批处理完全不同**：后者是为了提高资源利用率和吞吐量，前者是为了满足人机交互的需求——程序员写完新程序要上机调试，希望像早期一样独占全机并直接控制它，而计算机昂贵又必须让很多人共享。它用"每个人都稍微慢一点"换来了"每个人都能随时插话"。

多道批处理的收益与代价是成对出现的四条：

|  | 具体表现 | 由什么带来 |
| --- | --- | --- |
| **资源利用率高** | CPU 保持忙碌，内存与 I/O 设备的利用率也提高 | 一道阻塞就切另一道，空档被填上 |
| **系统吞吐量大** | 仅当作业完成或运行不下去时才切换，系统开销小 | CPU 与其他资源始终保持忙碌 |
| **平均周转时间长** | 作业要排队依次处理，可能要几小时甚至几天 | 同样是"排队等待"这一机制的反面 |
| **无交互能力** | 作业一旦提交，直到完成用户都无法干预，改错和调试极其不便 | 批处理没有终端这条回路 |

两道作业在单道与多道下的完整时间线：利用率到底提高了多少（想看清哪一段真的重叠上了、哪一段 CPU 还空着时展开）

两道作业，每道的执行序列都是「CPU 计算 20 ms → I/O 操作 40 ms → CPU 计算 10 ms」，单 CPU，两道作业使用**各自独立的 I/O 设备**（互不争用），忽略切换开销。

**单道。** 内存中只有一道作业，作业 1 全部做完才轮到作业 2，不存在任何重叠的可能：总时间 =(20+40+10)×2=140 ms。CPU 只在计算阶段忙：(20+10)×2=60 ms，利用率 =60/140=42.9%。I/O 期间 CPU 完全空转——这正是单道批处理的病根。

**多道。** 收益全部来自"一道在 I/O 时另一道占着 CPU"，而重叠能发生多少取决于两道作业各阶段的时长关系，所以必须排时间线，直接套公式会把两段空闲漏掉：

| 时刻（ms） | CPU 在做 | 各作业状态 |
| --- | --- | --- |
| 0 – 20 | 作业 1 计算 | 作业 2 就绪等 CPU |
| 20 – 40 | **作业 2 计算** | 作业 1 转入 I/O（20–60） |
| 40 – 60 | **空闲** | 作业 1 在 I/O，作业 2 也转入 I/O（40–80） |
| 60 – 70 | 作业 1 第二段计算 | 作业 1 的 I/O 于 60 完成 |
| 70 – 80 | **空闲** | 作业 1 已结束，作业 2 仍在 I/O |
| 80 – 90 | 作业 2 第二段计算 | 作业 2 的 I/O 于 80 完成 |

总时间 =90 ms；CPU 忙的总时间没变，仍是 60 ms（作业的计算量不会因为并发而减少），利用率 =60/90=66.7%。

**对照**：总时间 140 → 90 ms，利用率 42.9% → 66.7%。收益完全来自 20–40 这一段重叠，而这段重叠靠的正是中断与通道。若把作业数增加到三道、四道，空闲段会被进一步填满——这就是"多道"里那个"多"字的意义。

| 比较 | 分时系统 | 实时系统 | 判据 |
| --- | --- | --- | --- |
| 设计目标 | 交互性好 | 在**截止时间**前响应 | 看它优化的是平均值还是最坏值 |
| 核心指标 | 平均响应时间 | **最坏响应时间** |  |
| 调度方式 | 时间片轮转 | 抢占式优先级调度（软实时可非抢占） |  |
| 可靠性 | 一般要求 | 高可靠性 |  |
| 分类 | — | **硬实时**（必须在截止时间前完成，如导弹控制）/ **软实时**（偶尔超时可接受，如视频播放） |  |

## 本节小结

- OS 扮演**资源管理者、用户接口、扩充机器**三个身份；"扩充机器"扩充的是**接口的抽象层次**——把指令级接口抬高到系统调用级。
- 四个特征是一条链：**并发与共享互为存在条件、最为基本**，虚拟与异步建立在它们之上；虚拟只有**时分/空分**两种复用方式，异步的底线是**结果的可再现性**。
- 发展历程被矛盾推着走：人工操作（CPU 与 I/O 速度不匹配）→ 单道批处理（**监督程序**，但一道 I/O 就空转）→ 多道批处理（**中断 + 通道**换来利用率，代价是周转长、无交互）→ 分时（**时间片**换来交互，只保证平均够快）→ 实时（以**截止时间**为第一目标）。教材出处

- 汤小丹《计算机操作系统》**1.2.1 未配置操作系统的计算机系统**（p5~p6）：人工操作方式的两个缺点（用户独占全机、CPU 等待人工操作），以及"CPU 的速度在迅速提高，但 I/O 设备的速度却提高缓慢，这使 CPU 与 I/O 设备之间速度不匹配的矛盾更加突出"；脱机 I/O 方式的两个优点。
- 同书 **1.2.2 单道批处理系统**（p6~p7）：监督程序使作业一个接一个连续处理；"在内存中始终只保持一道作业"；缺点是"每逢该程序在运行中发出 I/O 请求后，CPU 便处于等待状态"。
- 同书 **1.2.3 多道批处理系统**（p7~p8）：多道程序设计的基本概念（多道作业调入内存共享资源、利用一道程序 I/O 的空档执行另一道）；四条优缺点（资源利用率高、吞吐量大、**平均周转时间长**、**无交互能力**）；以及多道批处理必须解决的六类问题，并由此给出操作系统的定义。
- 同书 **1.2.4 分时系统**（p8）："推动多道批处理系统形成和发展的主要动力是提高资源利用率和系统吞吐量，那么，推动分时系统形成和发展的主要动力，则是为了满足用户对人—机交互的需求。"

## 相关知识

[[01_操作系统_OS/01_操作系统概述/cpu-mode.md|CPU运行模式（内核态与用户态）]]｜[[01_操作系统_OS/01_操作系统概述/os-structure.md|操作系统结构]]｜[[01_操作系统_OS/02_进程管理/sync-mutex-concept.md|同步与互斥的基本概念]]

## 真题练习

### 相关真题（5题）
[2022Q232分](https://www.codebrick.tech/practice/q/os-2022-23?ctx=os-2022-23,os-2018-23,os-2017-28,os-2016-23,os-2016-24)[2018Q232分](https://www.codebrick.tech/practice/q/os-2018-23?ctx=os-2022-23,os-2018-23,os-2017-28,os-2016-23,os-2016-24)[2017Q282分](https://www.codebrick.tech/practice/q/os-2017-28?ctx=os-2022-23,os-2018-23,os-2017-28,os-2016-23,os-2016-24)[2016Q232分](https://www.codebrick.tech/practice/q/os-2016-23?ctx=os-2022-23,os-2018-23,os-2017-28,os-2016-23,os-2016-24)[2016Q242分](https://www.codebrick.tech/practice/q/os-2016-24?ctx=os-2022-23,os-2018-23,os-2017-28,os-2016-23,os-2016-24)[[01_操作系统_OS/01_操作系统概述/cpu-mode.md|下一篇CPU运行模式（内核态与用户态）]]

### 相关文章

- [[01_操作系统_OS/01_操作系统概述/cpu-mode.md|CPU运行模式（内核态与用户态）]]
- [[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|中断和异常的处理]]
- [[01_操作系统_OS/01_操作系统概述/system-call.md|系统调用]]
- [[01_操作系统_OS/01_操作系统概述/link-load.md|程序的链接与装入]]
- [[01_操作系统_OS/01_操作系统概述/memory-image.md|运行时内存映像与地址空间]]
- [[01_操作系统_OS/01_操作系统概述/os-structure.md|操作系统结构（分层/模块化/宏内核/微内核/外核）]]
- [[01_操作系统_OS/01_操作系统概述/os-boot.md|操作系统引导]]
- [[01_操作系统_OS/01_操作系统概述/virtual-machine.md|虚拟机]]