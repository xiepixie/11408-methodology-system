---
title: CPU运行模式（内核态与用户态） ​
source: https://www.codebrick.tech/os-blog/posts/overview/cpu-mode.html
---

# CPU运行模式（内核态与用户态）

2026 大纲 **一（三）1 CPU 运行模式**，补充说明点名两个术语：内核模式、用户模式。

## 速查

| **模式位在哪** | PSW（程序状态字）里的**处理器状态位**。硬件提供 0~3 共 4 个特权级，但**通用操作系统只用 0 级与 3 级** |
| --- | --- |
| **特权指令的判据** | 凡**可能影响其他进程或整个系统**的操作。它们集中在三处：**改硬件状态**（I/O、定时器）、**改保护规则**（页表基址、界地址）、**改控制流权限**（PSW、开关中断、`iret`） |
| 🔴 **用户态执行了特权指令会怎样** | **指令根本不被执行**（译码阶段就被拒），硬件产生**非法指令异常**，陷入内核，OS **终止该进程**。只说"不能执行"是不完整的——保护是靠"违规即被终止"这一步合上闭环的 |
| 🔴 **陷入指令不是特权指令** | 系统调用指令（访管指令）必须能在用户态执行，否则用户永远发不出请求；它执行后**立刻由硬件带入内核态**，入口由内核的系统调用表决定。反过来 `iret` **是**特权指令 |
| 🔴 **特权 ≠ 敏感** | 特权指令看"执行时会不会因权限不足而陷入"，敏感指令看"碰不碰系统关键状态"。**特权一定敏感，敏感不一定特权** |
| 🔴 **上行硬件、下行软件** | 用户态→内核态由**硬件**完成（中断/异常/陷入自动置模式位并交换 PSW）；内核态→用户态由**软件**完成（OS 执行 `iret`）。这处不对称就是隔离的全部技术含量 |
| **进内核只有一条路** | 只有中断才使处理器由用户态转向内核态，含三类：外中断（异步）、内中断/异常（同步）、自愿性中断（主动陷入） |
| **内核成员怎么筛出来的** | 一条判据：**这件事在用户态做得成吗**。做不成的才进内核 |
| 🔴 **原语必须在内核态的直接原因** | 它的原子性**靠关中断实现**，而开关中断是特权指令。也因此原语必须写得很短 |
| 🔴 **态切换 ≠ 进程切换** | 系统调用返回后通常仍回同一个进程；但**进程切换必然发生在内核态** |

## 一、两种运行模式

| 模式 | 别名 | 模式位 | 可执行的指令 |
| --- | --- | --- | --- |
| **内核态**（内核模式） | 核心态、**管态**、特权态 | 0 | 所有指令（含特权指令） |
| **用户态**（用户模式） | **目态** | 1（或 3） | 只有非特权指令 |

相应地，系统中运行的程序分为**系统程序**与**应用程序**：两者是控制与被控制的关系，前者有权管理与分配资源，后者只能申请与使用系统资源。

## 二、特权指令逐项对照

| 指令 / 操作 | 特权指令 | 依据 |
| --- | --- | --- |
| 启动 I/O（`in` / `out` / DMA 启动） | **是** | 直接操作硬件，可读写他人数据 |
| 修改 PSW（程序状态字） | **是** | 会改变 CPU 模式位——**这条是隔离的命门** |
| 关中断 / 开中断（修改 IF） | **是** | 影响响应中断的能力，关中断即可独占 CPU |
| 设置 / 修改页表基址寄存器（PTBR） | **是** | 改地址翻译规则，等于换一套内存视图 |
| 修改重定位 / 界地址寄存器 | **是** | 改内存保护边界 |
| 装入定时器 | **是** | 影响时钟中断频率，可让 OS 再也夺不回 CPU |
| 中断返回指令（`iret`） | **是** | 从内核态降到用户态，会写模式位 |
| 系统调用指令（`int 0x80` / `syscall`，即**陷入指令 / 访管指令**） | **不是**（用户态可执行） | 但**执行后立刻**由硬件带入内核态 |
| 算术 / 逻辑运算 | 不是 | 不影响其他进程 |
| 访存指令 / 跳转指令 | 不是 | 越界由 MMU 硬件当场检测 |
| 读取系统时间 | 不是（指令本身） | 但通过系统调用获取的接口涉及陷入 |

凡是能让"检查机制本身失效"的操作，一律划入特权。

## 三、隔离的闭环：用户态执行特权指令会怎样

"特权指令只能在内核态执行"这句话本身不构成保护——**真正让隔离成立的，是"违规时会发生什么"。**

图中三步缺一条闭环就不成立：**指令根本没被执行**（不是"先执行再检查"，不存在"已经把 I/O 端口写坏了才报错"）；**产生的是异常（内中断）不是外中断**，它由当前这条指令自身引发；**异常处理程序运行在内核态**，违规者自己没有任何处置权。教材给出的处理原则是：非法指令、用户态使用特权指令、地址越界、非法存取等指令异常 → **终止进程**。

## 四、特权指令 vs 敏感指令

这一组概念是[[01_操作系统_OS/01_操作系统概述/virtual-machine.md|虚拟机]]那一篇的地基：

| 概念 | 定义 | 判据 |
| --- | --- | --- |
| **特权指令** | 只能在内核态执行，用户态执行会触发异常 | **执行时会不会因权限不足而陷入** |
| **敏感指令** | 会**读或改**机器的关键状态（模式位、中断开关、页表基址、机器配置），或其**行为随当前模式不同而不同** | **它碰不碰系统全局状态** |

麻烦出在"敏感但不特权"这一类：它读/改了关键状态，**但在用户态执行时并不报错，而是悄悄返回一个当前模式下的结果或干脆什么也不做**。例如某条读取中断开关状态的指令，在用户态执行既不陷入也不报错，只返回一个无意义的值——对普通应用无所谓，但被降到用户态运行的 Guest OS 会据此做出错误判断，而 VMM **根本收不到任何通知**。当且仅当所有敏感指令都是特权指令时，"陷入-模拟"这套虚拟化方案才自动成立。

## 五、两个方向的态切换

| 触发方式 | 说明 | 举例 |
| --- | --- | --- |
| 外中断（异步中断事件） | 来自 CPU 外部，与当前指令无关 | 时钟中断、I/O 完成中断 |
| 内中断（同步异常事件） | 指令执行过程中出现问题 | 缺页异常、除零异常、**用户态执行特权指令** |
| 自愿性中断（主动陷入） | 用户程序**主动**请求 OS 服务 | 系统调用（执行陷入指令 / 访管指令） |

**为什么不能让程序自己改模式位？**模式位在 PSW 里 ⇒ 改 PSW 是特权指令 ⇒ 用户态执行它会触发非法指令异常并被终止 ⇒ **"自行升权"这条路在硬件层面就是死的**。用户能做的只有"请求"。反方向的 `iret` 也必须是特权指令，否则用户程序就能自己构造一个"模式位=0"的 PSW 再加载它，隔离当场失效。

## 六、内核的组成：为什么偏偏是这几样

| 层次 | 组件 | **为什么它必须在内核态** |
| --- | --- | --- |
| 与硬件紧密相关 | **时钟管理** | 要装入定时器、响应时钟中断，都是特权操作；且**时钟中断是抢占式调度的物理基础**，若能被用户态关掉，OS 就再也夺不回 CPU |
|  | **中断处理** | 中断向量表、断点与现场保存都在内核空间，用户态连读都读不到；且中断到来时硬件已把模式位置 0 |
|  | **原语（原子操作）** | 原语要求执行期间**不可被打断**，实现手段是**关中断**，而开关中断是特权指令 |
| 内核功能 | 进程管理 | 要读写 PCB、修改就绪队列，都是内核数据结构 |
|  | 存储器管理 | 要改页表基址寄存器、界地址寄存器，都是特权指令 |
|  | 设备管理 | 要执行 I/O 指令、操作设备寄存器，都是特权指令 |

硬件那 4 个特权级各自是干什么的，以及原语的原子性为什么只能靠关中断（想按教材原文口径答"处理器状态有几种"，或想把"原语必须在内核态"这条推导链走通时展开）

**先说 4 个特权级。** 大多数计算机系统的处理器状态有 4 种，支持 0~3 级共 4 个特权级别：

| 级别 | 名目 | 权限 |
| --- | --- | --- |
| **0 级** | 处理 I/O 与中断 | 权限最高，即内核态 |
| 1 级 | 系统调用级 |  |
| 2 级 | 共享库级 |  |
| **3 级** | 应用程序级 | 权限最低，即用户态 |

但**几乎所有通用操作系统实现都只使用了 0 级与 3 级**，即简单区分内核态与用户态，中间两级在实践中被跳过了——这也是大纲只点名内核模式与用户模式的现实依据。

**再说原语。**

**原语（primitive）**是由若干条指令组成的、完成某个特定功能的一段程序，其**执行必须一气呵成**——要么全做完，要么一条都不做。

为什么必须这样？以"把一个进程从阻塞队列摘下、挂到就绪队列"为例：这个动作要改好几个指针，做到一半被打断，队列就处于自相矛盾的中间状态；此时若切换到另一个进程也去动这个队列，数据结构就彻底坏了。

实现手段最直接的一种就是关中断：关中断→执行原语全部指令→开中断

关中断期间 CPU 不响应任何可屏蔽中断，**没有中断就不会发生进程切换**，这段代码自然不可能被打断。而开关中断是特权指令——用户态程序连"关中断"这个动作都做不出来，也就无法保证任何原子性。

推导链完整写出来是：原语要求执行期间不被打断 ⇒ 打断只可能来自中断（及由中断引发的进程切换）⇒ 屏蔽办法是关中断 ⇒ 关中断是特权指令 ⇒ 用户态做不到 ⇒ **原语只能在内核态**。

这也解释了为什么原语必须**短**：关中断期间时钟中断也被挡在门外，原语越长，整个系统对外部事件的响应就越迟钝。

## 本节小结

- 运行模式由 PSW 中的**处理器状态位**标识；特权指令的判据只有一条——**是否可能影响其他进程或整个系统**，因而集中在改硬件状态、改保护规则、改控制流权限三处。
- 隔离的闭环靠"违规会怎样"合上：用户态执行特权指令 → **指令不被执行** → 硬件产生**非法指令异常** → 陷入内核 → OS **终止该进程**。
- 两个方向不对称：**上行由硬件完成**（用户无法伪造），**下行由软件完成**（且需一条特权指令 `iret`）；进内核的唯一途径是中断的三类情况。教材出处

- 孙钟秀、费翔林《操作系统教程》（第 6 版）**2.1 处理器**（p43）：处理器状态位是"程序状态字中的比特位"；"大多数计算机系统的处理器状态有 4 种，分别支持 0~3 级共 4 个特权级别"，而"几乎所有通用操作系统实现都只使用了 0 级与 3 级"；处理器状态转换一节给出"**只有中断才会使处理器从用户态向内核态转换**"及其三类情况，并指出"计算机系统通常会提供一条称作加载程序状态字的特权指令（如 iret 指令），用于实现从内核态返回用户态"。
- 同书 **2.2.2 中断源·程序性中断事件**（p46）：处理原则中明确"**非法指令、用户态使用特权指令、地址越界、非法存取等指令异常：终止进程**"。
- 同书 **1.3.5 系统调用**（p25）："由于系统调用而引起处理器中断的机器指令称为陷入指令（trap），它是**非特权指令**，在用户态下执行时会产生 CPU 模式切换。"

## 相关知识

[[01_操作系统_OS/01_操作系统概述/os-concept.md|操作系统基本概念与发展历程]]｜[[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|中断和异常的处理]]｜[[01_操作系统_OS/01_操作系统概述/system-call.md|系统调用]]｜[[01_操作系统_OS/01_操作系统概述/virtual-machine.md|虚拟机]]

## 真题练习

### 相关真题（4题）
[2022Q272分](https://www.codebrick.tech/practice/q/os-2022-27?ctx=os-2022-27,os-2021-23,os-2014-25,os-2011-24)[2021Q232分](https://www.codebrick.tech/practice/q/os-2021-23?ctx=os-2022-27,os-2021-23,os-2014-25,os-2011-24)[2014Q252分](https://www.codebrick.tech/practice/q/os-2014-25?ctx=os-2022-27,os-2021-23,os-2014-25,os-2011-24)[2011Q242分](https://www.codebrick.tech/practice/q/os-2011-24?ctx=os-2022-27,os-2021-23,os-2014-25,os-2011-24)[[01_操作系统_OS/01_操作系统概述/os-concept.md|上一篇操作系统基本概念与发展历程]][[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|下一篇中断和异常的处理]]

### 相关文章

- [[01_操作系统_OS/01_操作系统概述/os-concept.md|操作系统基本概念与发展历程]]
- [[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|中断和异常的处理]]
- [[01_操作系统_OS/01_操作系统概述/system-call.md|系统调用]]
- [[01_操作系统_OS/01_操作系统概述/link-load.md|程序的链接与装入]]
- [[01_操作系统_OS/01_操作系统概述/memory-image.md|运行时内存映像与地址空间]]
- [[01_操作系统_OS/01_操作系统概述/os-structure.md|操作系统结构（分层/模块化/宏内核/微内核/外核）]]
- [[01_操作系统_OS/01_操作系统概述/os-boot.md|操作系统引导]]
- [[01_操作系统_OS/01_操作系统概述/virtual-machine.md|虚拟机]]