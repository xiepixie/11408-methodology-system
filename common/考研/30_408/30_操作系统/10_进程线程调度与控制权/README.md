# 进程、线程、调度与控制权

状态：目录已建立，已有发布物待纳管。

母问题：CPU 控制权怎样在执行实体之间安全转移？隔离的进程又怎样通过受控 IPC 交换信息？Owns task、context、PCB/TCB 语义、state、run/wait queue、scheduling、block/wakeup，以及 IPC 的语义分类与进程侧状态；不拥有同步协议、共享页映射细节、OFD、运输层 socket 语义或设备搬运。

