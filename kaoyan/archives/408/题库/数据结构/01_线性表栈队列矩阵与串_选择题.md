# 01｜线性表、栈队列、矩阵与串｜选择题

> 来源题号：1–66  
> 本文件只保留正式题面。

[查看答案与逐题解析](01_线性表栈队列矩阵与串_答案与解析.md)

## 基础概念、复杂度与线性表

### 1
以下属于逻辑结构的是（ ）。

A. 顺序表  B. 哈希表  C. 有序表  D. 单链表

### 2
以下关于数据结构的说法中，正确的是（ ）。

A. 数据的逻辑结构独立于其存储结构  
B. 数据的存储结构独立于其逻辑结构  
C. 数据的逻辑结构唯一决定其存储结构  
D. 数据结构仅由其逻辑结构和存储结构决定

### 3
程序段如下：

```c
for (i = n - 1; i > 1; i--)
    for (j = 1; j < i; j++)
        if (A[j] > A[j + 1])
            A[j] 与 A[j + 1] 交换;
```

其中 $n$ 为正整数，则最后一行语句的频度在最坏情况下的**紧确渐近量级**是（ ）。

A. $\Theta(n)$  B. $\Theta(n\log_2 n)$  C. $\Theta(n^3)$  D. $\Theta(n^2)$

### 4
若代码中 `combine` 函数处理长度为 $L$ 的区间时运行时间为 $\Theta(L)$，现对一个**长度为 $n$ 的区间**调用下列 `combineSort`，则其时间复杂度的紧确渐近量级是（ ）。

```c
void combineSort(int i, int j) {
    if (i != j) {
        int k = (i + j) / 2;
        combineSort(i, k);
        combineSort(k + 1, j);
        combine(i, j, k);
    }
}
```

A. $\Theta(n^2)$  B. $\Theta(n^3)$  C. $\Theta(n^{3/2})$  D. $\Theta(n\log_2 n)$

### 5
某算法的空间复杂度为 $O(1)$，则（ ）。

A. 该算法执行不需要任何辅助空间  
B. 该算法执行所需辅助空间大小与问题规模 $n$ 无关  
C. 该算法执行不需要任何空间  
D. 该算法执行所需空间大小与问题规模 $n$ 无关

### 6
设 $n\ge2$ 是描述问题规模的整数，下面程序片段的运行次数的紧确渐近量级是（ ）。

```c
int x = n * n;
while (x >= 1) {
    x = x / 2;
}
```

A. $\Theta(\log_2 n)$  B. $\Theta(n)$  C. $\Theta(n\log_2 n)$  D. $\Theta(n^{1/2})$

### 7
在线性表的顺序存储（数组表示）中，设当前存储区**仍有足够剩余容量，不发生扩容**。下列时间复杂度为 $O(1)$ 的操作是（ ）。

I. 访问第 $i\ (1\le i\le n)$ 个结点和求第 $i\ (2\le i\le n)$ 个结点的直接前驱  
II. 在最后一个结点后插入一个新的结点  
III. 删除第 1 个结点  
IV. 在第 $i\ (1\le i\le n)$ 个结点后插入一个结点

A. I  B. II、III  C. I、II  D. I、II、III

### 8
设线性表有 $n$ 个元素，严格说来，以下操作中，（ ）在顺序表上实现要比在链表上实现的效率高。

I. 输出第 $i\ (1\le i\le n)$ 个元素值  
II. 交换第 3 个元素与第 4 个元素的值  
III. 顺序输出这 $n$ 个元素的值

A. I  B. I、III  C. I、II  D. II、III

### 9
对于顺序存储的线性表，其算法时间复杂度为 $O(1)$ 的运算应该是（ ）。

A. 将 $n$ 个元素从小到大排序  
B. 删除第 $i\ (1\le i\le n)$ 个元素  
C. 改变第 $i\ (1\le i\le n)$ 个元素的值  
D. 在第 $i\ (1\le i\le n)$ 个元素后插入一个新元素

### 10
长度分别为 $m$ 和 $n$ 的升序顺序表，若将它们合并为一个长度为 $m+n$ 的升序顺序表，则最坏情况下算法执行的比较次数为（ ）。

A. $m+n-1$  B. $m-n$  C. $m+n$  D. $n$

### 11
采用动态分配存储空间的顺序表，若已存放 $n$ 个元素并存满存储空间，现需要再存放 $m$ 个元素。若扩容策略是**重新申请一块能连续容纳全部 $n+m$ 个元素的新存储区，再复制原表**，则申请成功表明内存中至少存在（ ）个地址连续可分配的空闲空间。

A. $m$  B. $m-n$  C. $n+m$  D. $n$

### 12
若长度为 $n$ 的非空线性表采用顺序存储结构，在表的第 $i$ 个位置插入一个数据元素，则 $i$ 的合法值应该是（ ）。

A. $1\le i\le n$  B. $1\le i\le n+1$  C. $0\le i\le n-1$  D. $0\le i\le n$

## 链表

### 13
在一个单链表中，已知 $q$ 所指结点是 $p$ 所指结点的前驱结点，如果要在 $q$ 和 $p$ 之间插入结点 $s$，则执行（ ）。

A. `s->next = p->next; p->next = s;`  
B. `p->next = s->next; s->next = p;`  
C. `q->next = s; s->next = p;`  
D. `p->next = s; s->next = q;`

### 14
现有两个普通单链表，**只给出各自头指针，且均未维护尾指针**。将长度为 $n$ 的单链表链接在长度为 $m$ 的单链表后面，其运行时间的紧确渐近量级为（ ）。

A. $\Theta(1)$  B. $\Theta(n)$  C. $\Theta(m)$  D. $\Theta(n+m)$

### 15
在一个长度为 $n$ 的带头结点单链表 $h$ 上，设有尾指针 $r$，执行（ ）操作与链表的表长有关。

A. 删除单链表中的第一个元素  
B. 删除单链表中的最后一个元素  
C. 在单链表第一个元素前插入一个新元素  
D. 在单链表最后一个元素后插入一个新元素

### 16
已知一个带头结点的单向循环链表，**仅保存头结点指针，不另设尾指针**，长度为 $n$。以下关于在首尾进行插入或删除操作的时间复杂度描述正确的是（ ）。

A. 头部插入 $\Theta(n)$，尾部插入 $\Theta(1)$  
B. 头部插入 $\Theta(1)$，尾部插入 $\Theta(1)$  
C. 头部删除 $\Theta(n)$，尾部删除 $\Theta(1)$  
D. 头部删除 $\Theta(1)$，尾部删除 $\Theta(n)$

### 17
已知一个双向循环链表，首尾相连，**保存表头结点指针，可由 `head->prior` 直接取得表尾结点**，长度为 $n$。以下关于在首尾进行插入或删除操作的时间复杂度描述正确的是（ ）。

A. 头部插入 $\Theta(n)$，尾部插入 $\Theta(1)$  
B. 头部插入 $\Theta(1)$，尾部插入 $\Theta(n)$  
C. 头部删除 $\Theta(n)$，尾部删除 $\Theta(1)$  
D. 头部删除 $\Theta(1)$，尾部删除 $\Theta(1)$

### 18
对于一个头指针为 `head` 的带头结点单链表，判定该表为空表的条件是（ ）；对于不带头结点的单链表，判定空表的条件为（ ）。

① `head == NULL`  ② `head->next == NULL`  ③ `head->next == head`  ④ `head != NULL`

A. ④①  B. ②①  C. ②③  D. ③④

### 19
在双链表中向 `p` 所指结点之前插入一个结点 `q` 的操作为（ ）。

A. `p->prior=q; q->next=p; p->prior->next=q; q->prior=p->prior;`  
B. `q->prior=p->prior; p->prior->next=q; q->next=p; p->prior=q->next;`  
C. `q->next=p; p->next=q; q->prior->next=q; q->next=p;`  
D. `p->prior->next=q; q->next=p; q->prior=p->prior; p->prior=q;`

### 20
在双向链表存储结构中，删除 `p` 所指的结点时必须修改指针（ ）。

A. `p->prior->next = p->next; p->next->prior = p->prior;`  
B. `p->prior = p->prior->prior; p->prior->next = p;`  
C. `p->next->prior = p; p->next = p->next->next;`  
D. `p->next = p->prior->prior; p->prior = p->next->next;`

### 21
在双链表中，已知指针 `p` 指向结点 A，若要在结点 A 和 C 之间插入指针 `q` 所指结点 B，即在 `p` 所指结点 A 之后插入 B，给定语句：

① `q->next = p->next;`  ② `q->prior = p;`  ③ `p->next = q;`  ④ `p->next->prior = q;`

依次执行的语句序列可以是（ ）。

A. ①②④③  B. ④③②①  C. ③④①②  D. ①③④②

### 22
对于一个带头结点的循环单链表 `L`，判断该表为空表的条件是（ ）。

A. 头结点的指针域为空  
B. `L` 的值为 `NULL`  
C. 头结点的指针域与 `L` 的值相等  
D. 头结点的指针域与 `L` 的地址相等

### 23
对于一个带头结点的双循环链表 `L`，判断该表为空表的条件是（ ）。

A. `L->prior == L && L->next == NULL`  
B. `L->prior == NULL && L->next == NULL`  
C. `L->prior == NULL && L->next == L`  
D. `L->prior == L && L->next == L`

### 24
一个链表最常用的操作是在末尾插入结点和删除结点，则选用（ ）最节省时间。

A. 带头结点的双循环链表  
B. 单循环链表  
C. 带尾指针的单循环链表  
D. 单链表

### 25
设有两个长度为 $n$ 的单链表（均带头结点），结点类型相同。若以 $h_1$ 为头结点指针的链表是非循环的，以 $h_2$ 为头结点指针的链表是循环的，则（ ）。

A. 删除开始结点的时间复杂度分别为 $O(1)$ 和 $O(n)$  
B. 删除终端结点的时间复杂度都是 $O(n)$  
C. 循环链表要比非循环链表占用更多的内存空间  
D. $h_1$ 和 $h_2$ 是不同类型的变量

### 26
设对 $n\ (n>1)$ 个元素的线性表只进行 4 种运算：删除第一个元素、删除最后一个元素、在第一个元素之前插入新元素、在最后一个元素之后插入新元素，则最好使用（ ）。

A. 只有尾结点指针、没有头结点指针的循环单链表  
B. 只有尾结点指针、没有头结点指针的非循环双链表  
C. 只有头结点指针、没有尾结点指针的循环双链表  
D. 既有头结点指针又有尾结点指针的循环单链表

### 27
关于静态链表，以下选项不正确的是（ ）。

A. 静态链表既有顺序存储的优点，又有动态链表的优点，所以它存取表中第 $i$ 个元素的时间与 $i$ 无关  
B. 静态链表中能容纳的元素个数在表定义时就确定了，在后续操作中不能增加  
C. 静态链表与动态链表在元素插入、删除上类似，不需要移动元素  
D. 静态链表相比动态链表有可能浪费较多存储空间

## 栈与队列

### 28
假定用数组 `a[n]` 存储一个栈，初始栈顶指针 `top=-1`，则元素 `x` 进栈的操作是（ ）。

A. `a[--top] = x`  B. `a[top--] = x`  C. `a[++top] = x`  D. `a[top++] = x`

### 29
假定用数组 `data[1...n]` 存储一个栈，初始栈顶指针 `top=1`，且 `top` 指向下一空闲位置，则元素 `x` 进栈的操作是（ ）。

A. `data[top--] = x`  B. `data[top++] = x`  C. `data[--top] = x`  D. `data[++top] = x`

### 30
假定用数组 `data[1...n]` 存储一个从高下标向低下标增长的栈，初始栈顶指针 `top=n+1`，则元素 `x` 进栈的操作是（ ）。

A. `data[--top] = x`  B. `data[top++] = x`  C. `data[top--] = x`  D. `data[++top] = x`

### 31
设链表不带头结点且所有栈操作均在逻辑表头进行，则下列最不适合作为链栈的是（ ）。

A. 只有表头结点指针，没有表尾指针的双向循环链表  
B. 只有表尾结点指针，没有表头指针的双向循环链表  
C. 只有表头结点指针，没有表尾指针的单向循环链表  
D. 只有表尾结点指针，没有表头指针的单向循环链表

### 32
3 个不同元素依次进栈，能得到（ ）种不同的出栈序列。

A. 4  B. 5  C. 6  D. 7

### 33
设 `a,b,c,d,e,f` 以所给次序进栈，在进栈过程中允许随时出栈，则下面得不到的出栈序列为（ ）。

A. `fedcba`  B. `bcafed`  C. `dcefba`  D. `cabdef`

### 34
4 个元素依次进栈的次序为 `a,b,c,d`，则以 `c,d` 开头的出栈序列个数为（ ）。

A. 1  B. 2  C. 3  D. 4

### 35
若栈的输入序列是 $1,2,3,\ldots,n$，输出序列的第一个元素是 $n$，则第 $i$ 个输出元素是（ ）。

A. 不确定  B. $n-i$  C. $n-i-1$  D. $n-i+1$

### 36
若栈的输入序列是 $1,2,3,\ldots,n$，输出序列的第一个元素是 $t$，则第 $j$ 个输出元素是（ ）。

A. 不确定  B. $i-j-1$  C. $t-j$  D. $j-i+1$

### 37
某栈的输入序列为 `a,b,c,d`，下面 4 个序列中不可能为其输出序列的是（ ）。

A. `a,b,c,d`  B. `c,b,d,a`  C. `d,c,a,b`  D. `a,c,b,d`

### 38
若栈的输入序列是 $P_1,P_2,\ldots,P_n$，输出序列是 $1,2,3,\ldots,n$，若 $P_3=1$，则 $P_1$ 的值（ ）。

A. 可能是 2  B. 一定是 2  C. 不可能是 2  D. 不可能是 3

### 39
若栈的输入序列是 $P_1,P_2,\ldots,P_n$，输出序列是 $1,2,3,\ldots,n$，若 $P_3=3$，则 $P_1$ 的值（ ）。

A. 可能是 2  B. 不可能是 1  C. 一定是 1  D. 一定是 2

### 40
已知栈的入栈序列是 `1,2,3,4`，其出栈序列为 $P_1,P_2,P_3,P_4$，则 $(P_2,P_4)$ 不可能是（ ）。

A. $(2,4)$  B. $(2,1)$  C. $(4,3)$  D. $(3,4)$

### 41
设栈 `s` 和队列 `q` 的初始状态都为空，元素 `a,b,c,d,e,f` 依次通过栈 `s`，一个元素出栈后即进入队列 `q`。若 6 个元素出队的序列是 `b,d,c,f,e,a`，则栈 `s` 的容量至少可以存（ ）个元素。

A. 2  B. 3  C. 4  D. 5

### 42
设有一个顺序共享栈 `Share[0:n-1]`，其中第一个栈顶指针 `top1` 的初值为 `-1`，第二个栈顶指针 `top2` 的初值为 `n`，则判断共享栈满的条件是（ ）。

A. `top2 - top1 == 1`  B. `top1 - top2 == 1`  C. `top1 == top2`  D. 都不对

### 43
假设用数组 `Q[MaxSize]` 实现循环队列，队首指针 `front` 指向队首元素的前一个位置，队尾指针 `rear` 指向队尾元素。下列条件中，与“队列为空”等价的是（多选）。

A. `(Q.rear+1)%MaxSize == (Q.front+1)%MaxSize`  
B. `(Q.rear+1)%MaxSize == Q.front+1`  
C. `(Q.rear+1)%MaxSize == Q.front`  
D. `Q.rear == Q.front`

### 44
用不带头结点的单链表存储队列时，队头指针指向队头结点，队尾指针指向队尾结点，则在进行出队操作时（ ）。

A. 仅修改队头指针  B. 仅修改队尾指针  C. 队头、队尾指针都要修改  D. 队头、队尾指针都可能要修改

### 45
假设循环队列 `Q[MaxSize]` 中 `front` 指向队首元素的前一个位置，`rear` 指向队尾元素；此外没有计数器或其他状态字段，并采用**牺牲一个数组单元来区分队空与队满**的方案，则判断队满的条件是（ ）。

A. `Q.front == Q.rear`  
B. `Q.front == (Q.rear+1)%MaxSize`  
C. `Q.front + Q.rear >= MaxSize`  
D. `Q.rear == (Q.front+1)%MaxSize`

### 46
假设用 `A[0...n]` 实现循环队列，`front`、`rear` 分别指向队首元素的前一个位置和队尾元素。若用 `(rear+1)%(n+1)==front` 作为队满标志，则（ ）。

A. 可用 `front==rear` 作为队空标志  
B. 队列中最多可有 $n+1$ 个元素  
C. 可用 `front>rear` 作为队空标志  
D. 可用 `(front+1)%(n+1)==rear` 作为队空标志

### 47
已知循环队列的存储空间为数组 `A[21]`，`front` 指向队头元素的前一个位置，`rear` 指向队尾元素。若当前 `front=8`、`rear=3`，则该队列的长度为（ ）。

A. 5  B. 6  C. 16  D. 17

### 48
以数组 `Q[0,...,m-1]` 存放循环队列中的元素，变量 `rear` 和 `qulen` 分别指示队尾元素的实际位置和当前队列中元素的个数，队列第一个元素的实际位置是（ ）。

A. `rear-qulen`  
B. `rear-qulen+m`  
C. `m-qulen`  
D. $(1+(rear-qulen+m))\bmod m$

### 49
若要求链式队列的入队、出队都为 $O(1)$，并希望**只保留一个外部入口指针**，则最适合的链表是（ ）。

A. 带队首指针和队尾指针的循环单链表  
B. 带队首指针和队尾指针的非循环单链表  
C. 只带队首指针的非循环单链表  
D. 只带队尾指针的循环单链表

### 50
若按链式队列的基本目标比较：**队头删除与队尾插入应尽量在 $O(1)$ 内完成**，则下列链表中最不适合的是（ ）。

A. 只带队首指针的非循环双链表  
B. 只带队首指针的循环双链表  
C. 只带队尾指针的循环双链表  
D. 只带队尾指针的循环单链表

### 51
用两个栈 $S_1$ 和 $S_2$ 模拟一个队列，若入队操作是向 $S_1$ 入栈，则出队操作是（ ）。

A. 栈 $S_1$ 出栈  
B. 栈 $S_2$ 出栈  
C. 若 $S_1$ 不空，$S_1$ 出栈；否则把 $S_2$ 中元素转回 $S_1$ 再出栈  
D. 若 $S_2$ 不空，$S_2$ 出栈；否则把 $S_1$ 中元素依次转入 $S_2$，直到 $S_1$ 空，再由 $S_2$ 出栈

### 52
在用单链表（无头结点）实现队列时，队头设在链表的（ ）位置。

A. 链头  B. 链尾  C. 链中  D. 以上都可以

### 53
用单链表（含头结点）表示队列，队头指针在链表的（ ）位置。

A. 链头  B. 链尾  C. 链中  D. 链尾或链中

### 54
用单链表（含头结点）表示链队，队尾指针在链表的（ ）位置。

A. 链头  B. 链尾  C. 链中  D. 以上都可以

## 表达式、特殊矩阵与 KMP

### 55
中缀表达式 $(A+B)*(C-D)/(E-F*G)$ 的后缀表达式是（ ）。

A. `A+B+C-D/E-F*G`  
B. `AB+CD-*EFG*-/`  
C. `AB+C*D-E/F-G*`  
D. `ABCDEFG+*-/ -*`

### 56
已知后缀表达式为 `ABC*+DE/-`，则其前缀形式为（ ）。

A. `-A+B*C/DE`  B. `-A+B*CD/E`  C. `-+*ABC/DE`  D. `-+A*BC/DE`

### 57
利用栈求表达式的值时，设操作符栈 `SK` 只有两个存储单元，在下列表达式中，不发生上溢的是（ ）。

A. $A-B\times(C-D)$  
B. $(A-B)\times C-D$  
C. $(A-B\times C)-D$  
D. $(A-B)\times(C-D)$

### 58
用足够容量的一维数组 $B$ 对 $n\times n$ 阶对称矩阵 $A$ 进行压缩存储，若 $B$ 中只存储 $A$ 的下三角元素，则 $A[i,j]\ (i<j,\ 0\le i,j\le n-1)$ 存储在 $B$ 中对应的元素为（ ）。

A. $B[jn/2+i]$  
B. $B[i(i+1)/2+j]$  
C. $B[j(j+1)/2+i]$  
D. $B[in/2+j]$

### 59
下三角矩阵 $A(n\times n)$ 按列优先顺序压缩在数组 $S[(n+1)n/2]$ 中，若非零元素 $a_{i,j}\ (0\le j\le i<n)$ 存放在 $S[k]$ 中，则 $i,j,k$ 的关系为（ ）。

A. $k=in+j$  
B. $k=(2n-j+1)j/2+i-1$  
C. $k=i(i+1)/2+j$  
D. $k=(2n-j+1)j/2+i-j$

### 60
设主串长度为 $n$，模式串长度为 $m$，则简单模式匹配算法的最坏时间复杂度上界为 ___，KMP（包含模式预处理）的时间复杂度上界为（ ）。

① $O(m)$  ② $O(n)$  ③ $O(mn)$  ④ $O(m+n)$

A. ①④  B. ③④  C. ③①  D. ②④

### 61
在 KMP 匹配中，用 `next` 数组存放模式串的部分匹配信息。当模式串位置 `j` 与主串位置 `i` 比较时两个字符不相等，则 `j` 的位移方式是（ ）。

A. `j=0`  B. `j=j+1`  C. `j` 不变  D. `j=next[j]`

### 62
在 KMP 匹配中，用 `next` 数组存放模式串的部分匹配信息。当模式串位置 `j` 与主串位置 `i` 比较时两个字符不相等，则 `i` 的位移方式是（ ）。

A. `i=next[i]`  B. `i` 不变  C. `i=0`  D. `i=i+1`

### 63
采用 **1 基下标**的 KMP `next` 约定：`next[1]=0`，当模式串第 $j$ 位失配时令 `j=next[j]`。已知模式串 $S=\texttt{aaab}$，其 `next` 数组值为（ ）。

A. `0123`  B. `0112`  C. `0231`  D. `1211`

### 64
采用与上一题相同的 **1 基下标** KMP `next` 约定，串 `ababaaababaa` 的 `next` 数组值为（ ）。

A. `01234567899`  
B. `012121111212`  
C. `011234223456`  
D. `0123012322345`

### 65
串 `ababaaababaa` 的 `next` 数组（采用 `next[0]=-1` 的教材约定）为（ ）。

A. `-1,0,1,2,3,4,5,6,7,8,8,8`  
B. `-1,0,1,0,1,0,0,0,0,1,0,1`  
C. `-1,0,0,1,2,3,1,1,2,3,4,5`  
D. `-1,0,1,2,-1,0,1,2,1,1,2,3`

### 66
采用 **1 基下标** KMP 约定，`nextval[1]=0`；对 $j>1$，若 `next[j]=0` 或 `P[j] != P[next[j]]`，则 `nextval[j]=next[j]`，否则 `nextval[j]=nextval[next[j]]`。串 `ababaaababaa` 的 `nextval` 数组为（ ）。

A. `0,1,0,1,1,2,0,1,0,1,0,2`  
B. `0,1,0,1,1,4,1,1,0,1,0,2`  
C. `0,1,0,1,0,4,2,1,0,1,0,4`  
D. `0,1,1,1,0,2,1,1,0,1,0,4`
