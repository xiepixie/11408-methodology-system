# 04｜查找、散列与排序｜选择题

> 来源题号：171–238  
> 本文件只保留正式题面。

[查看答案与逐题解析](04_查找散列与排序_答案与解析.md)

## 折半查找、BST、AVL、红黑树与 B/B+ 树

### 171
折半查找和二叉排序树的时间性能（ ）。

A. 相同  B. 有时不相同  C. 完全不同  D. 无法比较

### 172
已知有序表 `(10,20,25,30,45,60,100,150,300)`，当二分查找值为 25 的元素时，查找成功的元素比较次数为（ ）。

A. 1  B. 3  C. 4  D. 5

### 173
下列说法中正确的是（ ）。

A. 任何一棵含 $n$ 个结点的二叉查找树 A，都可以通过特定的 $O(n)$ 次旋转转变为另一棵含 $n$ 个结点的二叉查找树 B  
B. 满足任一分支结点的值都小于其右孩子、大于其左孩子的二叉树就是二叉查找树  
C. 在 BST 中查找关键字 $k$，设查找路径左侧、路径上、右侧关键字集合分别为 A、B、C，则任取 $a\in A,b\in B,c\in C$ 均有 $a\le b\le c$  
D. 给定一个确定的插入序列，从空 AVL 树开始按顺序插入可得到确定的 AVL 树

### 174
下列说法中，正确的是（ ）。

A. 只要数据元素保持有序，就一定可以采用折半查找法  
B. 折半查找与二叉查找树在最坏情况下时间性能相同  
C. 折半查找的查找速度一定比顺序查找快  
D. 折半查找一个元素平均需要 $O(\log_2 n)$ 次关键字比较

### 175
对于二叉排序树，下面说法中正确的是（ ）。

A. 对二叉排序树进行先序遍历可得到有序序列  
B. 用逐点插入法构造二叉排序树，若先后插入的关键字严格有序，所得树的深度达到最大  
C. 在二叉排序树中进行查找，关键字比较次数不超过结点数的 $1/2$  
D. 查找失败时插入新结点会引起树的重新分裂和组合

### 176
若有序表的关键字序列为 `{b,c,d,e,f,g,q,r,s,t}`，折半查找采用 `mid=\lfloor(low+high)/2\rfloor`（偶数个候选时取靠左的中点），则查找关键字 `b` 的过程中，比较关键字依次为（ ）。

A. `f,c,b`  B. `f,d,b`  C. `g,c,b`  D. `g,d,b`

### 177
已知一个长度为 16 的顺序表，其元素按关键字有序排列。若采用折半查找算法查找一个不存在的元素，则比较次数至少是（ ），至多是（ ）。

A. 4，5  B. 4，6  C. 3，4  D. 3，5

### 178
具有 12 个关键字的有序表中，对每个关键字的查找概率相同，折半查找成功的平均查找长度为（ ），查找失败的平均查找长度为（ ）。

A. $37/12,39/13$  B. $37/12,49/13$  C. $35/12,39/13$  D. $35/12,49/13$

### 179
为提高查找效率，将含 65025 个元素的有序顺序表**均匀分成 255 块，每块 255 个元素**，并建立按各块最大关键字有序的一级索引。若查找时**先在 255 项索引表中折半查找确定块，再在该块的 255 个元素中折半查找**，则查找一个表中已有元素最多需要执行（ ）次关键字比较。

A. 10  B. 14  C. 16  D. 21

### 180
设二叉排序树中关键字由 1 到 1000 的整数构成，现查找关键字 363。下述关键字序列中，不可能是在二叉排序树上的查找序列是（ ）。

A. `2,252,401,398,330,344,397,363`  
B. `924,220,911,244,898,258,362,363`  
C. `925,202,911,240,912,245,363`  
D. `2,399,387,219,266,382,381,278,363`

### 181
从空树开始，依次插入 `52,26,14,32,71,60,93,58,24,41` 后构成一棵二叉排序树。在该树查找 60 要进行比较的次数为（ ）。

A. 3  B. 4  C. 5  D. 6

### 182
含有 20 个结点的平衡二叉树（AVL）的最大深度为（ ）（根结点深度记为 1）。

A. 4  B. 5  C. 6  D. 7

### 183
将关键字 $1,2,3,\ldots,1024$ 依次插入初始为空的 AVL 树，假设只有一个根结点的树高度为 0，则插入结束后的 AVL 树高度是（ ）。

A. 8  B. 9  C. 10  D. 11

### 184
下列选项中，错误的是（ ）。

A. 非空红黑树的内部结点要么是红色，要么是黑色  
B. 在一棵红黑树中，如果一个内部结点是黑色的，那么它的非空孩子结点一定是红色的  
C. 在一棵红黑树中，如果所有内部结点都是黑色的，则黑高约束会强迫其各根到 NIL 路径含相同数量黑结点  
D. 红黑树的查找路径上不允许出现两个连续的红结点

### 185
下列关于红黑树和 AVL 树的说法中，不正确的是（ ）。

I. 一棵含 $n$ 个内部结点的红黑树高度至多为 $2\log_2(n+1)$  
II. 若一个结点是红色的，则它的父结点和非空孩子结点都是黑色的  
III. 红黑树的查询效率一般要优于含有相同结点数的 AVL 树  
IV. 若 AVL 树某结点的左右孩子的平衡因子都是 0，则该结点的平衡因子也一定为 0

A. I、III  B. III  C. II、IV  D. III、IV

### 186
下列关于红黑树的说法中，不正确的是（ ）。

A. 具有 $n$ 个关键字的红黑树中，红的内部结点数与黑的内部结点数之比最大为 $2:1$  
B. 如果一个结点是黑色的，则它的父结点和孩子结点都有可能是黑色的  
C. 插入 $n\ (n>1)$ 个结点形成的红黑树至少有 1 个红结点  
D. 通常情况下，与含有相同结点数的 AVL 树相比，红黑树的查询效率较优

### 187
下列关于红黑树和 AVL 树的描述中，不正确的是（ ）。

A. 两者都属于自平衡二叉查找树  
B. 两者查找、插入、删除的渐近时间复杂度均可达到 $O(\log n)$  
C. 红黑树插入和删除过程都至多有 2 次旋转操作  
D. 红黑树通过颜色/黑高约束控制高度，并不要求 AVL 的“左右子树高度差至多 1”

### 188
下列关于红黑树的说法中，正确的是（ ）。

A. 红结点数目最多和黑结点数目相同  
B. 若红黑树所有内部结点都是黑色，则黑高相等要求其内部结构为满二叉树形态  
C. 红黑树任何一个分支结点都有两个非空孩子  
D. 任取一个内部结点作为根得到的子树，在不重新着色该根的情况下都一定满足“根为黑色”的红黑树定义

### 189
下列关于 $m$ 阶 B 树的说法中，错误的是（ ）。

A. 根结点至多有 $m$ 棵子树  
B. 所有叶结点都在同一层次上  
C. 非叶结点至少有 $\lceil m/2\rceil$ 棵子树  
D. 根结点中的关键字有序

### 190
下列说法中，正确的是（ ）。

A. 在 9 阶 B 树中，除叶结点以外的任意结点的分支数介于 5 和 9 之间  
B. 高度为 8 的 3 阶 B 树中关键字数最少是 255  
C. 在一棵 7 阶 B 树中，一个结点最多有 6 棵子树  
D. AVL 树中若某结点的左右孩子平衡因子为 0，则该结点平衡因子一定为 0

### 191
以下关于 $m$ 阶 B 树的说法中，正确的是（ ）。

I. 每个结点至少有两棵非空子树  
II. 每个结点至多有 $m-1$ 个关键字  
III. 所有叶结点在同一层  
IV. 插入一个元素引起任一 B 树结点分裂后，整棵树一定长高一层

A. I、II  B. II、III  C. III、IV  D. I、II、IV

### 192
在一棵 $m$ 阶 B 树中做插入操作前，若一个结点中的关键字个数等于（ ），则再插入一个关键字会发生上溢并需要分裂；做删除操作前，若非根结点关键字个数等于（ ），删除后可能需要与兄弟合并。

A. $m,\lfloor m/2\rfloor-2$  
B. $m-1,\lceil m/2\rceil-1$  
C. $m+1,\lfloor m/2\rfloor$  
D. $m/2,\lfloor m/2\rfloor+1$

### 193
具有 $n$ 个关键字的 $m$ 阶 B 树，若把最底层失败查找的外部结点计作叶结点，则应有（ ）个叶结点。

A. $n+1$  B. $n-1$  C. $mn$  D. $nm/2$

### 194
高度为 5 的 3 阶 B 树至少有（ ）个结点，至多有（ ）个结点。

I. 32  II. 31  III. 120  IV. 121

A. II、IV  B. I、IV  C. II、III  D. I、III

### 195
含有 $n$ 个非叶内部结点的 $m$ 阶 B 树中至少包含（ ）个关键字。

A. $n(m+1)$  B. $n$  C. $n(\lceil m/2\rceil-1)$  D. $(n-1)(\lceil m/2\rceil-1)+1$

### 196
已知一棵 5 阶 B 树中共有 53 个关键字，则树的最大高度为（ ），最小高度为（ ）。

A. 3、2  B. 4、3  C. 4、2  D. 5、4

### 197
已知一棵 3 阶 B 树中有 2047 个关键字，则此 B 树的最大高度为（ ），最小高度为（ ）。

A. 11、10  B. 10、8  C. 10、7  D. 11、7

### 198
下列关于 B 树和 B+ 树的叙述中，不正确的是（ ）。

A. B 树和 B+ 树都同样依赖叶层链表来高效支持连续范围扫描  
B. B 树和 B+ 树都能有效支持随机查找  
C. B 树和 B+ 树都是平衡的多路查找树  
D. B 树和 B+ 树都可用于文件索引结构

### 199
在一棵**含 2016 个关键字**的 7 阶 B 树中查找某个关键字，若根结点已读入内存，则最坏情况下最多需再启动（ ）次 I/O。

A. 4  B. 5  C. 6  D. 7

## 散列表

### 200
下列关于散列表的说法中，正确的是（ ）。

I. 若填装因子 $\alpha<1$，则可避免冲突  
II. 散列查找中不需要任何关键字比较  
III. 查找成功时平均查找长度仅与散列表长度有关  
IV. 开放定址散列表中删除元素时不能简单把已占槽直接改成“从未使用”的空槽

A. I、IV  B. II、III  C. III  D. IV

### 201
关于散列表的描述，正确的是（ ）。

A. 双散列是链地址法的冲突处理方法  
B. 线性探测是开放定址法的一种冲突处理方法  
C. 装填因子表示冲突数量占整个散列表的比例  
D. 分离链接法解决冲突时必须把同义词放在二叉树中

### 202
假定有 $K$ 个关键字互为同义词，若用线性探测法把这 $K$ 个关键字填入初始空散列表，至少要进行（ ）次探测。

A. $K-1$  B. $K$  C. $K+1$  D. $K(K+1)/2$

### 203
对包含 $n$ 个元素的散列表进行查找，平均查找长度（ ）。

A. 必为 $O(\log_2 n)$  
B. 无条件为 $O(1)$  
C. 不直接由 $n$ 单独决定，还取决于装填因子、散列函数和冲突处理策略  
D. 只直接依赖于表长 $m$

### 204
关于 Hash 查找，以下说法不正确的有（ ）个。

① 采用链地址法时，查找任一元素的时间都相同  
② 链地址法若规定总在链首插入，则在不检查重复关键字的前提下，插入新元素的链表操作时间相同  
③ 链地址法易产生开放定址线性探测中的“主聚集”现象  
④ 线性探测不易产生堆积现象

A. 1  B. 2  C. 3  D. 4

### 205
现有长度为 17、初始为空的散列表 HT，散列函数 $H(key)=key\bmod17$，用线性探测解决冲突。依次插入 `26,25,72,38,8,18,59` 后，查找 59 需探查（ ）次。

A. 2  B. 3  C. 4  D. 5

### 206
现有长度为 17、初始为空的散列表 HT，散列函数 $H(key)=key\bmod17$，用平方探测解决冲突，探测序列按 $H(key),H(key)+1^2,H(key)-1^2,H(key)+2^2,H(key)-2^2,\ldots$（模 17）进行。依次插入 `6,22,7,26,9,23` 后，关键字 23 存放的位置是（ ）。

A. 0  B. 2  C. 6  D. 15

### 207
关于不同数据结构上的查找算法时间复杂度，描述正确的是（ ）。这里“线性表的查找”指**无序线性表上的顺序查找**。

A. 最坏情况下普通二叉树查找肯定比顺序查找快  
B. 最坏情况下普通 BST 查找肯定比顺序查找快  
C. 最坏情况下 AVL 树查找为 $\Theta(\log n)$，而顺序查找为 $\Theta(n)$  
D. 最坏情况下散列表查找肯定比普通 BST 快

### 208
对一个集合连续执行：先删除一个元素，再马上把刚删除的元素插入回去。操作前后集合的逻辑内容相同。若分别对二叉排序树、散列表和 B+ 树执行以上操作，其中操作前后物理结构必定完全相同的数据结构有（ ）种。

A. 0  B. 1  C. 2  D. 3

## 内部排序与堆

### 209
数据序列 `{8,10,13,4,6,7,22,2,3}` 只能是（ ）进行两趟排序后的结果。

A. 简单选择排序  B. 冒泡排序  C. 直接插入排序  D. 堆排序

### 210
在下列算法中，（ ）可能出现：在最后一趟开始之前，所有元素都不在最终位置上。

A. 堆排序  B. 冒泡排序  C. 直接插入排序  D. 快速排序

### 211
某种排序算法对线性表 `{25,84,21,47,15,27,68,35,20}` 排序时，元素序列变化如下：

1. `25,84,21,47,15,27,68,35,20`
2. `20,15,21,25,47,27,68,35,84`
3. `15,20,21,25,35,27,47,68,84`
4. `15,20,21,25,27,35,47,68,84`

所采用的排序算法是（ ）。

A. 选择排序  B. 插入排序  C. 二路归并排序  D. 快速排序

### 212
下列关于直接插入排序、折半插入排序和希尔排序的说法中，正确的有（ ）。

I. 直接插入排序平均时间复杂度为 $O(n^2)$  
II. 折半插入排序整体时间复杂度为 $O(n\log_2 n)$  
III. 希尔排序是不稳定排序  
IV. 折半插入排序每趟关键字比较次数的数量级较稳定，而直接插入排序每趟比较次数随输入状态变化

A. I、II、III、IV  B. I、III、IV  C. I、II、IV  D. II、III、IV

### 213
一组记录关键码为 `(46,79,56,38,40,84)`。采用教材常见的**首元素暂存为枢轴、左右指针交替向内扫描并用“填坑法”完成一次划分**，从小到大排序时，一次划分结果为（ ）。

A. `(38,40,46,56,79,84)`  
B. `(40,38,46,79,56,84)`  
C. `(40,38,46,56,79,84)`  
D. `(40,38,46,84,56,79)`

### 214
双向冒泡排序在正反两个方向交替扫描：第一趟把最大值放到最右端，第二趟把最小值放到最左端，之后继续缩小范围；**若某一趟没有发生交换，则立即结束排序**。对数组 `{4,7,8,3,5,6,10,9,1,2}` 排序，第一趟从左往右开始，则排序趟数为（ ）。

A. 7  B. 6  C. 8  D. 9

### 215
下列序列中，（ ）可能是执行第一趟快速排序后所得序列（分别允许按从小到大或从大到小排序讨论）。

I. `{68,11,18,69,23,93,73}`  
II. `{68,11,69,23,18,93,73}`  
III. `{93,73,68,11,69,23,18}`  
IV. `{68,11,69,23,18,73,93}`

A. I、IV  B. II、III  C. III、IV  D. 只有 IV

### 216
设 $n\ge4$，采用 1 基下标顺序存储一个含 $n$ 个**互异关键字**的小根堆；选项中的 `n/2` 均按整数除法，即 $\lfloor n/2\rfloor$ 解释。关键字最大的记录有可能存储在（ ）位置。

A. $n/2$  B. $n/2+2$  C. 1  D. $n/2-1$

### 217
由相同的 $n$ 个整数构成的二叉排序树和小根堆，下列说法中不正确的是（ ）。

A. 二叉排序树的高度大于或等于同样 $n$ 个结点的完全二叉堆高度  
B. 对二叉排序树中序遍历可得到从小到大序列  
C. 从小根堆根结点到任意叶结点的路径关键字非递减  
D. 对小根堆进行层序遍历可以得到从小到大的全序列

### 218
关键字序列 `{23,17,72,60,25,8,68,71,52}` 先按标准自底向上方法构造**小根堆**，随后连续两次删除堆顶并输出最小关键字；每次都用当前末元素补根并向下调整。两次输出后剩余堆是（ ）。

A. `{23,72,60,25,68,71,52}`  
B. `{23,25,52,60,71,72,68}`  
C. `{71,25,23,52,60,72,68}`  
D. `{23,25,68,52,60,72,71}`

### 219
堆排序第一阶段把序列构成初始堆。给定 `{48,62,35,77,55,14,35,98}`，若按标准自底向上 `sift-down` 构造成大根堆，并规定**每发生一次父结点与某个孩子的直接互换记作 1 次交换**，则交换元素的次数为（ ）。

A. 5  B. 6  C. 7  D. 8

### 220
已知大根堆 `{62,34,53,12,8,46,22}`。删除堆顶后，用末元素补到根并按标准 `sift-down` 调整：若当前结点有两个孩子，**先比较两个孩子选出较大者，再将其与待调元素比较**。整个调整过程中关键字比较次数为（ ）。

A. 2  B. 3  C. 4  D. 5

### 221
哪种数据结构从根结点到任意叶结点的路径关键字都是有序的（ ）。

A. 红黑树  B. 二叉查找树  C. Huffman 树  D. 堆

### 222
将两个各有 $N$ 个元素的升序表合并成一个升序表，最少比较次数是（ ），最多比较次数是（ ）。

A. $N,2N-1$  B. $N,2N$  C. $N-1,2N$  D. $N-1,2N-1$

### 223
若只想得到 1000 个元素组成的序列中前 5 个最小元素，不要求完成整个序列的全排序，则下列方法中最适合利用优先队列思想逐个取得这些极小元素的是（ ）。

A. 冒泡排序  B. 快速排序  C. 希尔排序  D. 堆排序

### 224
对 $n$ 个关键字进行二路归并排序，完成排序时记录移动次数的数量级为（ ）。

A. $\lfloor\log_2 n\rfloor$  B. $\lceil\log_2 n\rceil$  C. $O(\log_2 n)$  D. $O(n\log_2 n)$

### 225
用二路归并排序对序列 `{1,2,6,4,5,3,8,7}` 进行排序，共需要进行（ ）次关键字比较。

A. 12  B. 13  C. 14  D. 15

### 226
设线性表每个元素有两个数据项 $k_1,k_2$，要求先按 $k_1$ 升序；$k_1$ 相同时再按 $k_2$ 升序。下列两趟排序方案中满足要求的是（ ）。

A. 先按 $k_1$ 直接插入，再按 $k_2$ 简单选择  
B. 先按 $k_2$ 直接插入，再按 $k_1$ 简单选择  
C. 先按 $k_1$ 简单选择，再按 $k_2$ 直接插入  
D. 先按 $k_2$ 简单选择，再按 $k_1$ 直接插入

### 227
在数组上采用常规实现时，下列排序算法中，（ ）需要额外申请一个规模为 $\Theta(n)$ 的辅助数组，而其余算法不需要这样的线性辅助数组。

A. 快速排序  B. 堆排序  C. 归并排序  D. 插入排序

### 228
设输入序列已经按关键字**严格升序排列**。按题目采用的常见实现：直接插入排序逐个插入、归并排序仍完成完整归并层次、快速排序固定取第一个元素为枢轴。三者在该输入上的紧确渐近量级依次为（ ）。

A. $\Theta(n),\Theta(n),\Theta(n)$  
B. $\Theta(n),\Theta(n\log n),\Theta(n\log n)$  
C. $\Theta(n),\Theta(n\log n),\Theta(n^2)$  
D. $\Theta(n^2),\Theta(n\log n),\Theta(n^2)$

### 229
对 $n$ 个元素进行排序。约定冒泡排序采用“某趟无交换则提前结束”的优化，而直接插入排序和简单选择排序均按教材标准过程执行。其排序趟数仍**固定为 $n-1$ 趟**的算法组合是（ ）。

A. 直接插入排序和快速排序  
B. 冒泡排序和快速排序  
C. 简单选择排序和直接插入排序  
D. 简单选择排序和冒泡排序

### 230
下列排序算法均采用教材常见实现；其中冒泡排序使用“若某一趟无交换则提前结束”的优化。排序趟数与序列原始状态有关的是（ ）。

A. 插入排序  B. 选择排序  C. 冒泡排序  D. 基数排序

### 231
一台计算机具有多核 CPU。约定这里讨论的是**教材算法的外层阶段/递归子问题是否能彼此独立并行**，不讨论单趟内部的 SIMD、并行扫描或并行桶实现。在这种粒度下，下列排序算法中后续主要阶段不可以直接并行执行的有（ ）。

I. 基数排序  II. 快速排序  III. 冒泡排序  IV. 堆排序

A. I、III  B. I、II  C. I、III、IV  D. II、IV

## 外部排序

### 232
下列关于小顶堆和败者树的说法正确的是（ ）。

I. 败者树更新从叶对应的选手向根传播，每上一层只需与该层保存的败者比较一次  
II. 小顶堆下滤时每下一层通常需先比较两个孩子，再将当前元素与较小孩子比较  
III. 败者树内部结点直接存储完整的对应元素数值

A. I、III  B. II、III  C. I、II、III  D. I、II

### 233
下列关于小顶堆和败者树的说法正确的是（ ）。

I. 败者树一次更新沿被替换叶对应路径一直比较到根  
II. 堆维护一个结点位置时必定从根一直走到叶，不能中途停止  
III. 按本题教材的数组结点计数口径：处理 $k$ 个选手时，把 $k$ 个选手记录槽与约 $k$ 个败者树内部槽都计入结构空间，而小顶堆只计 $k$ 个记录槽，因此败者树所需结构空间约为小顶堆的 2 倍

A. I、III  B. II、III  C. I、II、III  D. I、II

### 234
下列关于置换-选择排序的说法正确的是（ ）。

I. 生成初始归并段时，需要不断从内存工作区中选择不小于旧 `MINIMAX` 的最小值，可用败者树维护候选  
II. 为处理新读入关键字小于当前输出值的情况，比较时应让“属于下一归并段”的记录优先胜出  
III. 置换-选择生成的初始归并段长度不受内存工作区容量作为硬上限限制；随机输入下平均长度约为工作区大小的两倍

A. I、III  B. II、III  C. I、II、III  D. I、II

### 235
若初始归并段大小分别为 `5,9,12,13,14,16,17,18,20,28,30,37,42`，采用四路归并，则最佳归并树的带权路径长度 WPL 是（ ）。

A. 460  B. 472  C. 480  D. 486

### 236
下列说法中正确的是（ ）。

I. 置换-选择排序的作用是生成初始归并段  
II. 最佳归并树用于设计多路归并的优化方案  
III. 外部排序是排序数据主要驻留外存、排序过程中分批调入内存处理的排序算法

A. I、II、III  B. I  C. I、II  D. II、III

### 237
输入数据流为 `(15,19,04,83,12,27,11,25,16,34,26,07,10,90,06)`，使用置换-选择排序生成初始归并段，内存工作区可容纳 4 个记录，则生成的初始归并段数量为（ ）。

A. 2  B. 3  C. 4  D. 5

### 238
在 $k$ 路归并排序中，使用败者树进行外部排序的多路平衡归并算法，以下正确的是（ ）。

A. 败者树不能处理重复关键字  
B. 每次输出后都需要重新构建整棵败者树  
C. 每次替换一个归并路的当前记录后，败者树调整时间为 $O(\log_2 k)$  
D. 败者树必须在内存中存储所有待归并数据，因此不适用于大数据处理
