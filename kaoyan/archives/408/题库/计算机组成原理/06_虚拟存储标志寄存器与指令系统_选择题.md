# 06｜虚拟存储、标志寄存器与指令系统｜选择题

> 来源题号：334–345  
> 已录入：12 题  
> 本文件只保留正式题面。

[查看答案与逐题解析](06_虚拟存储标志寄存器与指令系统_答案与解析.md)

## 跨科边界：分页管理与缺页处理（Canonical Owner：操作系统虚拟内存）

### 334

下列有关页式存储管理的叙述中，错误的是（ ）。

A. 进程地址空间被划分成等长的页，内存被划分成同样大小的页框  
B. 采用全相联映射，每页可以映射到任何一个空闲的页框中  
C. 当从磁盘装入的信息不足一页时会产生页内碎片  
D. 相对于段式存储管理，分页式更利于存储保护  

### 335

在经典请求分页教材模型中，页表项有效位为 0 表示该合法页面当前不驻留主存；若页面已有后备存储内容，系统能够从相关元数据取得其后备位置。下列有关缺页处理的叙述中，错误的是（ ）。

A. 若对应页表项中的有效位为 0，则发生缺页  
B. 缺页是一种外部中断，需要调用操作系统提供的中断服务程序来处理  
C. 若该缺页对应的是已有后备存储内容的普通页面，则缺页处理需要根据系统记录的后备位置把页面内容装入主存  
D. 缺页处理完后要重新执行发生缺页的指令  

## TLB 与硬件地址翻译

### 336

在分页地址翻译中，TLB（快表）缓存近期翻译项，主存中的页表保存权威映射信息。下面关于二者的叙述中正确的是（ ）。

A. 快表与慢表都存储在主存中，但快表比慢表容量小  
B. 快表采用了优化的搜索算法，因此查找速度快  
C. 快表比慢表的命中率高，因此快表可以得到更多的搜索结果  
D. 快表采用相联存储器件组成，按照查找内容访问，因此比慢表查找速度快  

## 标志寄存器与定长减法

### 337

在本题采用的经典教学模型中，标志寄存器是专用状态寄存器，不按通用寄存器编号，普通数据处理指令不能把它当作通用寄存器任意直接读写。下列有关标志寄存器的叙述中，错误的是（ ）。

A. 主要用于反映处理器的状态和 ALU 运算结果的某些特征  
B. 不需要对标志寄存器进行编号  
C. 可通过指令直接访问标志寄存器并修改其值  
D. 条件转移指令需要根据标志寄存器的值确定 PC 的值  

### 338

假设 $R[ax]=7\mathrm{FE6H}$，$R[bx]=\mathrm{FFE8H}$，执行指令 `sub bx, ax`（$R[bx]=R[bx]-R[ax]$）后，寄存器的内容和各标志的变化为（ ）。

A. $R[ax]=8002\mathrm{H}$，OF=0，SF=1，CF=0，ZF=0  
B. $R[bx]=8002\mathrm{H}$，OF=0，SF=1，CF=0，ZF=0  
C. $R[ax]=8002\mathrm{H}$，OF=1，SF=1，CF=0，ZF=0  
D. $R[bx]=8002\mathrm{H}$，OF=1，SF=1，CF=0，ZF=0  

## 扩展操作码与寻址

### 339

指令的长度为 16 位，每个地址码 5 位，现采用操作码扩展技术。若二地址指令为 60 条、零地址指令为 256 条，则一地址指令最多有（ ）。

A. 120 条  
B. 128 条  
C. 64 条  
D. 512 条  

### 340

设某机器的指令字长为 16 位，每个地址码段长为 4 位，用扩展操作码方法设计指令格式。若该指令系统中已定义了 12 条三地址指令、62 条二地址指令、30 条一地址指令，则该指令系统最多可以有（ ）条零地址指令。

A. 2  
B. 16  
C. 32  
D. 64  

### 341

按本题采用的抽象成本模型：当前指令已经取入 CPU；立即数已包含在指令中，不再需要额外读取；寄存器寻址还需读取寄存器堆；直接寻址和间接寻址还需访问数据存储器。则获取操作数最快的方式是（ ）。

A. 直接寻址  
B. 立即寻址  
C. 寄存器寻址  
D. 间接寻址  

### 342

设指令中的地址码为 $A$，变址寄存器内容为 $(X)$，程序计数器内容为 $(PC)$，并用 $M[a]$ 表示“读取内存地址 $a$ 中保存的地址值”。则变址间址寻址方式的操作数有效地址 EA 是（ ）。

A. $EA=M[(PC)+A]$  
B. $EA=M[(X)+A]$  
C. $EA=(X)+M[A]$  
D. $EA=(X)+A$  

### 343

假设相对寻址的转移指令占两个字节，第一个字节是操作码，第二个字节是相对偏移量（补码表示）。每当 CPU 从存储器取出一个字节，PC 自动加一。若当前 PC 值为 1234H，1234H 处指令为 `JMP *5`（`*` 为相对寻址特征），则执行完这条指令后 PC 的值为（ ）。

A. 1234H  
B. 1236H  
C. 1239H  
D. 123BH  

### 344

在本题采用的相对寻址模型中，CPU 取完当前指令后，PC 已指向下一条指令首地址，并以该值作为相对位移的基准。此时指令所提供的相对地址实质上是一种（ ）。

A. 立即数  
B. 内存地址  
C. 以本条指令在内存中首地址为基准位置的偏移量  
D. 以下条指令在内存中首地址为基准位置的偏移量  

### 345

某计算机指令为 16 位，由低到高 0 到 7 位是位移量 $D$，8 到 9 位为寻址特征位 $X$，10 到 15 位为操作码。当 $X=00$ 时直接寻址；当 $X=01$ 时使用 X1 进行变址寻址；当 $X=10$ 时使用 X2 进行变址寻址；当 $X=11$ 时使用 PC 相对寻址。设 $(PC)=1234\mathrm{H}$，$(X1)=0005\mathrm{H}$，$(X2)=1188\mathrm{H}$，则指令 2222H 的有效地址是（ ）。

A. 1256H  
B. 0027H  
C. 2222H  
D. 11AAH  
