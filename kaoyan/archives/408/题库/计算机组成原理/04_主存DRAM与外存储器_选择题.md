# 04｜主存、DRAM 与外存储器｜选择题

> 来源题号：284–306  
> 已录入：23 题  
> 本文件只保留正式题面。

[查看答案与逐题解析](04_主存DRAM与外存储器_答案与解析.md)

## 存储器基本组织与芯片扩展

### 284

设机器字长为 32 位，并规定半字为 16 位、$1\,\mathrm{MB}=2^{20}\,\mathrm{B}$。一个容量为 $16\,\mathrm{MB}$ 的存储器，CPU 按半字寻址，其可寻址的单元数是（ ）。

A. $2^{24}$  
B. $2^{23}$  
C. $2^{22}$  
D. $2^{21}$  

### 285

关于存储器的分类，以下选项中正确的是（ ）。

A. 为加快存取速度，Cache 和 TLB 都可以采用相联存储器制成  
B. RAM 是破坏性读取，ROM 是非破坏性读取  
C. SRAM 常用作 Cache，DRAM 常用作 TLB  
D. 计算机的存储总容量 = Cache 容量 + 主存容量 + 辅存容量  

### 286

在本题采用的典型 DRAM 教材模型中，DRAM 使用行/列地址复用。下列关于存储器的叙述正确的是（ ）。

I. 动态存储器利用电容电荷保存信息，需要周期性逐个刷新每一个存储单元  
II. FLASH 具有不易失特性，所以它是 ROM 且无法更新存储单元  
III. DRAM 是动态存储器，采用随机存取方式，行地址和列地址分时送入

A. I  
B. I、II  
C. II、III  
D. III  

### 287

以下对于半导体存储器的叙述错误的是（ ）。

A. Flash 存储器功耗低、集成度高，读写速度一样快  
B. SRAM 由双稳态电路保存 0/1，DRAM 则主要利用存储电容上的电荷保存 0/1  
C. 在关掉电源后，SRAM 和 DRAM 的数据均不能稳定保持  
D. 与 DRAM 相比，SRAM 容量小、速度快、价格高  

### 288

某一 DRAM 芯片，采用地址复用技术，容量为 $1024\times8$ 位，该芯片的地址引脚和数据引脚数至少是（ ）。

A. 18  
B. 13  
C. 8  
D. 17  

## DRAM 刷新与交叉存储

### 289

采用 $64K\times1$ 位的 DRAM 芯片构成 $256K\times8$ 位的存储器。规定每片 DRAM 的内部阵列按 $256\times256$ 组织，即每片共有 256 个需要逐行刷新的行地址。若采用异步刷新方式，每个存储单元的刷新间隔不超过 $2\,\mathrm{ms}$，则刷新信号的最大平均间隔是（ ）；若采用集中刷新方式，则完成一遍刷新最少占用（ ）个刷新周期。

A. $7.8\,\mu\mathrm{s}$，256  
B. $1.9\,\mu\mathrm{s}$，256  
C. $7.8\,\mu\mathrm{s}$，128  
D. $1.9\,\mu\mathrm{s}$，256  

### 290

对于某存储芯片，假定动态刷新间隔为 $2\,\mathrm{ms}$，读写周期和刷新周期均为 $0.2\,\mu\mathrm{s}$，该芯片中包含 128 行，每个刷新周期可以完成 1 行存储单元的刷新。如果该芯片采用异步刷新方式工作，那么其读写周期和刷新周期可以安排为（ ）。

A. 3999 次读写周期后，安排一次刷新操作  
B. 2000 次读写周期后，安排一次刷新操作  
C. 128 次读写周期后，安排一次刷新操作  
D. 64 次读写周期后，安排一次刷新操作  

### 291

某系列 DRAM 芯片采用行、列地址复用。若新一代芯片在其他组织不变的前提下增加 1 根复用地址引脚，使行地址和列地址都各增加 1 位，则芯片容量提高到原来的（ ）倍。

A. 2  
B. 4  
C. 8  
D. 16  

### 292

关于多模块存储器，以下选项中正确的是（ ）。

A. 高位交叉存储器有效地利用了程序的局部性原理  
B. 多体并行存储器里拥有多个模块，每个模块都有自己的读写控制电路  
C. 高位交叉存储器是连续编址的多模块存储器，无法提高存取效率  
D. 低位 $n$ 体交叉存储器的存取效率一定是单体存储器的 $n$ 倍  

### 293

某低位交叉存储器有 4 个模块，存储周期为 $200\,\mathrm{ns}$，存储字长为 16 位，按字编址，其每个体的容量是 $64K\times16$ 位。若总线传输周期为 $50\,\mathrm{ns}$，该存储器读取的平均速度为（ ）。

A. $2\times10^8\,\mathrm{bit/s}$  
B. $3.2\times10^8\,\mathrm{bit/s}$  
C. $2\times10^7\,\mathrm{bit/s}$  
D. $3.2\times10^7\,\mathrm{bit/s}$  

### 294

某机器采用四体低位交叉存储器。单体存储周期为 $4\tau$，控制器对连续地址最多每隔 $\tau$ 启动一次访问。每次操作都从同一连续地址序列的首地址重新开始；按重复执行的稳态启动间隔计，不计两种操作相同量级的首尾固定延迟。分别执行：①读取 6 个连续地址单元中的存储字，重复 80 次；②读取 8 个连续地址单元中的存储字，重复 60 次。则①、②所花费的时间之比为（ ）。

A. $1:1$  
B. $2:1$  
C. $4:3$  
D. $3:4$  

### 295

假定用若干 $16K\times8$ 位的存储芯片组成一个 $64K\times8$ 位的存储器，采用 4 片低位交叉编址，最低 2 位用于选择芯片，则地址 BFFFH 所在芯片可对应的最小全局地址为（ ）。

A. 0000H  
B. 0001H  
C. 0002H  
D. 0003H  

### 296

采用四体并行低位交叉存储器，即存储体数量 $m=4$。设每个体一次访问提供 16 位数据，存取周期 $T=400\,\mathrm{ns}$；四个体可以并行工作，且不考虑总线带宽瓶颈。下述说法中（ ）正确。

A. 在 $0.1\,\mu\mathrm{s}$ 内，存储器可向 CPU 提供 $2^6$ 位二进制信息  
B. 在 $0.1\,\mu\mathrm{s}$ 内，每个体可向 CPU 提供 $2^6$ 位二进制信息  
C. 在 $0.4\,\mu\mathrm{s}$ 内，存储器可向 CPU 提供 $2^6$ 位二进制信息  
D. 在 $0.4\,\mu\mathrm{s}$ 内，每个体可向 CPU 提供 $2^6$ 位二进制信息  

### 297

假定一个存储器系统支持四体低位交叉存取，连续请求按每个节拍 1 个的速率进入请求队列，每个存储体从开始服务一个请求起连续忙 4 个节拍。前面的体冲突只造成该体内部排队，不改变后续请求进入队列的原始节拍。若某请求到达时目标存储体仍有更早请求尚未完成，则记 1 次体冲突。某程序的地址序列为 9、17、2、51、37、13、4、8、41、67、10，则共发生（ ）次体冲突。

A. 2  
B. 3  
C. 4  
D. 5  

### 298

某容量为 $128\,\mathrm{KB}$ 的存储器由若干 $8K\times8$ 位的芯片扩展而成，若其存储字长为 16 位且按字节编址，则地址 BC2FH 所在芯片的首地址为（ ）。

A. 8000H  
B. B000H  
C. A000H  
D. F000H  

## 磁盘与 SSD

### 299

下列关于磁盘驱动器的叙述中，错误的是（ ）。

A. 送到磁盘驱动器的地址由磁头号、盘面号和扇区号组成  
B. 能控制磁头移动到指定磁道，并发回“寻道结束”信号  
C. 能控制磁盘片转过指定的扇区，并发回“扇区符合”信号  
D. 能控制对指定盘面的指定扇区进行数据的读或写操作  

### 300

磁盘转速提高一倍，则（ ）。

A. 寻道时间缩小一半  
B. 存取速度也提高一倍  
C. 平均旋转延迟缩小一倍  
D. 不影响磁盘传输速率  

### 301

已知硬盘的传输速率为 $10\,\mathrm{MB/s}$，磁盘转速为 $9600\,\mathrm{RPM}$，磁盘控制器延迟 $1.5\,\mathrm{ms}$，平均寻道时间为 $10.5\,\mathrm{ms}$。本题按 $1\,\mathrm{KB}=10^3\,\mathrm{B}$、$1\,\mathrm{MB}=10^6\,\mathrm{B}$ 计算，则该硬盘写 $2\,\mathrm{KB}$ 数据的时间为（ ）。

A. $13.825\,\mathrm{ms}$  
B. $15.125\,\mathrm{ms}$  
C. $15.325\,\mathrm{ms}$  
D. $18.45\,\mathrm{ms}$  

### 302

关于 SSD 的特点，以下说法不正确的是（ ）。

A. 读写性能明显高于磁盘  
B. 读写速度较快，常用作主存  
C. 写入次数有限，需要考虑磨损  
D. 基于闪存的存储技术  

### 303

按 NAND Flash 介质内部的典型操作粒度讨论，下列关于固态硬盘（SSD）的叙述中，不正确的是（ ）。

A. Flash 阵列的数据读出/编程以页为基本粒度  
B. Flash 阵列的擦除以页为基本粒度  
C. NAND Flash 的编程以及后续擦除/垃圾回收路径通常比单纯读取更慢，因此写入延迟一般高于读取  
D. 固态硬盘的写入次数有限，引入磨损均衡可以延长使用寿命  

### 304

一个磁盘的转速为 7200 转/分，每个磁道有 160 个扇区，每个扇区有 512 字节。按 $1\,\mathrm{KB}=1024\,\mathrm{B}$ 计算，则在理想连续传输情况下，磁盘每秒传输的数据量是（ ）。

A. $7200\times160\,\mathrm{KB/s}$  
B. $7200\,\mathrm{KB/s}$  
C. $9600\,\mathrm{KB/s}$  
D. $19200\,\mathrm{KB/s}$  

### 305

磁盘盘面共 200 个磁道，盘面总存储容量为 $60\,\mathrm{MB}$，磁盘旋转一周时间为 $25\,\mathrm{ms}$，每个磁道有 8 个扇区，各扇区之间有一间隙，磁头通过每个间隙需 $1.25\,\mathrm{ms}$。磁盘接口所需最大传输速率是（ ）。

A. $10\,\mathrm{MB/s}$  
B. $60\,\mathrm{MB/s}$  
C. $83.3\,\mathrm{MB/s}$  
D. $20\,\mathrm{MB/s}$  

### 306

在高速缓存系统中，主存容量为 $12\,\mathrm{MB}$，Cache 容量为 $400\,\mathrm{KB}$，该存储系统的容量应为（ ）。

A. $12\,\mathrm{MB}+400\,\mathrm{KB}$  
B. $12\,\mathrm{MB}$  
C. $12\,\mathrm{MB}-12\,\mathrm{MB}+400\,\mathrm{KB}$  
D. $12\,\mathrm{MB}-400\,\mathrm{KB}$  
