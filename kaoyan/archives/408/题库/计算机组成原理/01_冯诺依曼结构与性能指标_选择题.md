# 01｜冯·诺依曼结构与性能指标｜选择题

> 来源题号：239–245  
> 已录入：7 题  
> 本文件只保留正式题面。

[查看答案与逐题解析](01_冯诺依曼结构与性能指标_答案与解析.md)

## 冯·诺依曼基本特征

### 239

下列（ ）是冯·诺依曼机工作方式的基本特点。

A. 多指令流单数据流  
B. 按地址访问并顺序执行指令  
C. 堆栈操作  
D. 存储器按内容选择地址  

## CPU 性能指标

### 240

关于 CPU 主频、CPI、MIPS、MFLOPS，说法正确的是（ ）。

A. CPU 主频是指 CPU 系统执行指令的频率，CPI 是执行一条指令平均使用的频率  
B. CPI 是执行一条指令平均使用 CPU 时钟的个数，MIPS 描述一条 CPU 指令平均使用的 CPU 时钟数  
C. MIPS 是描述 CPU 执行指令的频率，MFLOPS 是计算机系统的浮点数指令数  
D. CPU 主频指 CPU 使用的时钟脉冲频率，CPI 是执行一条指令平均使用的 CPU 时钟数  

### 241

在程序、指令集和具体处理器组织固定，并假定只改变时钟信号频率而不改变各类指令所需周期数的教材抽象下，CPU 的 CPI 与下列（ ）因素无关。

A. 时钟频率  
B. 系统结构  
C. 指令集  
D. 计算机组织  

### 242

机器 A 的主频为 $800\,\mathrm{MHz}$，某程序在 A 上运行需要 $12\,\mathrm{s}$。现在硬件设计人员想设计机器 B，希望该程序在 B 上的运行时间能缩短为 $8\,\mathrm{s}$。使用新技术后可使 B 的主频大幅提高，但在 B 上运行该程序所需的时钟周期数为在 A 上的 $1.5$ 倍，则机器 B 的主频至少应为（ ）。

A. $800\,\mathrm{MHz}$  
B. $1.2\,\mathrm{GHz}$  
C. $1.5\,\mathrm{GHz}$  
D. $1.8\,\mathrm{GHz}$  

### 243

假定机器 $M$ 的时钟频率为 $200\,\mathrm{MHz}$，程序 $P$ 在机器 $M$ 上的执行时间为 $12\,\mathrm{s}$。对 $P$ 优化时将其所有乘 $4$ 指令都换成了左移两位的指令，得到优化后的程序 $P'$。若在 $M$ 上乘法指令的 CPI 为 $102$，左移指令的 CPI 为 $2$，$P$ 的执行时间是 $P'$ 执行时间的 $1.2$ 倍，则 $P$ 中的乘法指令条数为（ ）。

A. $200$ 万  
B. $400$ 万  
C. $800$ 万  
D. $1600$ 万  

### 244

假定编译器对高级语言的某条语句可以编译生成两种不同的指令序列。A、B 和 C 三类指令的 CPI 和两种不同序列所含的三类指令条数如下表所示。两个指令序列都在时钟周期为 $2\,\mathrm{ns}$ 的机器上运行，则下列结论中正确的是（ ）。

| 指令类型 | CPI | 序列一的指令条数 | 序列二的指令条数 |
| --- | ---: | ---: | ---: |
| A | 1 | 1 | 2 |
| B | 2 | 1 | 1 |
| C | 3 | 4 | 2 |

A. 序列一的 MIPS 数比序列二多 50，序列一的执行时间比序列二少 $10\,\mathrm{ns}$  
B. 序列一的 MIPS 数比序列二多 50，序列二的执行时间比序列一少 $10\,\mathrm{ns}$  
C. 序列二的 MIPS 数比序列一多 50，序列二的执行时间比序列一少 $10\,\mathrm{ns}$  
D. 序列二的 MIPS 数比序列一多 50，序列一的执行时间比序列二少 $10\,\mathrm{ns}$  

### 245

假定执行指令 A 需要 $20$ 个时钟周期，该指令在程序中的出现频度为 $20\%$，其他所有指令的平均 CPI 为 $5$，则 CPU 执行指令 A 所用时间占整个 CPU 时间的百分比是多少？（ ）

A. $20\%$  
B. $50\%$  
C. $80\%$  
D. $25\%$  
