# 05｜Cache、局部性与存储层次｜选择题

> 来源题号：307–333  
> 已录入：27 题  
> 本文件只保留正式题面。

[查看答案与逐题解析](05_Cache局部性与存储层次_答案与解析.md)

## Cache 写策略、命中率与局部性

### 307

对于下面两种情况需要采用适合的 Cache 写策略：①主要运行访存密集型应用，其中包含较多写操作，希望减少向下一级存储器传播的写流量；②要求每次写命中后，主存中的对应数据立即与 Cache 中保持一致。适合它们的写策略分别是（ ）。

A. 回写法、全写法  
B. 全写法、回写法  
C. 回写法、回写法  
D. 全写法、全写法  

### 308

关于 Cache 的全写法（写直达法）和写回法这两种写策略，以下说法正确的是（ ）。

I. 采用写回法时，只有在第一次写入 Cache 时需要写入主存  
II. 采用全写法时，写命中时则同时写 Cache 和辅存  
III. 采用写回法时，每块 Cache 都需要多加一位辅助位

A. 仅 I  
B. 仅 II  
C. 仅 III  
D. I、II 和 III  

### 309

假设一个计算机系统中的 Cache 采用写回策略，主存中某个字的初始值为 0，且它所对应的 Cache 块已经被加载到 Cache 中，并且该 Cache 块中有一些字被修改过。如果该字被 CPU 写入了新值 1，那么该字在主存中的值，在 Cache 块替换之前是（ ）。

A. 0  
B. 1  
C. 取决于 Cache 的替换策略  
D. 取决于 Cache 之前的修改值  

### 310

一台计算机有两级 Cache，CPU 的原始访存请求依次访问 L1、必要时再访问 L2。某程序共有 1000 次 CPU 原始访存，其中 L1 有 40 次不命中，这 40 次继续访问 L2 后仍有 10 次不命中。则相对于这 1000 次 CPU 原始访存，两级 Cache 层次的总命中率是（ ）。

A. $99\%$  
B. $90\%$  
C. $96\%$  
D. $75\%$  

### 311

局部性通常有两种不同的形式：时间局部性和空间局部性。程序员是否编写出高速缓存友好的代码，就取决于这两方面的问题。对于下面这个函数，说法正确的是（ ）。

```c
int sum_vec(int v[N]) {
    int i, sum = 0;
    for (i = 0; i < N; i++)
        sum += v[i];
    return sum;
}
```

A. 对于变量 `i` 和 `sum`，循环体具有良好的空间局部性  
B. 对于变量 `i`、`sum` 和数组 `v`，循环体具有良好的空间局部性  
C. 对于变量 `i` 和 `sum`，循环体具有良好的时间局部性  
D. 对于变量 `i`、`sum` 和数组 `v`，循环体具有良好的时间局部性  

### 312

对于下列代码，以下哪种变化将使其具有更好的空间局部性（ ）。

```c
int i, j, k, sum = 0;
for (i = 0; i < n; i++)
    for (j = 0; j < n; j++)
        for (k = 0; k < n; k++)
            sum += a[k][j][i];
```

A. 将第 2 行与第 3 行互换  
B. 将第 2 行与第 4 行互换  
C. 将第 5 行改为 `sum += a[i][k][j];`  
D. 将第 5 行改为 `sum += a[j][i][k];`  

### 313

在一个采用直接映射的 Cache 中，Cache 数据容量为 8KB，主存地址空间为 32 位，主存块大小为 32B，按字节编址。某 `double` 数组共有 1024 个元素，每个元素 8B，数组首地址按 32B 块边界对齐；Cache 初始为空，程序仅按下标从 0 到 1023 顺序读取每个元素一次，且没有其他访存干扰。该过程中，Cache 的命中率是（ ）。

A. $0\%$  
B. $25\%$  
C. $50\%$  
D. $75\%$  

### 314

在如下 C 语言程序段中，数组 A、B、C、D 均为 `int` 型，每个元素 4B。数据 Cache 采用 4 路组相联映射，数据容量 2KB，共 8 组，因此每块为 64B。规定四个数组的首地址均按 64B 块边界对齐；除这四个数组外没有其他数据 Cache 访问；对应下标附近的 A/B/C/D 当前块即使映射到同一组，也可分别占据 4 个 way 而不在复用前互相驱逐。按未优化的访存模型，每次源码中对 `A[i]` 的一次读或写都形成一次独立的数据 Cache reference。程序执行前 Cache 为空，则执行过程中访问数组 A 的 Cache 缺失率约为（ ）。

```c
for (int i = 0; i < 1000; i++) {
    A[i] = A[i] + B[i];
    C[i] = A[i] + D[i];
}
```

A. $1.25\%$  
B. $2.1\%$  
C. $12.5\%$  
D. $25\%$  

### 315

在经典透明硬件 Cache 模型中，下列描述正确的是（ ）。

A. Cache 的核心工作机制全部由硬件实现  
B. Cache 替换时的单位为字  
C. Cache 与主存统一编址，即主存地址空间的某一部分属于 Cache  
D. 无论何时，Cache 中的信息一定与主存中的信息一致  

### 316

保持 Cache 块大小、总行数和地址宽度不变，将 Cache 相联度提高一倍。下列变化中一定成立的是（ ）。

I. Cache 命中访问时间减少  
II. Cache 块偏移量需要的位数增加  
III. 组索引需要的位数减少 1

A. I  
B. III  
C. I、II  
D. II、III  

## 跨科边界：页面置换（Canonical Owner：操作系统虚拟内存）

### 317

某虚拟存储器系统采用页式内存管理，使用 LRU 页面替换算法，考虑下面的页面访问地址流（每次访问在一个时间单位中完成）：

`1 8 1 7 8 2 7 2 1 8 3 8 2 1 3 1 7 1 3 7`

假定内存容量为 4 个页面，开始时是空的，则页面失效率是（ ）。

A. $30\%$  
B. $5\%$  
C. $1.5\%$  
D. $15\%$  

## Cache 映射、替换与地址字段

### 318

假设一个 Cache 有 4 路组相联，每个 Cache 行的大小为 32 字节。主存地址空间为 32 位，按字节编址，Cache 的大小为 64KB。初始状态下 Cache 是空的，现在考虑使用 LRU 算法进行 Cache 块替换。对于以下主存地址访问序列：

`0x00010000, 0x00020000, 0x00010004, 0x00030000, 0x00010008, 0x00040000, 0x00020004, 0x0005000c`

在最后一次访问 `0x0005000c` 时若发生替换，应淘汰哪个主存块？完成全部访问后，Cache 中保留哪些主存块？

A. 淘汰 `0x00010000` 所在主存块；最后为 `0x00030000`、`0x00040000`、`0x00020004`、`0x0005000c` 所在的 4 个主存块  
B. 淘汰 `0x00020000` 所在主存块；最后为 `0x00030000`、`0x00010008`、`0x00040000`、`0x0005000c` 所在的 4 个主存块  
C. 淘汰 `0x00030000` 所在主存块；最后为 `0x00010008`、`0x00040000`、`0x00020004`、`0x0005000c` 所在的 4 个主存块  
D. 淘汰 `0x00040000` 所在主存块；最后为 `0x00030000`、`0x00010008`、`0x00020004`、`0x0005000c` 所在的 4 个主存块  

### 319

假设有一个块数为 4 的 Cache，采取全相联映射。起初 Cache 中内容为空，接下来 CPU 连续访问了以下主存块（均为十进制）：1、2、3、4、5、4、2、3、5、1。当 Cache 采取 LRU 替换策略时，命中率为（ ）。

A. $20\%$  
B. $30\%$  
C. $40\%$  
D. $50\%$  

### 320

假设一个 Cache 中共有 $M$ 块，每 $K$ 块组成一组，则下列描述中正确的是（ ）。

A. 若 $K=1$，则该 Cache 是直接映射 Cache  
B. 若 $K=1$，则该 Cache 是全相联映射 Cache  
C. 若 $K=M$，则该 Cache 是直接映射 Cache  
D. 若 $K>1$ 且 $K<M$，则该 Cache 是 $M/K$ 路组相联映射 Cache  

### 321

某存储系统中，主存容量是 Cache 数据容量的 4096 倍，Cache 被分为 64 行并采用直接映射。若只统计每一行中用于区分“当前副本来自哪个主存块”的 Tag 字段，不计 valid、dirty、一致性和其他元数据，则全部 Tag 字段共需（ ）。

A. $6\times4097\,\mathrm{bit}$  
B. $64\times12\,\mathrm{bit}$  
C. $6\times4096\,\mathrm{bit}$  
D. $64\times13\,\mathrm{bit}$  

### 322

关于 Cache 与主存的地址映射，说法正确的是（ ）。

A. Cache 与主存之间的地址映射是由硬件中的地址转换逻辑电路完成的，属于硬件自动完成  
B. 在地址映射的过程中如果出现 Cache 满且未命中，需要程序员调度完成  
C. 由于不同计算机的硬件系统不一致，地址映射是由操作系统完成的  
D. 为解决复杂情况的地址映射，Cache 与主存之间的地址映射由硬件和操作系统共同协调完成  

### 323

某计算机主存地址为 32 位，按字节编址，采用两级数据 Cache。L1 与 L2 的组织完全相同：主存块大小 64B，每级均有 64 行，采用直接映射和写回法。只计数据、Tag、valid 与 dirty 位，不计 ECC 等其他元数据，则两个 Cache 的物理总容量至少为（ ）。

A. 8512B  
B. 8544B  
C. 8512bit  
D. 8544bit  

### 324

有效容量为 128KB 的 Cache，每块 16B，采用 8 路组相联。字节地址为 1234567H 的单元调入该 Cache，则其 Tag 应为（ ）。

A. 1234H  
B. 2468H  
C. 048DH  
D. 12345H  

### 325

若主存按字节编址，主存块大小为 32B，Cache 共有 64 行，主存和 Cache 间采用直接映射方式，编号都从 0 开始，则主存第 2123 号单元（十进制）对应的 Cache 行号是（ ）。

A. 0  
B. 1  
C. 2  
D. 3  

### 326

有一主存–Cache 层次的存储器，其主存容量为 1MB，Cache 容量为 16KB，每块有 8 个字，每字 32 位，采用直接地址映射方式，Cache 起始字块为第 0 块。若主存地址为 35301H，且 CPU 访问 Cache 命中，则在 Cache 的第（ ）（十进制表示）字块中。

A. 152  
B. 153  
C. 154  
D. 151  

### 327

某计算机 Cache 容量为 1KB，采用 4 路组相联映射方式，主存容量为 1MB，每个主存块大小为 32 字节，按字节编址。若 CPU 访问主存地址 819A7H 单元且 Cache 命中，则该单元位于 Cache 组号是（ ）。

A. 2  
B. 5  
C. 10  
D. 13  

### 328

对于 $n$ 路组相联映射 Cache，在保持 $n$ 及主存和 Cache 总容量不变的前提下，将主存块大小和 Cache 块大小都增加一倍，则下列描述中正确的是（ ）。

A. 字块内地址的位数增加 1 位，主存 tag 字段的位数增加 1 位  
B. 字块内地址的位数增加 1 位，主存 tag 字段的位数不变  
C. 字块内地址的位数减少 1 位，主存 tag 字段的位数增加 1 位  
D. 字块内地址的位数增加 1 位，主存 tag 字段的位数减少一半  

### 329

某计算机的 Cache 有 16 行，块大小为 16B，其映射方式可配置为直接映射或 2 路组相联映射，主存按字节编址，主存单元从 0 开始编号。下列哪一对主存单元所属的两个不同主存块，在直接映射时落入同一 Cache 行，并且在 2 路组相联时也落入同一 Cache 组（ ）。

A. 52 号和 102 号单元  
B. 48 号和 308 号单元  
C. 60 号和 160 号单元  
D. 46 号和 236 号单元  

### 330

假设主存地址位数为 32 位，按字节编址，主存和 Cache 之间采用全相联映射方式，每存块大小为 1 个字，每字 32 位，采用回写（write back）方式和随机替换策略，则能存放 32K 字数据的 Cache 的总容量至少应有（ ）位。

A. 1536K  
B. 1568K  
C. 2016K  
D. 2048K  

## Cache 缺失代价与虚拟存储联动

### 331

假定 8 个存储器模块采用交叉方式组织，存储器芯片和总线支持突发传送，CPU 通过存储器总线读取数据的过程为：发送首地址和读命令需 1 个时钟周期，存储器准备第 1 个数据需 8 个时钟周期，随后每个时钟周期总线上传送 1 个数据，可连续传送 8 个数据（即突发长度为 8）。若主存和 Cache 之间交换的主存块大小为 64B，存取宽度和总线宽度都为 8B，则 Cache 的一次缺失损失至少为（ ）个时钟周期。

A. 17  
B. 20  
C. 33  
D. 80  

### 332

假定 CPU 通过存储器总线读取数据的过程为：发送地址和读命令需 1 个时钟周期，存储器准备一个 8B 数据需 8 个时钟周期，总线上传送该数据需 1 个时钟周期。系统不支持突发或流水重叠，下一笔 8B 访问必须等前一笔传送完成后才能发送新的地址/命令。若主存和 Cache 之间交换的存储块大小为 64B，存取宽度和总线宽度都为 8B，则 Cache 的一次缺失损失至少为（ ）个时钟周期。

A. 64  
B. 72  
C. 80  
D. 160  

### 333

在经典分页 + 物理地址 Cache 的教学模型中，下列说法中错误的有（ ）。

I. 若地址翻译发现目标页当前不驻留并触发缺页，则本次普通 Cache 数据访问路径不会完成；缺页修复后需重试访问  
II. Cache miss 的损失通常大于 Page Fault（缺页）的损失  
III. Cache miss 和 TLB miss 后的全部处理都一定由硬件完成  
IV. 虚拟存储器中能够被系统实际承载的数据容量可以无条件大于主存与后备存储可用容量之和

A. I 和 II  
B. II 和 III  
C. I、III 和 IV  
D. II、III 和 IV  
