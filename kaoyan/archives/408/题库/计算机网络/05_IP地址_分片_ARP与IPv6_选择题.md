# 05｜IP 地址、分片、ARP 与 IPv6｜选择题

> 来源题号：827–873

[查看答案与逐题解析](05_IP地址_分片_ARP与IPv6_答案与解析.md)

## 异构互联、数据报与虚电路

### 827

路由器连接的异构网络是指（ ）。

A. 网络的拓扑结构不同  
B. 网络中计算机操作系统不同  
C. 数据链路层和物理层均可以不同  
D. 数据链路层协议相同，仅物理层协议不同  

### 828

网络层的一个重要任务是实现异构网络互联。以下关于异构网络的说法，正确的是（ ）。

I. 网络可以具有不同的拓扑结构  
II. 网络之间的数据链路层协议和物理层协议都可能不同  
III. 路由器连接的两个网络分别采用以太网和广域网链路技术时，可以视为异构网络

A. 仅 I、II  
B. 仅 I、III  
C. 仅 II、III  
D. I、II、III  

### 829

在路由器互连的多个局域网结构中，要求每个局域网（ ）。

A. 物理层协议可以不同，而数据链路层及其以上高层协议必须相同  
B. 物理层、数据链路层协议可以不同，而数据链路层以上的高层协议必须具有可互通的网络层协议体系  
C. 物理层、数据链路层、网络层协议可以不同，而网络层以上高层协议必须相同  
D. 物理层、数据链路层、网络层及高层协议都可以任意不同  

### 830

在互联网中，一个路由器的路由表通常包含（ ）。

A. 目的网络和到达目的网络的完整路径  
B. 所有目的主机和到达该目的主机的完整路径  
C. 目的网络以及到达该目的网络的下一跳路由器 IP 地址/出接口信息  
D. 目的网络以及下一跳路由器的 MAC 地址作为完整路由信息  

### 831

在下列数据交换方式中，数据经过网络的传输延迟长而且不固定，不适合实时语音数据传输的是（ ）。

A. 电路交换  
B. 报文交换  
C. 数据报交换  
D. 虚电路交换  

### 832

按本题采用的传统教材虚电路模型，下列关于各种数据交换方式的叙述，错误的是（ ）。

A. 电路交换本身不提供逐块差错控制功能  
B. 分组交换的分组有最大长度限制  
C. 虚电路是面向连接的，在该教材模型下提供有序、可靠的网络层服务  
D. 在结点/链路故障率很高的传输系统中，选择虚电路方式更合适  

### 833

下列有关数据报和虚电路的叙述中，错误的是（ ）。

A. 数据报方式中，某个结点若因故障丢失一个分组，其他分组仍可能正常传输  
B. 数据报方式中，每个分组独立进行路由选择和转发，不同分组之间没有必然的路径联系  
C. 虚电路方式中，属于同一条虚电路的分组按照同一路由转发  
D. 虚电路方式虽然面向连接，但不能保证同一虚电路分组按原顺序到达  

### 834

下列关于虚电路和数据报的叙述中，正确的是（ ）。

A. 虚电路是一种分组交换技术，但不能按照存储转发方式工作  
B. 所有虚电路都只能是临时连接，会话结束时必然释放  
C. 数据报服务不提供可靠传输，但可以保证分组有序到达  
D. 数据报服务中，每个分组都必须携带完整的源地址和目的地址  

### 835

下列关于虚电路的说法中，正确的是（ ）。

A. 中间结点发生故障后，可沿另一条路径继续同一虚电路，无需重新建立连接  
B. 虚电路数据传输阶段的每个分组首部都必须包含完整目的地址  
C. 虚电路结合了电路交换“先建立逻辑路径”的思想，适合对实时性要求较高的长期通信  
D. 多站点同时使用同一段物理链路承载虚电路时必然发生冲突，无法正常通信  

### 836

下列关于虚电路的说法中，正确的是（ ）。

A. 虚电路和电路交换一样，在数据传输前要建立专用物理连接  
B. 虚电路中间结点发生故障后，可自动沿其他路径继续通信，无须重新建立连接  
C. 建立虚电路的控制分组需要携带源/目的地址，建立后普通数据分组可使用虚电路标识转发  
D. 在同一条虚电路上传送的同一会话数据分组可以独立选择不同路径  

### 837

下列关于 SDN 的描述中，错误的是（ ）。

I. SDN 是一种新型物理网络拓扑结构  
II. OpenFlow 交换机可基于流表转发分组  
III. SDN 远程控制器必须位于 OpenFlow 交换机内部  
IV. OpenFlow 可作为 SDN 控制平面与数据平面之间的南向通信接口

A. I 和 III  
B. I 和 IV  
C. II 和 IV  
D. I、III 和 IV  

### 838

下列关于 SDN 的说法中，错误的是（ ）。

A. 在典型 SDN 架构中，数据平面设备不再独立运行传统分布式路由选择逻辑来决定全局路线  
B. SDN 的核心思想之一是控制平面与数据平面分离  
C. OpenFlow 是常见的南向接口协议  
D. SDN 以控制器为核心，通过北向接口直接与数据平面设备通信  

## IPv4 首部、分片与转发

### 839

IPv4 分组首部中有三个有关长度/位置的字段：首部长度字段、片偏移字段和总长度字段，它们的计数单位分别为（ ）。

A. 4 B、1 B、8 B  
B. 1 B、8 B、4 B  
C. 4 B、8 B、1 B  
D. 8 B、4 B、1 B  

### 840

IPv4 分组首部中有两个有关长度的字段：首部长度和总长度，其中（ ）。

A. 二者都以 8 bit 为计数单位  
B. 首部长度以 8 bit 为计数单位，总长度以 32 bit 为计数单位  
C. 首部长度以 32 bit 为计数单位，总长度以 8 bit 为计数单位  
D. 二者都以 32 bit 为计数单位  

### 841

下列关于 IPv4 分组首部校验和字段的说法中，正确的是（ ）。

A. 校验和检查整个 IP 分组  
B. 计算校验和时，对首部按 16 bit 字分组进行反码加法求和，再取反码  
C. 若路由器发现首部校验和错误，则丢弃该分组并一定向源主机发送 ICMP 差错报文  
D. IP 首部校验和的计算需要加入伪首部  

### 842

若 IPv4 数据报在传输中被分片，则这些分片最终在（ ）重组。

A. 中间路由器  
B. 下一跳路由器  
C. 核心路由器  
D. 目的主机  

### 843

局域网 A 和局域网 B 通过路由器连接。局域网 A 中某主机有一个 400 字节长的 TCP 报文段传到 IP 层，加上 20 字节 IP 首部后成为数据报并发往局域网 B。局域网 B 的数据链路层帧最多只能承载 150 字节的 IP 数据报，因此路由器必须分片。目的主机完成 IP 重组后，网络层向传输层交付的数据长度为（ ）。

A. 400 B  
B. 460 B  
C. 480 B  
D. 以上都不对  

### 844

IPv4 分组经过路由器转发时，如果不发生分片和 NAT 等地址改写，则（ ）。

A. TTL 字段和首部校验和字段都会改变  
B. TTL 字段和源/目的 IP 地址都会改变  
C. 源/目的 IP 地址和首部校验和字段都会改变  
D. DF 和 MF 字段都会改变  

### 845

某 IPv4 分组的片偏移字段值为 100，首部长度字段值为 5，总长度字段值为 100。按原数据部分从 0 开始编号，则该分组数据部分的第一个字节编号与最后一个字节编号分别是（ ）。

A. 100，200  
B. 100，500  
C. 800，879  
D. 800，900  

### 846

一个长度为 3000 字节的 TCP 报文段需要经 IPv4 从源主机发送到目的主机，路径 MTU 为 1400 字节，假设 IPv4 固定首部为 20 字节且传输过程中不丢片。下列说法正确的是（ ）。

A. 分片后的 IP 数据报总长度必为 1400、1400 和 260 字节  
B. 最后一个分片的 MF=0，且 DF=1  
C. 同一原始数据报的各分片 Identification 字段应依次递增  
D. 目的主机 IP 层依据共同的 Identification、片偏移和 MF 等字段重组各分片的数据部分，恢复原完整 TCP 报文段  

### 847

路由器 R0 的转发表如下。若进入 R0 的分组目的地址为 `132.19.237.5`，则该分组应转发到（ ）下一跳路由器。

| 目的网络 | 下一跳 |
| --- | --- |
| 132.0.0.0/8 | R1 |
| 132.0.0.0/11 | R2 |
| 132.19.232.0/22 | R3 |
| 0.0.0.0/0 | R4 |

A. R1  
B. R2  
C. R3  
D. R4  

## IPv4 地址、CIDR 与子网

### 848

访问互联网的主机需要分配可用 IP 地址（本题按传统分类编址的默认子网掩码判断），下列可以分配给普通主机的 IP 地址是（ ）。

A. 192.46.10.0  
B. 110.47.10.0  
C. 127.10.10.17  
D. 211.60.256.21  

### 849

为了提供更多子网，为一个 B 类地址指定子网掩码 `255.255.240.0`，则按传统“网络地址与广播地址不可分配”模型，每个子网最多可用主机数为（ ）。

A. 16  
B. 256  
C. 4094  
D. 4096  

### 850

不考虑 NAT、隧道等中间改写机制，IP 数据报从源结点经过多个网络和路由器到达目的结点的整个过程中，其首部中的（ ）。

A. 源地址和目的地址都不会发生变化  
B. 源地址可能变化而目的地址不会变化  
C. 源地址不会变化而目的地址可能变化  
D. 源地址和目的地址都可能变化  

### 851

按传统分类编址的默认 `/24` 掩码理解，一台主机已有地址 `192.168.11.25`，若另一接口必须位于不同 IP 子网，则另一个地址可能是（ ）。

A. 192.168.11.0  
B. 192.168.11.26  
C. 192.168.13.25  
D. 192.168.11.24  

### 852

某 IPv4 地址为 `126.166.66.99/22`，则下面描述中错误的是（ ）。

A. 网络前缀占 22 bit  
B. 主机编号占 10 bit  
C. 所在地址块包含 $2^{10}$ 个地址  
D. `126.166.66.99` 是所在地址块的第一个地址  

### 853

某主机的 IPv4 地址为 `200.15.10.6/29`，配置的默认网关地址为 `200.15.10.7`。配置后主机无法通过该网关访问远程网络，最直接的原因是（ ）。

A. 默认网关地址不属于该主机所在子网  
B. 默认网关地址是该 `/29` 子网的广播地址  
C. 主机地址 `200.15.10.6` 是该子网的网络地址  
D. 默认网关地址属于 IPv4 多播地址  

### 854

下列对 IPv4 分组分片和重组的描述中，正确的是（ ）。

A. IP 分组可以被源主机分片，并在中间路由器重组  
B. 在允许分片时，IP 分组可以被路径中的路由器分片，并只在目的主机重组  
C. IP 分组可以被路径中的路由器分片，并在任意中间路由器重组  
D. IP 分组可以被路径中的路由器分片，并在最后一跳路由器重组  

### 855

一个网络中已有子网 `74.178.247.96/29`，则下列网络前缀中不能再分配给其他不重叠子网的是（ ）。

A. 74.178.247.120/29  
B. 74.178.247.64/29  
C. 74.178.247.96/28  
D. 74.178.247.104/29  

### 856

主机 A 和主机 B 的 IP 地址分别为 `216.12.31.20` 和 `216.13.32.21`。若要从下列选项中选择一个子网掩码，使二者处于同一 IP 子网，则应选（ ）。

A. 255.255.255.0  
B. 255.255.0.0  
C. 255.255.255.255  
D. 255.0.0.0  

### 857

有 4 条路由：`172.18.129.0/24`、`172.18.130.0/24`、`172.18.132.0/24`、`172.18.133.0/24`。若用一条汇总路由覆盖这 4 条路由，则下列可行的是（ ）。

A. 172.18.128.0/21  
B. 172.18.130.0/22  
C. 172.18.128.0/22  
D. 172.18.132.0/23  

### 858

按传统教材保留网络地址和广播地址的 IPv4 子网模型，在一条点对点链路上为了减少地址浪费，子网掩码应指定为（ ）。

A. 255.255.255.252  
B. 255.255.255.240  
C. 255.255.255.248  
D. 255.255.255.192  

### 859

将一个 `/24` IPv4 网络完整划分成恰好 4 个互不重叠子网。若其中一个子网是 `172.16.1.128/26`，则下列网络中不可能成为另外三个子网之一的是（ ）。

A. 172.16.1.0/25  
B. 172.16.1.64/26  
C. 172.16.1.96/27  
D. 172.16.1.224/27  

### 860

下图中，主机 1 通过以太网 1 连接路由器 R1，R1 经 WAN 与路由器 R2 相连，R2 再通过以太网 2 连接主机 2。主机 1 向主机 2 发送一个 IP 数据报。以太网 1 上承载该 IP 数据报的以太网帧，其**目的 MAC 地址**和 IP 首部中的**目的 IP 地址**分别是（ ）。

![主机1经R1、WAN、R2到主机2的逐跳交付拓扑](./assets/q860_逐跳MAC与端到端IP.svg)

A. 主机 2 的 MAC 地址，主机 2 的 IP 地址  
B. 主机 2 的 MAC 地址，R1 的 IP 地址  
C. R1 在以太网 1 上接口的 MAC 地址，主机 2 的 IP 地址  
D. R1 在以太网 1 上接口的 MAC 地址，R1 的 IP 地址  

## ARP、ICMP 与逐跳交付

### 861

在采用 TCP/IP 的以太网中，以下事件中最直接产生二层广播流量、因而在大量同时发生时最可能形成广播压力的是（ ）。

A. 多台主机同时进行 ARP 查询  
B. 多台主机上的多个进程同时建立 TCP 连接  
C. 多台主机同时使用 ICMP 报告错误  
D. 多台主机同时使用 UDP 传输视频等多媒体数据  

### 862

位于不同子网中的主机相互通信时，下列说法中正确的是（ ）。

A. 路由器转发 IP 数据报时，会为下一跳重新封装当前链路的源硬件地址和目的硬件地址  
B. 路由器转发 IP 数据报时，会把端到端源 IP 地址和目的 IP 地址都改成下一跳地址  
C. 路由器转发时只重新封装目的硬件地址和目的 IP 地址  
D. 源主机可直接 ARP 广播得到远端目的主机的硬件地址  

### 863

下面关于 IPv4 地址与以太网硬件地址的叙述，错误的是（ ）。

A. 在以太网局域网中，硬件地址通常称为物理地址或 MAC 地址  
B. 硬件地址用于当前链路交付，IP 地址用于网络层端到端寻址与上层通信标识  
C. 在以太网链路上实际发送帧时，需要使用当前一跳的硬件地址完成二层交付  
D. RARP 用于由已知 IP 地址查询对应硬件地址  

### 864

路由器收到目的 IPv4 地址为 `255.255.255.255` 的受限广播分组时，应（ ）。

A. 不跨接口转发该 IP 分组  
B. 从所有其他接口转发该 IP 分组  
C. 通过默认路由转发该 IP 分组  
D. 通过特定主机路由转发该 IP 分组  

### 865

在以太网 IPv4 场景中，下列情况需要启动 ARP 请求的是（ ）。

A. 主机接收信息时，ARP 表中没有源 IP 与 MAC 的映射  
B. 主机接收信息时，ARP 表中已有源 IP 与 MAC 的映射  
C. 主机准备向当前下一跳发送信息，但 ARP 表中没有该下一跳 IP 与 MAC 的映射  
D. 主机准备发送信息，且 ARP 表中已有所需下一跳映射  

### 866

主机 A 向主机 B 发送 IPv4 数据报，路径中经过 5 个路由器。假设相邻结点之间的 6 段链路均为以太网，且相关 ARP 缓存初始均为空。在第一个数据报逐跳转发的过程中，共需要发起（ ）次 ARP 请求。

A. 5  
B. 6  
C. 10  
D. 11  

### 867

某以太网中，主机甲 IP 地址为 `211.71.136.23`，子网掩码为 `255.255.240.0`，网关地址为 `211.71.136.1`。若主机甲向主机乙（IP 地址 `211.71.130.25`）发送一个 IP 分组，则（ ）。

A. 直接发送给主机乙，但帧目的 MAC 为网关 MAC  
B. 直接发送给主机乙，帧目的 MAC 为主机乙 MAC  
C. 交由网关转发，帧目的 MAC 为网关 MAC  
D. 交由网关转发，帧目的 MAC 为主机乙 MAC  

### 868

若某路由器收到一个 TTL 值为 1 的 IPv4 数据报，则路由器应（ ）。

A. 正常转发该数据报  
B. 仅丢弃该数据报  
C. 丢弃并向源主机发送“终点不可达”ICMP 差错报文  
D. 丢弃并向源主机发送“时间超过”ICMP 差错报文  

### 869

下列关于 ICMP 报文的说法中，错误的是（ ）。

A. ICMP 报文可绕过 IP，直接作为普通数据链路层帧的上层负载发送  
B. ICMP 报文可以用于报告部分 IP 数据报处理错误  
C. ICMP 报文封装在 IP 数据报中发送  
D. 对已经携带 ICMP 差错报文而触发的错误，不再继续产生新的 ICMP 差错报文  

## IPv6

### 870

与 IPv4 相比，IPv6（ ）。

A. 采用 32 位 IP 地址  
B. 增加了基础首部字段数目  
C. 不提供任何 QoS 支持字段  
D. 基础首部没有首部校验和字段  

### 871

一个 IPv6 地址的简化写法为 `8::D0:123:CDEF:89A`，其完整地址应为（ ）。

A. `8000:0000:0000:0000:00D0:1230:CDEF:89A0`  
B. `0008:0000:0000:0000:00D0:0123:CDEF:89A0`  
C. `8000:0000:0000:0000:D000:1230:CDEF:89A0`  
D. `0008:0000:0000:0000:00D0:0123:CDEF:089A`  

### 872

下列关于 IPv6 的描述中，错误的是（ ）。

A. IPv6 基础首部长度固定为 40 B  
B. IPv6 只允许源节点分片，不允许中间转发路由器分片  
C. IPv6 地址长度为 16 B  
D. IPv6 基础首部使用首部校验和保证传输正确性  

### 873

若路由器收到的 IPv6 数据报因过大而不能在下一条链路上传输，则路由器会将该数据报（ ）。

A. 丢弃，并通过 ICMPv6 Packet Too Big 等机制通知源端调整分组大小  
B. 暂存直到链路 MTU 变大  
C. 在路由器处分片后继续转发  
D. 自动改走一条能够支持该数据报的其他链路  
