# 07｜多播、移动 IP 与路由器｜选择题

> 来源题号：891–911

[查看答案与逐题解析](07_多播_移动IP与路由器_答案与解析.md)

## IP 多播

### 891

以下关于多播概念的描述中，错误的是（ ）。

A. 对一个普通单播 IP 分组，路由器在一次转发动作中只选择一个出接口/下一跳  
B. 对多播分组，路由器可以根据多播转发状态从多个出接口复制并转发  
C. 用多份独立单播模拟一次多播，通常会消耗更多发送端/链路带宽  
D. 用多份独立单播模拟一次多播时，其总网络开销与时延特性基本不会因接收者数量增加而变化  

### 892

以太网多播 IP 地址 `224.215.145.230` 应映射到的多播 MAC 地址是（ ）。

A. 01-00-5E-57-91-E6  
B. 01-00-5E-5B-91-E6  
C. 01-00-5E-D7-91-E6  
D. 01-00-5E-55-91-E6  

### 893

下列 IPv4 地址中，（ ）是多播地址。

A. 10.255.255.255  
B. 228.47.32.45  
C. 192.32.44.59  
D. 172.16.255.255  

## Mobile IP

### 894

以下关于经典 Mobile IPv4 基本工作原理的描述中，错误的是（ ）。

A. 基本工作过程可以分为代理发现、注册、分组路由与注销等阶段  
B. 在使用外地代理提供的转交地址时，归属代理与外地代理之间需要通过隧道转送发往移动结点的分组  
C. 移动结点到达新的网络后，通过注册过程把新的可达信息只通知外地代理，而不需要让归属代理建立/更新移动绑定  
D. 移动 IP 教材模型还可分别讨论移动结点的单播、广播和多播分组路由场景  

### 895

经典 Mobile IP 定义移动结点、归属代理和外地代理等功能实体。以下说法错误的是（ ）。

A. 移动结点具有稳定的归属 IP 地址  
B. 归属代理负责维护移动结点的移动绑定并截获/隧道转发相关分组  
C. 外地代理位于归属网络中，代表移动结点执行归属网络内的移动管理  
D. 归属代理和外地代理都属于移动代理角色  

### 896

一台主机移动到另一个 LAN。若一个发往其归属地址的分组先到达它原来所在的归属 LAN，则该分组会先被转发/截获给（ ）。

A. 移动 IP 的归属代理（本地代理）  
B. 移动 IP 的外地代理  
C. 已离开的移动主机本身  
D. 直接丢弃  

### 897

按经典 Mobile IP 教材模型，一台主机移动到了与其归属 IP 地址不同子网的被访网络。若暂不考虑入口过滤等额外工程策略，则它（ ）。

A. 仍可直接接收和直接发送所有分组  
B. 可按普通路由直接向外发送分组，但发往其归属地址的分组不能按普通路由直接到达当前位置  
C. 不能直接发送，但发往其归属地址的分组可按普通路由直接到达当前位置  
D. 既不能直接接收，也不能直接发送  

### 898

Mobile IP 为移动主机维护稳定的归属地址和随位置变化的转交地址，则（ ）。

A. 两个地址都固定  
B. 两个地址都随移动而动态改变  
C. 归属地址固定，转交地址随移动动态改变  
D. 归属地址动态改变，转交地址固定  

### 899

按经典 Mobile IP 教材模型，一台主机归属地址为 `160.80.40.20/16`，当它移动到不属于 `160.80.0.0/16` 的被访网络时，若只问“能否通过被访网络的普通路由器直接发送/直接接收发往归属地址的数据报”，则它（ ）。

A. 可以直接接收，也可以直接发送  
B. 既不能直接接收，也不能直接发送  
C. 不可直接发送，但可以直接接收  
D. 可以直接发送，但不能按普通路由直接接收发往归属地址的数据报  

## 广播域、路由器结构与转发

### 900

要控制网络中的二层广播风暴，可以采用的方法是（ ）。

A. 只用网桥把网络分成更多二层网段  
B. 用路由器/三层边界把网络划分成不同广播域  
C. 把网络统一改成 10BASE-T  
D. 仅使用网络分析仪观察广播源，不改变网络边界  

### 901

一个局域网要与远处的另一个局域网互连，通常需要使用（ ）。

A. 物理通信介质和集线器即可  
B. 网间连接器和集线器即可  
C. 路由器以及相应的广域网/远距离链路技术  
D. 只需要广域网传输技术，不需要三层互连设备  

### 902

关于路由器的下列说法中，正确的是（ ）。

A. 路由器处理的协议信息比二层交换机更少，因此转发速度必然更快  
B. 对同一目的，路由器只允许选择“传播时延最小”这一种最佳路由标准  
C. 多协议路由器可以支持多种网络层协议，并分别依据相应协议的地址与路由状态完成转发  
D. 路由器的数据平面既可以把 MAC 地址作为普通二层交换依据，又可以不解析网络层协议完成跨网转发  

### 903

路由选择算法的核心任务之一是构造路由状态。以下说法错误的是（ ）。

A. 路由表/控制平面状态总是只能由通用软件结构实现，不能借助专门硬件或加速结构  
B. 转发表可以由 TCAM 等专用硬件/高速数据结构实现  
C. 转发表通常包含目的前缀以及对应的下一跳/出接口动作  
D. 默认路由可作为最低具体度的兜底项，在没有更具体匹配时使用  

### 904

关于 IP 数据报的直接交付和间接交付，下列说法中错误的是（ ）。

I. IP 交付可分为直接交付和间接交付  
II. 直接交付时，通信双方可以不共享任何直接可达的链路/物理网络  
III. 间接交付过程中完全不包含任何逐跳的直接交付  
IV. 当源主机与目的主机处于同一个直接连接 IP 子网时，主机间直接交付不需要中间路由器

A. I 和 II  
B. II 和 III  
C. III 和 IV  
D. I 和 IV  

### 905

路由器中负责运行路由协议、计算和维护路由信息的主要部件是（ ）。

A. 输入队列  
B. 输出队列  
C. 交换结构  
D. 路由选择处理机  

### 906

下面关于路由器结构和功能的说法，正确的是（ ）。

I. 路由器可分为路由选择部分和分组转发部分，分组转发部分由输入端口、交换结构和输出端口构成  
II. 使用路由器建立三层边界可以控制二层广播的传播范围  
III. 路由表可由路由算法/配置形成，转发表根据可用路由结果生成并供数据平面快速使用

A. 仅 I、II  
B. 仅 I、III  
C. 仅 II、III  
D. I、II、III  

### 907

下列关于交换机连接的网络和路由器连接的网络的描述中，错误的是（ ）。

A. 二层交换机转发帧时主要依据 MAC 地址  
B. 路由器转发 IP 分组时主要依据 IP 目的地址/前缀  
C. 普通二层交换机必须理解并解析帧内承载的各种网络层协议后才能完成 MAC 转发  
D. 路由器必须理解所支持网络层协议的首部与地址语义，才能完成相应三层转发  

### 908

路由器转发一个经链路接收的 IP 分组并从另一链路发出时，需要进行（ ）。

A. 网络层处理和数据链路层处理，不涉及物理层  
B. 网络层处理和物理层处理，不涉及数据链路层  
C. 数据链路层处理和物理层处理，不涉及网络层  
D. 网络层、数据链路层和物理层处理  

### 909

在 IPv4 路由表中设置默认路由，其目的前缀应表示为（ ）。

A. 192.168.1.1/24  
B. 127.0.0.0/8  
C. 0.0.0.0/0（等价于目的地址 0.0.0.0、掩码 0.0.0.0）  
D. 0.0.0.0/32  

### 910

在经典 IPv4/Ethernet 教材模型中，路由器能够分割二层广播域的主要原因是（ ）。

A. 路由器工作在网络层，普通二层广播帧不会被它透明转发到其他接口  
B. 路由器工作在数据链路层，因此不转发广播帧  
C. 路由器工作在物理层，因此不转发广播帧  
D. 路由器工作在传输层，因此不转发广播帧  

### 911

在集线器、二层交换机/网桥和路由器之间比较，通常需要解析最高层次协议信息、因而传统软件转发处理时延相对最大的是（ ）。

A. 局域网交换机  
B. 网桥  
C. 路由器  
D. 集线器  
