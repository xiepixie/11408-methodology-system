# 08｜传输层、UDP 与 TCP 状态机｜选择题

> 来源题号：912–946

[查看答案与逐题解析](08_传输层_UDP与TCP状态机_答案与解析.md)

## 传输层端点、UDP 与校验和

### 912

按经典 TCP 面向连接服务模型，其主要特性是（ ）。

A. 不保证可靠和顺序交付  
B. 不保证可靠但保证顺序交付  
C. 保证可靠但不保证顺序交付  
D. 保证可靠和按序交付  

### 913

可靠传输协议中的“可靠”主要指通过确认、序号和必要重传等机制，使发送的数据能够完整、无重复并按要求交付。下列最符合这一含义的是（ ）。

A. 只要使用面向连接的会话  
B. 使用尽力而为的传输  
C. 只要存在滑动窗口就天然可靠  
D. 使用确认并在确认缺失/错误时配合重传等机制保证交付  

### 914

以下（ ）能够在给定传输协议下标识一台主机上的通信进程端点。

A. 主机名  
B. MAC 地址及端口号  
C. IP 地址及 MAC 地址  
D. IP 地址及端口号  

### 915

下列关于套接字（socket endpoint）的描述，错误的是（ ）。

A. 套接字端点用于标识主机上的应用通信端点  
B. IP 地址与端口号是网络套接字端点的重要组成部分，TCP 连接还需结合对端端点标识  
C. 套接字只能标识不同主机，不能区分同一主机上的不同应用进程  
D. 套接字也是应用程序访问操作系统网络通信能力的软件接口抽象  

### 916

在 TCP/IP 参考模型中，若用户程序使用 UDP 传输而应用又要求可靠交付，则（ ）层必须自行承担 UDP 未提供的可靠性工作。

A. 数据链路层  
B. 网际层  
C. UDP 传输层自身  
D. 应用层  

### 917

以下说法错误的是（ ）。

A. UDP 是无连接的，TCP 是面向连接的  
B. 与 IP 相比，UDP 增加了端口复用/分用和端到端校验等能力  
C. TCP 与 UDP 的端口号属于各自独立的传输协议命名空间，因此可出现相同数字的 TCP 端口和 UDP 端口  
D. HTTP 通信中客户端和服务器端通常都必须使用服务器的 80 号端口  

### 918

以下关于 UDP 协议的描述中，错误的是（ ）。

A. UDP 首部主要包括源/目的端口、长度和校验和字段  
B. UDP Length 字段统计 UDP 首部、数据以及用于校验的伪首部总长度  
C. UDP 校验和计算概念上覆盖伪首部、UDP 首部和应用数据  
D. UDP 伪首部使用了来自 IP 层的源/目的地址等字段  

### 919

下列关于 UDP 的描述，正确的是（ ）。

A. 保证数据按序投递  
B. 不允许多个应用进程复用 UDP  
C. 自带接收窗口流量控制机制  
D. 是无连接的传输协议  

### 920

UDP 相比 IP 数据报服务额外提供的核心能力之一是（ ）。

A. 路径流量控制  
B. 拥塞控制  
C. 端口复用/分用，使数据可交付到主机内正确应用端点  
D. 路由转发  

### 921

以下关于 IPv4 中 UDP 校验和的说法，错误的是（ ）。

A. 计算时按 16 bit 字处理；若数据总字节数为奇数，可在计算末尾补一个全 0 字节，该字节不发送  
B. 若计算得到的 UDP 校验和值为全 0，则在线校验和字段仍直接填写全 0  
C. 校验和计算包括伪首部、UDP 首部和用户数据  
D. 采用 16 bit 反码加法求和，再对结果取反  

### 922

以下有关 UDP 校验和的描述中，错误的是（ ）。

A. 计算校验和时需要按 4 B 对齐；若数据不足 4 B 的整数倍，应补零到 4 B 边界  
B. UDP 校验和覆盖 UDP 首部和数据，并把 IP 层提供的伪首部字段纳入计算  
C. 接收端检测到 UDP 校验和错误时，应把该数据报视为损坏并丢弃；UDP 本身不负责重传纠错  
D. 伪首部中的源 IP、目的 IP 和上层协议号等字段使校验和还能发现一部分误投递错误  

### 923

一个 UDP 数据报的数据字段长度为 9192 B。采用 IPv4 固定 20 B 首部，且通过 MTU=1500 B 的标准以太网传输，则需要的 IPv4 分片数为（ ）。

A. 6  
B. 7  
C. 8  
D. 9  

### 924

长度为 2000 B 的应用层数据依次封装为 TCP（最小 20 B 首部）、IPv4（20 B 首部）和以太网帧。若暂不考虑 MTU 分片，且以太网每帧只计 14 B 首部与 4 B FCS，则应用数据占在线帧字节的最高传输效率为（ ）。

A. 97.28%  
B. 95.42%  
C. 97.19%  
D. 96.34%  

### 925

长度为 2000 B 的应用层数据依次封装为 TCP（最小 20 B 首部）、IPv4（20 B 首部）和以太网帧。标准以太网 MTU 为 1500 B，需要考虑 IPv4 分片；每个以太网帧只计 14 B 首部与 4 B FCS，则应用数据的最高传输效率为（ ）。

A. 97.28%  
B. 95.42%  
C. 97.19%  
D. 96.34%  

### 926

某应用层数据大小为 200 B，传输层使用 UDP（8 B 首部），网际层使用 IPv4 最大首部 60 B，使用以太网传输并只计 14 B 以太网首部和 4 B FCS（不计前导码、IFG 和 VLAN），则应用数据的传输效率是（ ）。

A. 82.6%  
B. 77.5%  
C. 69.9%  
D. 67.1%  

## TCP 首部、字节序号与连接建立/关闭

### 927

下列关于 TCP 端口的叙述中，错误的是（ ）。

A. 客户端通常动态选择短暂端口  
B. TCP 端口号字段长度为 16 bit  
C. 端口号用于主机内传输层分用到应用端点  
D. 同一局域网内不同计算机不能使用相同 TCP 端口号  

### 928

以下关于 TCP 首部格式的描述中，错误的是（ ）。

A. 首部长度为 20～60 B，其中固定部分 20 B  
B. 首部开始的两个 16 bit 端口字段依次为源端口和目的端口  
C. TCP 首部长度总是 4 B 的整数倍  
D. TCP 校验和伪首部中的 IP Protocol 字段值为 17  

### 929

TCP 是面向字节流的传输协议。关于 TCP 报文段长度的表述，正确的是（ ）。

A. 只由应用进程每次 `write` 的数据块边界决定  
B. 只由路径 MTU 决定，与接收方和当前网络状态无关  
C. 可受 MSS/路径限制、接收窗口、拥塞/发送状态等共同约束，不要求固定等于应用数据块长度  
D. 一旦首次确定，在整条连接中保持不变  

### 930

以下关于 TCP 协议的说法，不正确的是（ ）。

A. TCP 具有拥塞控制功能  
B. TCP 首部中的 Window 字段用于通告发送端的拥塞窗口 `cwnd`，从而实现拥塞控制  
C. TCP 提供面向连接的可靠字节流服务，并保证已交付给应用的数据按序  
D. TCP 是点到点传输协议，不提供 IP 组播式的一对多连接  

### 931

下列关于 TCP 的叙述中，正确的是（ ）。

A. TCP 提供可靠无连接的数据传输  
B. TCP 必须保持应用层每次交付数据块的消息边界，并把每个数据块独立组成一个分组  
C. TCP 提供一对多广播传输  
D. TCP 把应用交付的数据解释为连续字节流，并按需要切分为报文段传输  

### 932

A 与 B 已建立 TCP 连接。当 A 收到确认号 `ack=100` 的有效 ACK 时，表示对端已经连续收到的最后一个字节序号为（ ）。

A. 99 号报文段  
B. 100 号报文段  
C. 99 号字节  
D. 100 号字节  

### 933

以下关于 TCP 窗口与拥塞控制概念的描述中，错误的是（ ）。

A. 接收窗口 `rwnd` 通过 TCP 首部 Window 字段向对端通告  
B. 教材模型中发送在途上限受 `min(rwnd,cwnd)` 约束  
C. 拥塞窗口 `cwnd` 是接收端根据自身缓冲区空闲量计算并通知发送端的窗口值  
D. 慢开始阶段 `cwnd` 在 RTT 粒度上可近似指数增长  

### 934

TCP 使用三次握手建立连接。设 A、B 的初始序列号分别为 $X$、$Y$。A 首先发送（①），B 接收后发送（②），然后 A 再确认 B 的 SYN。

I. `SYN=1, seq=X`  
II. `SYN=1, seq=X+1, ACK_X=1`  
III. `SYN=1, seq=Y, ACK_{Y+1}=0`  
IV. `SYN=1, seq=Y, ACK_{Y+1}=1`  
V. `SYN=1, seq=X+1, ACK_X=0`  
VI. `SYN=1, ACK=1, seq=Y, ack=X+1`

A. I、VI  
B. II、IV  
C. V、VI  
D. I、III  

### 935

TCP 三次握手的第二个报文段中，被置为 1 的主要控制标志是（ ）。

A. SYN  
B. ACK  
C. ACK 和 RST  
D. SYN 和 ACK  

### 936

TCP 三报文握手建立连接时，第三个报文的核心作用是（ ）。

A. 首次发出 TCP 连接请求  
B. 服务器首次确认客户端 SYN  
C. 客户端确认已经收到服务器的 SYN/初始序号，使服务器获得双向同步完成的证据  
D. 必须携带普通应用数据  

### 937

A、B 已建立 TCP 连接。A 向 B 发送一个报文段：`seq=200, ack=201`，数据部分 2 B。若 B 此前没有新的数据要发送，则 B 对该段发送纯确认时可取（ ）。

A. `seq=202, ack=200`  
B. `seq=201, ack=201`  
C. `seq=201, ack=202`  
D. `seq=202, ack=201`  

### 938

在不携带应用数据的标准 TCP 三次握手中，ACK / SYN / FIN 标志被置 1 的报文段次数分别为（ ）。

A. 1，2，0  
B. 1，1，1  
C. 2，2，0  
D. 2，1，0  

### 939

主机甲向乙发送 `(SYN=1, seq=1000)` 请求建立 TCP 连接。若乙接受请求，则乙返回的报文段**可能**是（ ）。

A. `(SYN=1, ACK=0, seq=1001, ack=1001)`  
B. `(SYN=1, ACK=1, seq=1000, ack=1000)`  
C. `(SYN=1, ACK=1, seq=1001, ack=1001)`  
D. `(SYN=0, ACK=1, seq=1000, ack=1000)`  

### 940

TCP 通信双方中，一方发送带 FIN 标志的报文段后，表示（ ）。

A. 双方两个方向的 TCP 通信都立即被强制断开  
B. 本方声明其发送方向结束；在对端尚未关闭反方向时，本方仍可继续接收对方数据  
C. 双方都立即禁止发送和接收  
D. 连接重新建立  

### 941

TCP 客户和服务器的数据传输已结束，端到端 RTT 为 RTT。$t$ 时刻客户端主动发送 FIN；假设服务器收到 FIN 后可立即回复 ACK+FIN，忽略处理和发送时延，则从 $t$ 起服务器最早在何时收到客户端最终 ACK 并释放自己的连接状态？

A. $0.5\,RTT$  
B. $1\,RTT$  
C. $1.5\,RTT$  
D. $2\,RTT$  

### 942

甲发起 TCP 连接，初始序号 ISN=200。连接建立过程中第三个握手报文不携带数据，则连接建立后甲发送的第一个数据字节序号为（ ）。

A. 203  
B. 202  
C. 201  
D. 200  

### 943

A 发起与 B 的 TCP 连接，A 的 ISN=1666，握手不携带数据。连接建立后 A 发送 1000 B 数据，B 正确连续接收后返回的确认号应为（ ）。

A. 1667  
B. 2666  
C. 2667  
D. 2668  

### 944

TCP 数据传输阶段，若发送端当前发送窗口大小为 3000 B，在忽略已经占用窗口的在途字节这一简化条件下，意味着发送端最多可在等待窗口继续推进前保持（ ）数据未确认。

A. 3000 个 TCP 报文段  
B. 1000 B  
C. 3000 B  
D. 2000 个 TCP 报文段  

### 945

甲向乙发送 3 个连续 TCP 段，有效载荷分别为 200 B、300 B、400 B，第 3 段 `seq=1000`。若乙仅正确收到第 1 段和第 3 段，按累计确认语义，乙返回的确认号为（ ）。

A. 500  
B. 600  
C. 700  
D. 800  

### 946

甲乙建立 TCP 连接。甲向乙发送的报文段数据部分为 10 B，乙随后返回的确认报文段中 `seq=401, ack=311`。若该 ACK 正常确认了甲刚发送的 10 B 数据，则甲原报文段的字段应为（ ）。

A. `seq=301, ack=301`  
B. `seq=300, ack=401`  
C. `seq=301, ack=401`  
D. `seq=302, ack=311`  
