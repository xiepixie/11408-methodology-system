# 10｜应用层：DNS、FTP、邮件与 HTTP｜选择题

> 来源题号：959–1000

[查看答案与逐题解析](10_应用层_DNS_FTP_邮件与HTTP_答案与解析.md)

## 客户/服务器与 P2P

### 959

按基本客户/服务器（C/S）模型并忽略浏览器缓存等实现优化，下列描述中存在错误的是（ ）。

I. 客户端发起服务请求前通常需要知道服务器的地址，而服务器无需预先知道某个具体客户端的地址  
II. 客户端通常负责用户交互，服务器通常集中实现数据/服务处理  
III. 浏览器通过 Web 服务取得并显示服务器返回的内容  
IV. 客户端是请求方，因此连接一旦建立，服务器也不能主动向客户端发送任何数据

A. I、IV  
B. III、IV  
C. 只有 IV  
D. 只有 III  

### 960

以下关于对等（P2P）应用模型的描述中，错误的是（ ）。

A. 对等结点之间的客户/服务器角色通常不是长期固定的，同一结点可在不同交互中既发起请求又提供服务  
B. P2P 网络是一张与 Internet 并行建设、由对等结点组成的独立物理网络  
C. P2P 中每个结点可以既作为服务请求方，也作为服务提供方  
D. 纯 P2P 模型通常不依赖一台始终在线、集中处理全部业务请求的中心服务器  

### 961

下列关于客户/服务器模型的说法中，不正确的是（ ）。

A. 服务器用于提供某类服务，客户机作为这些服务的使用者  
B. 客户机通常位于用户侧前端，服务器通常位于服务后端  
C. 客户机和服务器可通过网络协同完成计算任务  
D. 客户机是面向后台任务的，而服务器是面向最终用户交互的  

### 962

以下关于 P2P 概念的描述中，错误的是（ ）。

A. P2P 是网络结点之间以对等方式直接交换信息的应用工作模式  
B. P2P 结点可同时具有客户和服务器能力，有利于把负载分散到多个结点  
C. P2P 网络是与 Internet 平行建设的一张独立物理网络  
D. P2P 实现技术包括支持对等结点直接通信所需的协议和软件机制  

## DNS

### 963

域名与下列哪一项具有严格的一一对应关系（ ）。

A. IP 地址  
B. MAC 地址  
C. 物理主机  
D. 以上都不是  

### 964

域名系统（DNS）的组成不包括（ ）。

A. 域名空间  
B. 分布式数据库  
C. 域名服务器  
D. 从内部 IP 地址到外部 IP 地址的 NAT 翻译程序  

### 965

在典型 DNS 配置中，主机上的 resolver 需要知道本地域名服务器地址，但不需要预先知道（ ）信息。

I. 本地域名服务器的 IP 地址  
II. 本地域名服务器“父结点”的 IP 地址  
III. 根域名服务器的 IP 地址

A. I 和 II  
B. I 和 III  
C. II 和 III  
D. I、II 和 III  

### 966

在一次**递归 DNS 查询**中，客户端只把查询交给最开始接受递归请求的 DNS 服务器，由该服务器负责继续查询。最终给该客户端返回结果的是（ ）。

A. 最开始接受客户端递归请求的服务器  
B. 最后被查询到的服务器必须直接回复原客户端  
C. 目的域的权威服务器必须绕过中间服务器回复客户端  
D. 无法确定，因为递归查询不规定返回路径  

### 967

本地域名服务器需要解析 `www.a.b.com`，且本地缓存中没有该域名、相关委派或更具体的权威服务器信息。按典型迭代解析流程，它首先应把查询发送给（ ）。

A. `a.b.com` 的权威 DNS 服务器  
B. `b.com` 的权威 DNS 服务器  
C. 根域名服务器  
D. 不再发送查询，直接返回失败  

### 968

一部尚未取得校园网 IP 配置的手机完成 Wi-Fi 二层接入后，要访问 `http://www.test.edu.cn`。在给出的选项中，它首先需要发送的是（ ）报文。

A. DHCP  
B. TCP 连接请求  
C. DNS 域名查询  
D. ARP 地址解析  

### 969

下面关于 DNS 的描述中，错误的是（ ）。

A. 域名层次与 IP 地址的地理位置或地址前缀没有必然一一对应关系  
B. 终端主机通常直接对根、顶级域和权威服务器执行迭代查询，而不是把递归请求交给本地递归解析器  
C. 主机或解析器可缓存域名解析结果，不同记录的 TTL 可以不同  
D. 普通 DNS 查询通常可以使用 UDP  

### 970

下列说法错误的是（ ）。

A. Internet 上任何向客户提供服务的主机都必须拥有域名，不能只通过 IP 地址访问  
B. 同一域名在不同时间可能解析到不同 IP 地址  
C. 多个域名可以解析到同一个 IP 地址  
D. 同一 IP 子网中的不同主机记录可以由不同权威 DNS 区域/服务器体系管理  

### 971

（ ）负责保存某个 DNS 区域内的权威记录，并可把其管辖的主机名解析为相应 IP 地址。

A. 本地域名服务器  
B. 根域名服务器  
C. 权限（权威）域名服务器  
D. 代理域名服务器  

### 972

若本地域名服务器无缓存，用户主机采用递归查询向本地域名服务器查询另一网络某主机的域名，而本地域名服务器采用迭代查询向 DNS 层次结构继续解析，则用户主机和本地域名服务器发送的 DNS 请求条数通常分别为（ ）。

A. 一条，一条  
B. 一条，多条  
C. 多条，一条  
D. 多条，多条  

### 973

在一个**理论上的全递归 DNS 查询模型**中，用户主机只向本地域名服务器发送一次请求；本地域名服务器向上级服务器发出一次递归请求，此后由被查询服务器继续递归向下查询并沿原路径返回。若本地无缓存，则用户主机和本地域名服务器发送的 DNS 请求条数分别为（ ）。

A. 一条，一条  
B. 一条，多条  
C. 多条，一条  
D. 多条，多条  

## FTP

### 974

在经典 FTP 模型中，客户可以通过控制命令指定文件表示类型及相关传输表示方式。下列说法正确的是（ ）。

A. 允许指定文件类型，但不允许指定表示格式  
B. 不允许指定文件类型，但允许指定表示格式  
C. 可以对文件类型/表示方式进行相应选择  
D. 文件类型和表示方式均不能选择  

### 975

以下关于 FTP 工作模型的描述中，错误的是（ ）。

A. FTP 使用控制连接和数据连接完成文件传输  
B. 服务器端控制连接的熟知端口号为 TCP 21  
C. 客户端建立控制连接时默认使用 TCP 20 作为本地端口  
D. FTP 服务器逻辑上包含控制处理与数据传输处理  

### 976

下列关于 FTP 连接的叙述中，正确的是（ ）。

A. 控制连接先于数据连接建立，并先于数据连接释放  
B. 数据连接先于控制连接建立，并先于控制连接释放  
C. 控制连接先于数据连接建立，并晚于数据连接释放  
D. 数据连接先于控制连接建立，并晚于控制连接释放  

### 977

FTP 服务器通过监听（ ）熟知端口接收新的控制连接请求。

A. 53  
B. 80  
C. 20  
D. 21  

### 978

下列关于 FTP 的叙述中，错误的是（ ）。

A. FTP 基于 TCP，可在异构网络主机之间可靠传送文件数据  
B. 传输文件时，FTP 控制连接与数据连接承担不同职责  
C. FTP 服务器主控制进程在 TCP 20 端口监听客户端的控制连接请求  
D. 经典 FTP 机制可支持由客户端协调的第三方服务器间数据传送模型  

### 979

下列关于 FTP 的叙述中，错误的是（ ）。

A. 控制连接由客户端发起；在主动模式中数据连接可由服务器端发起  
B. FTP 更适合完整文件传输，而不是像远程文件系统那样持续共享随机读写  
C. 控制连接通常在整个 FTP 会话期间保持  
D. FTP 客户端默认使用本地 TCP 20 端口与服务器建立数据传输连接  

### 980

FTP 客户端发起对服务器的会话时，第一阶段首先建立（ ）。

A. 普通“传输连接”而不区分用途  
B. 会话层连接  
C. 数据连接  
D. 控制连接  

### 981

下列关于 FTP 工作过程的描述，错误的是（ ）。

A. FTP 支持不同操作系统之间的文件传输  
B. FTP 服务器逻辑上具有控制处理和数据传输处理  
C. 每次数据传输结束时，FTP 会同时释放控制连接和数据连接  
D. 控制连接通常在整个 FTP 会话期间持续存在  

## 电子邮件

### 982

SMTP 基于传输层的（ ）协议，POP3 基于传输层的（ ）协议。

A. TCP，TCP  
B. TCP，UDP  
C. UDP，UDP  
D. UDP，TCP  

### 983

下列关于 POP3 的说法，错误的是（ ）。

A. 邮件取回后是否标记删除/保留只能由服务器单方面决定，客户端不能控制  
B. POP3 的经典文本命令接口不能直接裸传任意二进制内容，邮件附件通常依赖 MIME 编码  
C. 建立 TCP 连接之后，才进入 POP3 会话  
D. POP3 会话需要经过用户认证后才能进入事务阶段  

### 984

下列关于 SMTP 的描述，正确的是（ ）。

A. SMTP 使用 TCP 进行可靠字节流传输  
B. SMTP 协议本身天然对邮件正文进行端到端加密  
C. SMTP 客户端无需与服务器建立传输连接即可直接发送邮件  
D. MIME 的主要目的只是让 SMTP 支持原本已经可以传输的普通 ASCII 文本，不用于扩展非 ASCII/多媒体表示  

### 985

下列关于电子邮件格式的说法中，错误的是（ ）。

A. 邮件通常由首部和主体组成，主体承载实际内容  
B. From、To、Subject 等首部字段全部都只能由系统自动生成，用户不能指定  
C. 同一邮件服务器命名空间中，账号/本地用户名需要能够唯一定位相应邮箱  
D. MIME 使邮件系统能够表示文本以外的图像、音频、视频等内容  

### 986

下列关于经典 POP3 的说法，错误的是（ ）。

A. 客户端可决定取回后是否对服务器邮件执行删除操作  
B. 使用传统 USER/PASS 且未启用 TLS 等保护时，密码会自动以加密形式在协议层传输  
C. POP3 命令/响应是文本协议，二进制附件通常需经过邮件内容编码后承载  
D. 经典 POP3 面向用户单一邮箱，不提供 IMAP 式多文件夹服务器管理模型  

## WWW / HTTP / Cookie / NAT

### 987

若浏览器访问的 URL 使用域名、相关 DNS 结果未缓存，则在建立 Web 服务器 TCP 连接前首先需要完成（ ）。

A. 对 Web 服务器发出“地址解析”应用请求  
B. 先建立到 Web 服务器的传输连接  
C. DNS 域名解析  
D. OSI 会话层连接建立  

### 988

用户可以通过 `http://www.a.com` 和 `http://www.b.com` 访问同一台 Web 服务器上的两个不同站点。若二者使用相同服务器 IP 和相同 TCP 端口，则服务器区分这两个基于名称的虚拟主机，关键依据是（ ）。

A. 为两个站点分配不同的服务器 IP 地址  
B. 为两个站点强制使用不同 TCP 端口  
C. 为两个站点使用不同的传输层协议  
D. HTTP 请求中的 `Host`（或等价 authority）信息  

### 989

HTTP/1.0 默认采用非持续连接。若不使用 `Keep-Alive` 等扩展，则其典型工作模式是（ ）。

A. 一个 TCP 连接建立后默认传送任意多个页面对象  
B. 使用 TCP 但不需要建立连接  
C. 每个页面对象建立 UDP 连接  
D. 每个独立对象请求/响应通常使用独立 TCP 连接  

### 990

下列关于 HTTP 请求方法的描述，错误的是（ ）。

A. GET 通常不适合把大量请求实体数据放在 URL 查询部分  
B. HTTP 协议本身没有为 POST 请求体规定一个统一的很小固定上限，实际限制通常由服务器/应用配置决定  
C. HEAD 返回与 GET 类似的响应首部，但不返回响应主体  
D. PUT 方法的标准语义是查询目标资源所支持的 HTTP 方法集合  

### 991

下列关于 HTTP 非持续连接和持续连接的描述中，错误的是（ ）。

A. HTTP/1.0 默认非持续，HTTP/1.1 默认持续  
B. 持续连接中服务器发送一个响应后可继续保持 TCP 连接供后续请求使用  
C. 非持续连接中每个独立请求/响应对象通常需要各自建立 TCP 连接  
D. 同一服务器上的多个 HTML/对象必须通过多个不同的持续 TCP 连接才能传送  

### 992

按 HTTP/1.0 默认非持续连接访问一个 HTML 文档，该文档还引用 3 个 JPG 图像。忽略 DNS 本身并只统计 HTTP 所需传输连接，则需要建立（ ）次 UDP 连接和（ ）次 TCP 连接。

A. 0，4  
B. 1，3  
C. 0，2  
D. 1，2  

### 993

浏览器使用 HTTP/1.1 持续连接、**非流水线**方式访问一个网页，该 HTML 页面包含 5 个 GIF 图像。忽略对象发送时间与 TCP 建连时间，且第三次握手不携带 HTTP 请求。若从发出第一个 HTTP 请求开始计时，则最快需要（ ）获得完整页面。

A. 1 RTT  
B. 2 RTT  
C. 5 RTT  
D. 6 RTT  

### 994

浏览器采用 HTTP/1.1 持续、非流水线方式，请求一个引用 3 个 JPEG 小图像的 `demo.html`。已假定 TCP 连接建立完成，每次请求—响应需要 1 RTT，忽略对象发送时延。则从发出第一个 Web 请求到收到全部内容需要（ ）RTT。

A. 3  
B. 4  
C. 5  
D. 6  

### 995

浏览器通过已经建立的 HTTP/1.1 持续连接请求 `rfc.html`，该页面引用同目录下 3 个 JPEG 小图像。假设只有收到 `rfc.html` 后才能知道图像 URL；收到 HTML 后允许在同一持续连接上**流水线一次发送 3 个图像请求**。每一批请求—响应往返耗时为 1 RTT，忽略对象发送时延、拥塞控制和流量控制。则从发出 `rfc.html` 的 HTTP 请求到收到全部内容共需（ ）。

A. 2 RTT  
B. 2.5 RTT  
C. 4 RTT  
D. 4.5 RTT  

### 996

一个 Web 页面含 10 幅 GIF。基本 HTML、HTTP 请求和 TCP 握手报文的发送时延忽略，RTT=150 ms，每幅 GIF 的发送时延为 35 ms。采用一个 HTTP 持续连接、非流水线（stop-and-wait 请求/响应）方式访问，则从开始建立 TCP 连接到收到全部页面至少需要（ ）ms。

A. 3650  
B. 2150  
C. 3350  
D. 1850  

### 997

学生 A 在浏览器中输入 `http://www.taobao.com` 并成功访问首页。按正常成功访问流程，假设 URL 需要 DNS 解析、当前下一跳 MAC 未缓存，且不发生需要 ICMP 报错的异常。从应用层到网络接口相关过程中可能使用（ ）。

I. DNS  
II. HTTP  
III. TCP  
IV. ARP  
V. ICMP

A. I、II  
B. I、II、III  
C. I、II、III、IV  
D. I、II、III、IV、V  

### 998

下列关于 HTTP Cookie 的说法中，错误的是（ ）。

A. Cookie 的副本只存储在服务器端，客户端浏览器不保存 Cookie  
B. 服务器可通过响应头创建/更新 Cookie  
C. Cookie 可用于状态跟踪，因此也会带来隐私问题  
D. 浏览器后续请求可携带 Cookie，使服务器关联用户会话/状态  

### 999

关于非持续 HTTP，下列描述错误的是（ ）。

A. HTTP 支持非持续和持续连接两类使用方式  
B. HTTP/1.0 默认非持续，HTTP/1.1 默认持续  
C. 非持续连接中，每个独立对象请求/响应通常建立一次 TCP 连接  
D. 读取一个“基本 HTML + 100 个图片对象”的页面时，总共只需要打开和关闭 100 次 TCP 连接  

### 1000

某 NAT 路由器公网地址为 `205.56.79.35`，已有映射：

| 公网转换端口 | 内部源 IP         | 内部源端口 |
| --------------: | --------------- | ------------: |
|   2056 | 192.168.32.56  |    21 |
|   2057 | 192.168.32.56  |    20 |
|   1892 | 192.168.48.26  |    80 |
|   2256 | 192.168.55.106 |    80 |

现收到一个准备发往公网的分组，源为 `192.168.32.56:80`。若不存在其他匹配映射，NAT 的正确动作是（ ）。

A. 直接复用公网端口 2056，因为源 IP 已经出现过  
B. 为新的内部源 tuple 分配一个可用公网端口并新增映射，然后改写并转发  
C. 因端口 80 未出现在该内部主机已有表项中而丢弃  
D. 不做地址/端口转换，直接把私有源地址转发到公网  
