# 01｜操作系统基础与程序运行环境｜选择题

> 来源题号：475–502  
> 已录入：28 题  
> 其中 480、481 依据高清原页重新校正；481 的缺失判断项经同题版本交叉恢复。

[查看答案与逐题解析](01_操作系统基础与程序运行环境_答案与解析.md)

## 操作系统发展与基本特征

### 475

提高单机资源利用率的关键技术是（ ）。

A. 脱机技术  
B. 虚拟技术  
C. 交换技术  
D. 多道程序设计技术  

### 476

下列关于分时系统的叙述中，错误的是（ ）。

A. 分时系统主要用于批处理作业  
B. 分时系统中每个任务依次轮流使用时间片  
C. 分时系统的响应时间较好  
D. 分时系统是一种多用户操作系统  

### 477

下列关于分时操作系统的叙述中，正确的是（ ）。

I. 用户间的操作互不干扰  
II. 需要实现对共享资源的保护  
III. 需要实现虚拟内存技术

A. 仅 I  
B. 仅 II  
C. 仅 I、II  
D. I、II 和 III  

### 478

分时系统强调交互响应。若暂不考虑其他瓶颈，在上下文切换开销仍可接受的前提下，下列哪项调整通常更有利于缩短交互任务的响应时间？（ ）

A. 一味增大时间片，使当前任务尽量长时间连续运行  
B. 将存储管理方式改为静态页式管理，而不改变 CPU 调度  
C. 适当缩短时间片，并允许就绪的高优先级交互任务在需要时抢占 CPU  
D. 仅把程序代码改成可重入代码，而不改变调度与等待关系  

### 479

引入多道程序技术的前提条件之一是系统具有（ ）。

A. 多个 CPU  
B. 多个终端  
C. 中断功能  
D. 分时功能  

## 程序运行环境、特权级与系统调用

### 480

下列关于操作系统的说法中，错误的是（ ）。

I. 在通用操作系统管理下的计算机上运行程序，需要向操作系统预订运行时间  
II. 在通用操作系统管理下的计算机上运行程序，需要确定起始地址，并从这个地址开始执行  
III. 操作系统需要提供高级程序设计语言的编译器  
IV. 管理计算机系统资源是操作系统关心的主要问题

A. I  
B. I、III  
C. II、III  
D. I、II、III、IV  

### 481

下列说法中，正确的是（ ）。

I. 批处理系统的主要缺点是需要大量内存  
II. 当计算机提供核心态和用户态时，输入/输出指令必须在核心态下执行  
III. 操作系统中采用多道程序设计技术的最主要原因是提高 CPU 和外部设备的可靠性  
IV. 操作系统中，通道技术是一种硬件技术

A. I、II  
B. I、III  
C. II、IV  
D. II、III、IV  

### 482

下列关于系统调用的说法中，正确的是（ ）。

I. 用户程序使用系统调用命令，该命令经过编译后形成若干参数和陷入指令  
II. 用户程序使用系统调用命令，该命令经过编译后形成若干参数和屏蔽中断指令  
III. 用户程序创建一个新进程，需使用操作系统提供的系统调用接口  
IV. 当操作系统完成用户请求的系统调用功能后，应使 CPU 从内核态转到用户态

A. I、III  
B. III、IV  
C. I、III、IV  
D. II、III、IV  

### 483

在执行系统调用的过程中，下列哪些操作由用户程序完成？（ ）

I. 参数传递  
II. 切换到内核态  
III. 系统调用服务例程的调用  
IV. 返回值的获取

A. I  
B. I、II  
C. I、IV  
D. I、II、III  

### 484

下列关于系统调用的叙述中，错误的是（ ）。

A. 系统调用是用户程序与操作系统之间的接口  
B. 执行系统调用时，用户程序执行受控陷入/系统调用指令，随后由硬件入口完成从用户态到内核态的特权级切换  
C. 在执行系统调用过程中，操作系统会将内核态堆栈保存在用户程序的堆栈中  
D. 系统调用执行完毕后，操作系统会将 CPU 从内核态切换回用户态  

### 485

用户程序在用户态下试图执行特权指令所引起的异常属于（ ）。

A. 故障（Fault）异常  
B. 终止（Abort）异常  
C. 外部中断  
D. 陷阱（Trap）异常  

### 486

操作系统由多个功能模块组成。下列选项中，不需要专门硬件机制支持即可完成其核心功能的是（ ）。

A. 地址转换  
B. 进程调度  
C. 中断处理  
D. 设备驱动  

### 487

下列说法中，正确的是（ ）。

I. 使用系统调用读取一个已经打开的文件时，不需要再次提供文件名  
II. 用户模式下不可以直接执行特权 I/O 指令  
III. 中断处理过程不需要硬件参与  
IV. 采用多道程序设计技术本身即可提高系统交互能力

A. 仅 I、II  
B. 仅 II、III  
C. II、IV  
D. II、III、IV  

### 488

计算机区分核心态和用户态后，从核心态到用户态的转换由操作系统执行相应返回过程后完成，而用户态到核心态的转换则由（ ）完成。

A. 硬件  
B. 核心态程序  
C. 用户程序  
D. 中断处理程序  

### 489

“访管”指令（ ）使用。

A. 仅在用户态下  
B. 仅在核心态下  
C. 在规定时间内  
D. 在调度时间内  

### 490

按经典操作系统教材对“访管”指令的约定，CPU 处于核心态时，它可以执行的指令是（ ）。

A. 只有特权指令  
B. 只有非特权指令  
C. 只有“访管”指令  
D. 除“访管”指令外的全部指令  

### 491

下列关于库函数和系统调用的说法中，不正确的是（ ）。

A. 库函数通常运行在用户态，系统调用的服务代码运行在内核态  
B. 一般而言，普通库函数调用的边界开销小于系统调用  
C. 库函数不方便替换，而系统调用通常很方便被替换  
D. 一般而言，用户态库函数比内核中的系统调用服务代码更便于调试  

### 492

下列关于系统调用和一般过程调用的说法中，正确的是（ ）。

A. 两者都必须将当前 CPU 中的 PSW 和 PC 的值压栈，以保存现场信息  
B. 系统调用的被调用服务过程运行在内核态  
C. 一般过程调用的被调用过程一定运行在用户态  
D. 两者的调用过程与被调用过程一定都运行在用户态  

### 493

在计算机体系结构中，CPU 通过中断和异常处理机制响应硬件和软件产生的事件。为保证处理后能够正确恢复被打断的执行流，以下关于现场保护的描述中，正确的是（ ）。

A. 只需保存程序计数器和程序状态字，不需要保存可能被处理程序改写的通用数据寄存器和通用地址寄存器  
B. 除程序计数器和程序状态字外，还需要保护处理程序可能改写、而被中断程序又需要保持的通用寄存器现场  
C. 只需保存程序计数器，不需要保存程序状态字和通用寄存器  
D. 只需保存程序状态字，不需要保存程序计数器和通用寄存器  

## 操作系统结构

### 494

下列关于分层式结构操作系统的说法中，错误的是（ ）。

A. 各层之间的依赖或调用关系受到严格的层次约束  
B. 清晰的层间接口有利于在不破坏其他层接口契约的前提下替换某一层  
C. 具有非常灵活、可任意跨层的依赖关系  
D. 严格分层可能带来额外的路径开销，使系统效率降低  

### 495

对于以下五种服务，在采用微内核结构的操作系统中，（ ）不宜放在微内核中。

I. 进程间通信机制  
II. 低级 I/O  
III. 低级进程管理和调度  
IV. 中断和陷入处理  
V. 文件系统服务

A. I、II 和 III  
B. II 和 V  
C. 仅 V  
D. IV 和 V  

### 496

下列关于操作系统结构的说法中，正确的是（ ）。

I. 当前广泛使用的操作系统普遍采用严格的分层式结构  
II. “每一层只使用其底层所提供的功能和服务”是模块化结构的一般定义  
III. 微内核结构具有较清晰的服务边界和消息通信机制，适合支持分布式风格的系统组织  
IV. 微内核添加系统服务时通常不必修改核心内核，因此一定会使系统更高效

A. I 和 II  
B. I 和 III  
C. III  
D. III 和 IV  

## 系统引导与虚拟机

### 497

对于经典 BIOS/MBR 教材模型下的计算机操作系统引导，下列叙述错误的是（ ）。

A. BIOS 固件存储在非易失性固件存储器中，开机后首先取得控制  
B. 启动早期会进行硬件自检，严重硬件故障可能使启动中止  
C. 引导程序必须先把“完整操作系统”的全部组成部分一次性装入内存，系统才能开始启动  
D. 在经典 MBR 启动链中，可依据分区表定位后续启动阶段  

### 498

在经典 BIOS/MBR 教材模型中，计算机的启动过程为（ ）。

① 扫描磁盘分区表，加载分区引导记录（PBR）  
② 硬件自检，并保存相应配置信息  
③ 计算机加电，进入 BIOS 固件  
④ 读取主引导记录（MBR）  
⑤ 加载操作系统内核

A. ③④①②⑤  
B. ③②④①⑤  
C. ⑤④③②①  
D. ①②③④⑤  

### 499

在经典 x86 BIOS 教材模型中，计算机的启动过程为（ ）。

① CPU 加电后从复位入口开始执行  
② 进行操作系统引导  
③ 跳转并进入 BIOS 固件代码  
④ 初始化 BIOS 中断服务等早期运行环境  
⑤ 进行硬件自检

A. ①②③④⑤  
B. ①③⑤④②  
C. ①③④⑤②  
D. ①⑤④③②  

### 500

引导块（boot block）是用于启动操作系统的特殊块。以下关于其功能和作用的描述中，正确的是（ ）。

A. 引导块保存启动所需的引导代码或定位信息，并把控制权逐步转移给更完整的引导加载程序，由后者继续加载操作系统核心代码  
B. 引导块位于磁盘最后一个扇区，专门保存系统备份数据和错误修复工具  
C. 引导块负责管理系统文件权限并验证用户身份  
D. 引导块用于保存操作系统运行日志和错误信息  

### 501

虚拟机的实现离不开虚拟机管理程序（VMM）。下列关于 VMM 的说法中，正确的是（ ）。

I. 第一类 VMM 直接运行在硬件之上，其虚拟化路径通常比经通用宿主操作系统的第二类 VMM 更短  
II. 因为 VMM 上层要支持操作系统和应用程序，所以 VMM 的代码量通常必然大于一个完整操作系统  
III. VMM 可将一台物理机器虚拟化为多台相互隔离的虚拟机器  
IV. 第二类 VMM 为支持客户操作系统运行，其全部代码都必须运行在处理器最高特权级

A. I、II 和 III  
B. I、III  
C. I、III 和 IV  
D. I、II、III 和 IV  

### 502

下列关于虚拟机的说法中，正确的是（ ）。

A. 虚拟机等价于由一套独立物理硬件直接实现的机器，因此不需要 VMM 参与  
B. 客户操作系统执行敏感/特权操作时，可以绕过 VMM 对真实硬件状态直接生效  
C. 客户操作系统可以绕过 VMM 直接拥有并管理宿主机的全部物理 I/O 设备  
D. 多个运行不同操作系统的虚拟机可以在 VMM 仲裁下共享同一台物理机器的硬件资源  
