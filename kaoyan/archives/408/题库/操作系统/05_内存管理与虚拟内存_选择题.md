# 05｜内存管理与虚拟内存｜选择题

> 来源题号：581–633  
> 来源：用户提供的 `操作系统.pdf_by_PaddleOCR-VL-1.6(1).md`。  
> 本文件完整收录该范围全部题号。OCR 错误、隐藏前提和原题多解风险均已完成最终校订；答案与必要边界见对应《答案与解析》。

[查看答案与逐题解析](05_内存管理与虚拟内存_答案与解析.md)

## 连续分配、重定位与基本存储管理

### 581

下列关于内存保护的描述中，不正确的是（ ）。

A. 一个进程不能未被授权就访问另外一个进程的内存空间  
B. 内存保护可以仅通过操作系统（软件）来满足，不需要处理器（硬件）的支持  
C. 内存保护的方法有界地址保护和上下限地址保护  
D. 一个进程不能直接跳转到另一个进程的指令地址中  

### 582

按经典教材实现：单一连续分配采用装入时完成的静态地址处理，而固定分区、动态分区采用运行时重定位/保护，分页及请求分页采用运行时页表地址变换。下列需要硬件地址变换机构的是（ ）。

I. 单一连续分配  
II. 固定分区分配  
III. 页式存储管理  
IV. 动态分区分配  
V. 页式虚拟存储管理

A. I、III、V  
B. II、III、IV  
C. III、IV、V  
D. II、III、IV、V  

### 583

对于 4 种动态分区分配算法而言，下列说法错误的是（ ）。

A. 最佳适应算法最容易产生内存碎片，最坏适应算法优先使用大内存空间  
B. 首次适应算法给进程分配的空间不是最合适的  
C. 循环首次适应算法需要记录上一次访问的内存地址  
D. 无论采用何种空闲区数据结构，4 种算法的搜索与维护时间复杂度都完全一致  

### 584

在动态分区分配中，系统回收进程的主存空间并与相邻空闲区合并，下面的说法正确的是（ ）。

A. 空闲区合并只需考虑相邻的两个空闲区  
B. 首次适应算法不需要合并空闲区  
C. 最佳适应算法的空闲区合并比首次适应算法更容易  
D. 空闲区合并需要修改空闲区表或空闲区链表，可能会影响内存分配效率  

### 585

在动态分区分配方案中，某一进程完成后，系统回收其主存空间并与相邻空闲区合并。为此需修改空闲区表，造成空闲区数减少的情况是（ ）。

A. 无上邻空闲区也无下邻空闲区  
B. 有上邻空闲区但无下邻空闲区  
C. 有下邻空闲区但无上邻空闲区  
D. 有上邻空闲区也有下邻空闲区  

### 586

在程序链接时，静态链接的主要优点是（ ）。

A. 静态链接允许程序共享代码和数据  
B. 静态链接允许动态加载模块，可以在运行时动态地修改函数调用  
C. 静态链接减少了可执行文件的大小  
D. 静态链接执行速度可能更快  

### 587

下列关于程序装入的动态重定位方式的描述中，错误的是（ ）。

A. 系统将程序装入内存后，程序在内存中的位置可能发生移动  
B. 处理器必须为系统中的每个进程同时配置一只独占的物理重定位寄存器  
C. 被访问单元的物理地址应等于逻辑地址加上重定位寄存器的值  
D. 逻辑地址到物理地址的映射过程在进程执行时发生  

### 588

动态重定位的过程依赖于（ ）。

I. 可重定位装入程序  
II. 重定位寄存器  
III. 地址变换机构  
IV. 目标程序

A. I 和 II  
B. II 和 III  
C. I、II 和 III  
D. I、II、III 和 IV  

### 589

按经典**单处理器 Base/Relocation Register 动态重定位模型**，CPU 在任一时刻只执行一个进程，进程切换时由操作系统装入当前进程的重定位值。对这种实现，应（ ）。

A. 在处理器中设置一组当前运行进程使用的重定位寄存器  
B. 为系统中的每道程序同时配置一只独占的物理重定位寄存器  
C. 为每道程序同时配置两只独占的物理重定位寄存器  
D. 为每道程序的代码和数据分别永久配置一只物理重定位寄存器  

### 590

某具有请求调入和页面置换功能的存储器中，CPU 给出逻辑地址到其被转换为物理地址的这一段过程中，不会出现以下哪一种情况（ ）。

A. 缺页  
B. 访问权限错误  
C. 地址越界  
D. 内存溢出  

### 591

下列措施中，能加快虚实地址转换的是（ ）。

I. 增大快表（TLB）容量  
II. 让页表常驻内存  
III. 增大交换区

A. 仅 I  
B. 仅 II  
C. 仅 I、II  
D. 仅 II、III  

### 592

在动态分区分配存储管理中，不需要对空闲区链进行排序的分配算法是（ ）。

A. 首次适应法  
B. 最佳适应法  
C. 最差适应法  
D. 都不需要  

### 593

计算机系统采用动态分区内存管理方式，某一时刻内存中存在五个空闲区域，按地址由低至高排列为：110KB、430KB、230KB、180KB 和 500KB。地址分配指针指向内存地址的起始点，接下来依次有 216KB、420KB、115KB 和 428KB 共四个进程申请使用内存。以下算法中能够成功分配的是（ ）。

A. 首次适应算法  
B. 最佳适应算法  
C. 最坏适应算法  
D. 邻近适应算法  

### 594

某计算机系统按字节编址，采用动态分区存储管理方式，内存分配采用最佳适应算法。内存低 32MB 被操作系统占据，用户区大小为 55MB，初始为空。接下来一段时间里内存中进行了以下操作：进程 A 分配 15MB，进程 B 分配 30MB，进程 A 释放 15MB，进程 C 分配 8MB，进程 D 分配 6MB。完成上述操作后内存中最大的空闲分区大小是（ ）。

A. 15MB  
B. 7MB  
C. 9MB  
D. 10MB  

### 595

计算机系统按字节编址，采用可变分区分配存储管理方法，用空闲分区表管理空闲分区，分配内存采用首次适应算法，并将分区的高地址部分分配出去。内存低地址部分的 200KB 由操作系统占据，用户区从 200KB 开始，大小为 386KB，初始时为空。接下来一段时间里进行以下操作：进程 A 申请 80KB，进程 B 申请 56KB，进程 C 申请 120KB，进程 A 释放 80KB，进程 C 释放 120KB，进程 D 申请 156KB，进程 E 申请 81KB。完成这些操作后内存中最小空闲块的大小为（ ）。

A. 56KB  
B. 13KB  
C. 12KB  
D. 89KB  

### 596

在段式存储管理中，共享段表是用来实现（ ）。

A. 多个进程共享同一段代码或数据  
B. 多个进程共享同一段物理内存空间  
C. 多个进程共享同一段逻辑地址空间  
D. 多个进程共享同一段号  

### 597

以下存储管理方式中，不适合多道程序设计系统的是（ ）。

A. 单用户连续分配  
B. 固定式分区分配  
C. 可变式分区分配  
D. 分页式存储管理方式  

### 598

在分页存储管理中，主存的分配（ ）。

A. 以页框为单位进行  
B. 以作业的大小进行  
C. 以物理段进行  
D. 以逻辑记录大小进行  

### 599

在段式分配中，不考虑 TLB 等加速机构，CPU 每次从内存中取一次数据需要（ ）次访问内存。

A. 1  
B. 2  
C. 3  
D. 4  

### 600

在段页式分配中，不考虑 TLB 等加速机构，CPU 每次从内存中取一次数据需要（ ）次访问内存。

A. 1  
B. 2  
C. 3  
D. 4  

### 601

以下对分页存储管理方式的描述中，正确的是（ ）。

I. 在不使用快表（TLB）的分页存储管理方式中（不考虑多级页表），CPU 每次取指令都需要访问两次内存  
II. 分页存储管理方式不会产生外部碎片，但会产生内部碎片  
III. 分页存储管理方式对用户是不透明的  
IV. 分页存储管理方式不能采用静态重定位

A. I、II、IV  
B. I、IV  
C. 仅 I  
D. 全都正确  

### 602

在段页式存储管理中，地址映射表是（ ）。

A. 每个进程一张段表，两张页表  
B. 每个进程的每个段一张段表，一张页表  
C. 每个进程一张段表，每个段一张页表  
D. 每个进程一张页表，每个段一张段表  

### 603

操作系统采用分页存储管理方式，要求（ ）。

A. 每个进程拥有一张页表，且进程的页表驻留在内存中，页表起始地址存放在各自进程的 PCB 中，运行时被写入页表寄存器  
B. 每个进程拥有一张页表，但只有执行进程的页表驻留在内存中  
C. 所有进程共享一张页表，以节约有限的内存空间，但页表必须驻留在内存中  
D. 所有进程共享一张页表，只有页表中当前使用的页面必须驻留在内存中，以最大限度地节省有限的内存空间  

### 604

段页式存储管理汲取了页式管理和段式管理的长处，其实现原理结合了页式和段式管理的基本思想，即（ ）。

A. 用分段方法来分配和管理物理存储空间，用分页方法来管理用户地址空间  
B. 用分段方法来分配和管理用户地址空间，用分页方法来管理物理存储空间  
C. 用分段方法来分配和管理主存空间，用分页方法来管理辅存空间  
D. 用分段方法来分配和管理辅存空间，用分页方法来管理主存空间  

### 605

以下存储管理方式中，会产生内部碎片的是（ ）。

I. 分段虚拟存储管理  
II. 分页虚拟存储管理  
III. 段页式存储管理  
IV. 固定式分区管理

A. I、II、III  
B. III、IV  
C. 仅 II  
D. II、III、IV  

## 分页、虚拟内存、TLB 与页面置换

### 606

在某分页存储管理系统中，地址结构长 18 位，其中 11～17 位为页号，0～10 位为页内偏移量。若有一作业的第 0、1、2 页依次装入 2、3、7 号物理块，相对地址 1500 处有一条指令 `store r1, 2500`，则该指令所在页对应的物理块号、指令的物理地址、指令数据地址所在页对应的物理块号分别为（ ）。

A. 3、3548、3  
B. 2、5596、3  
C. 3、5596、7  
D. 2、3548、7  

### 607

一个进程的页表如下，页大小为 1024B。地址 2586（十进制）对应的物理地址是（ ）。

| 页号 | 0 | 1 | 2 | 3 |
| --- | ---: | ---: | ---: | ---: |
| 块号 | 20 | 30 | 10 | 80 |

A. 2586  
B. 10240  
C. 10778  
D. 31258  

### 608

在操作系统中，多级页表是一种将虚拟地址空间分级管理的方式。以下哪些是多级页表的优点？（ ）

I. 可以减少页表所占的连续内存空间  
II. 可以减少缺页中断次数  
III. 通过分级、按需建立下级页表，更适合管理很大的虚拟地址空间  
IV. 可以加快内存分配的速度  
V. 可以减少页面大小  
VI. 可以减少内存碎片

A. I、II、IV  
B. II、III、IV  
C. III、IV、VI  
D. I、III  

### 609

在采用页式存储管理的系统中，逻辑地址空间大小为 256TB，页表项大小为 8B，页面大小为 4KB，且**每一级页表本身恰占一个页面**。则该系统应采用（ ）级页表。

A. 2  
B. 3  
C. 4  
D. 5  

### 610

若对经典的页式存储管理方式的页表做出改造，允许不同页表的页表项指向同一个页帧，则可能的结果有（ ）。

I. 可以实现对可重入代码的共享  
II. 可以通过修改页表项让多个虚拟页暂时指向同一页帧，为共享映射或写时复制（COW）提供基础  
III. 容易发生越界访问  
IV. 可以实现进程间通信

A. I、II、IV  
B. II、III  
C. I、II、III  
D. 仅 I  

### 611

在采用页式虚拟存储管理和固定分配局部置换策略的系统中，数组采用行优先存储，页框大小为 512B，且 `int` 占 4B。某进程中有如下代码段（该代码段已提前读入内存）：

```c
int a[128][128];
for (int i = 0; i < 128; i++)
    for (int j = 0; j < 128; j++)
        a[j][i] = 0;
```

系统为该进程分配的数据区只有 1 个页框，则执行该代码会发生（ ）次缺页中断。

A. 1  
B. 2  
C. 128  
D. 16384  

### 612

假设某进程分配有 4 个页框，每个页框大小为 128 个字（一个整数占一个字）。进程的代码段正好可以存放在一页中，而且总是占用 0 号页框。数据会在其他 3 个页框中换进或换出，**数组 `a` 的首地址与页边界对齐**。数组按行优先存储，则执行下列代码会发生（ ）次缺页中断。

```c
int a[64][64];
for (int j = 0; j < 64; j++)
    for (int i = 0; i < 64; i++)
        a[i][j] = 0;
```

A. 32  
B. 1024  
C. 2048  
D. 64  

### 613

在配置了 TLB 的页式虚拟存储管理系统中，假设 TLB 命中率约为 75%，忽略访问 TLB 的时间，使用二级页表，**页表页均已驻留且不发生缺页**，则每次存取的平均主存访问次数是（ ）。

A. 1.25  
B. 1.5  
C. 1.75  
D. 2  

### 614

在请求分页系统的一次页面访问及必要的缺页/置换处理过程中，系统可能会执行以下哪些操作？（ ）

I. 将页从磁盘读入内存  
II. 保存当前进程的上下文信息  
III. 检查页表是否有效  
IV. 更新页面的访问计数器  
V. 将页框写回到磁盘  
VI. 更新页面的修改位

A. I、II、III  
B. I、II、IV  
C. II、III、IV、V  
D. I、II、III、IV、V、VI  

### 615

考虑页面置换算法，系统有 $m$ 个物理块供调度，初始时全空，页面引用串长度为 $p$，包含了 $n$ 个不同的页号。无论用什么算法，缺页次数不会少于（ ）。

A. $m$  
B. $p$  
C. $n$  
D. $\min(m,n)$  

### 616

以下说法中正确的有（ ）。

I. 由于进程某段时间只会使用到部分页面，所以虚拟存储中为了提高系统并发率，只调入进程的部分页面到内存  
II. 请求分页存储管理方式与基本分页存储管理方式中，页面大小都是相同的  
III. 段页式存储管理方式中逻辑地址空间的管理单位是段，物理地址空间的管理单位是页，一个进程一张段表，一个段一张页表  
IV. 虚拟存储器的大小只与内存和外存的容量和有关

A. I、II  
B. II、III  
C. 全部  
D. I、II、III  

### 617

在一个**无 TLB 的单级分页系统**中，页表存放在内存。假设该指令除取指外还需要访问一次内存操作数，且不发生缺页；访问一次内存耗时 100ns。则完成这条指令所需的内存访问时间为（ ）。

A. 100ns  
B. 200ns  
C. 300ns  
D. 400ns  

### 618

系统按字节编址，采用请求分页存储方式管理内存，页框分配与置换使用固定分配局部置换方式。系统对数组按行存放，一个 `int` 型数据大小 4B，页面大小 1600B，且**数组首地址与页边界对齐**。某进程有 10 个页框可以用于存放数组数据。若采用 FIFO 置换算法，下列两种初始化写法的缺页次数分别是（ ）。

```c
// 写法 1
int arr[200][400];
for (int i = 0; i < 200; ++i)
    for (int j = 0; j < 400; ++j)
        arr[i][j] = i * j;

// 写法 2
int arr[200][400];
for (int j = 0; j < 400; ++j)
    for (int i = 0; i < 200; ++i)
        arr[i][j] = i * j;
```

A. 200，400  
B. 400，200  
C. 200，80000  
D. 80000，200  

### 619

关于进程页表的页表项，基本分页存储管理方式和请求分页存储管理方式均须设立的字段为（ ）。

A. 状态位  
B. 访问字段  
C. 修改位  
D. 物理块号  

### 620

在请求分页存储管理中，若采用 FIFO 页面淘汰算法，则当可供分配的页帧数增加时，缺页中断的次数（ ）。

A. 减少  
B. 增加  
C. 无影响  
D. 可能增加也可能减少  

### 621

给一个进程分配 4 个页框，其页表如下。在时间 164 产生虚页号 4 缺页。分别按照 FIFO、LRU、CLOCK 策略，被置换出的页框号是（ ）。

| 虚页号 | 页框号 | 加载时间 | 引用时间 | 引用位 |
| ---: | ---: | ---: | ---: | ---: |
| 1 | 0 | 60 | 161 | 1 |
| 2 | 1 | 130 | 160 | 1 |
| 0 | 2 | 26 | 162 | 0 |
| 3 | 3 | 20 | 163 | 1 |

A. 0、1、2  
B. 3、2、0  
C. 1、2、3  
D. 3、1、2  

### 622

按经典教材口径，虚拟存储器容量同时受虚拟地址空间上限和可作为后备存储的主存、外存总容量约束。若主存为 1MB、可用于后备存储的外存为 400MB，虚拟地址为 32 位，则虚拟存储器的最大容量是（ ）。

A. 1MB  
B. 401MB  
C. $1\text{MB}+2^{32}\text{MB}$  
D. $2^{32}\text{B}$  

### 623

一台机器有 32 位虚拟地址和 16 位物理地址，若页面大小为 512B，采用单级页表，则页表共有（ ）个页表项。

A. $2^7$  
B. $2^{16}$  
C. $2^{23}$  
D. $2^{32}$  

### 624

在某分页存储管理系统中，逻辑地址为 16 位，页面大小为 1KB，第 0、1、2、3 号页依次存放在 3、7、11、10 号页框中，则逻辑地址 `0A6FH` 对应的物理地址为（ ）。

A. `1E6FH`  
B. `2E6FH`  
C. `DE6FH`  
D. `EE6FH`  

### 625

在页式虚拟存储管理中，设访问过程中**确实会发生页面置换**。在下列哪种访问规律下，FIFO 与 LRU 的淘汰顺序可以保持一致，从而得到相同的缺页轨迹？（ ）

A. 访问串中的页面访问顺序与页面进入驻留集的先后顺序一致，即最早放入驻留集的页面最早被再次访问，最后放入的页面最晚被再次访问  
B. 访问串中的页面访问顺序完全随机，没有任何规律  
C. 驻留集大小等于物理内存大小，即所有页面都可以同时放入物理内存  
D. 驻留集大小等于 2  

### 626

下列关于驻留集和工作集的表述中，正确的是（ ）。

I. 驻留集是进程已装入内存的页面的集合  
II. 工作集是某段时间间隔内进程实际访问页面的集合  
III. 工作集可以认为是驻留集的子集

A. I  
B. I、II  
C. II、III  
D. I、II、III  

### 627

有关颠簸（thrashing）的描述正确的有（ ）。

I. 颠簸时，CPU 利用率迅速下降  
II. 颠簸时，应该增加进程，使 CPU 利用率提高  
III. 颠簸时，通过挂起进程，可以缓解内存压力  
IV. 采用局部置换时，一个进程发生缺页所选择的 victim 限定在该进程自己的驻留集中，不会直接淘汰其他进程的页面

A. I、II  
B. II、III  
C. I、II、IV  
D. I、III、IV  

### 628

在采用请求分页式存储管理的经典教材模型中，使用 TLB 且页表只有一级，不考虑 Cache 和磁盘 I/O 时间。若 TLB 命中，直接访问目标数据；若发生缺页，则按“查页表发现缺页 → 装入后更新页表 → 重新执行访存并再次查页表 → 访问目标数据”计主存读写次数。从 CPU 给出逻辑地址到最终取得数据，最少和最多分别需要（ ）次主存访问。

A. 2，4  
B. 1，4  
C. 2，3  
D. 1，3  

### 629

在配置了 TLB 的页式虚拟存储管理系统中，若访问内存需 $1\mu s$，查询 TLB 需 $0.2\mu s$。已知 TLB 和内存串行访问，采用单级页表，**忽略缺页且页表项可正常从主存取得**。则 TLB 命中率为 85% 和 50% 时，平均访问时间分别是（ ）。

A. $1.5\mu s,\ 1.8\mu s$  
B. $1.35\mu s,\ 1.7\mu s$  
C. $1.6\mu s,\ 1.7\mu s$  
D. $1.35\mu s,\ 1.8\mu s$  

### 630

某计算机系统按字节编址，采用请求分页式存储管理方式，页表由寄存器保存，访问一次内存需 $100\mu s$。在系统发生缺页中断时，若不需要调出页面或调出页面未被修改、不需要写回磁盘，则缺页处理需要 9ms；若调出页面被修改过、需要写回磁盘，则缺页处理需要 21ms。若系统中有 80% 的被淘汰页面被修改过，要保证有效访问时间不超过 $286\mu s$，则缺页率不可以超过（ ）。

A. 3%  
B. 4%  
C. 2%  
D. 1%  

### 631

内存映射可以将一个文件映射到进程虚拟地址空间的某个区域，实现文件磁盘对象和进程虚拟地址空间之间的映射关系。下列说法中正确的是（ ）。

A. 内存映射文件是将整个文件内容一次性加载到内存中的一种方式  
B. 内存映射文件只适用于读取文件，不支持对文件进行写操作  
C. 内存映射文件可以通过修改内存中的数据来实现对文件的写操作  
D. 由于进程的虚拟地址空间是独立的，内存映射文件不支持多进程映射到同一文件  

### 632

已知系统为 32 位实地址，采用 48 位虚拟地址，页面大小为 4KB，页表项大小为 8B，且**每一级页表恰占一个 4KB 页面**。假设系统使用纯页式存储，则要采用（ ）级页表，页内偏移为（ ）位。

A. 3，12  
B. 3，14  
C. 4，12  
D. 1，14  

### 633

假定有一个请求分页存储管理系统，测得系统各相关设备的利用率为：CPU 利用率为 10%，磁盘交换区利用率为 99.7%，其他 I/O 设备利用率为 5%。下面哪些措施将可能改进 CPU 的利用率？（ ）

I. 增大内存的容量  
II. 增大磁盘交换区的容量  
III. 减少多道程序的度数  
IV. 增加多道程序的度数  
V. 使用更快速的磁盘交换区  
VI. 使用更快速的 CPU

A. I、II、III、IV  
B. I、III  
C. II、III、V  
D. II、VI  
