# 07｜I/O 与设备管理｜选择题

> 来源题号：660–695  
> 来源：用户提供的 `操作系统.pdf_by_PaddleOCR-VL-1.6(1).md`。  
> 本文件完整收录该范围全部题号。OCR/排版错误、旧教材实现前提和歧义均已完成最终校订；答案与必要边界见对应《答案与解析》。

[查看答案与逐题解析](07_IO与设备管理_答案与解析.md)

## 设备、控制器、DMA 与 I/O 软件层次

### 660

按经典操作系统教材对独占设备与共享设备的分类，下列叙述中正确的是（ ）。

A. 字符设备的基本特征是可寻址到字节，即能指定输入的源地址或输出的目标地址  
B. 共享设备必须是可寻址的和可随机访问的设备  
C. 共享设备是指同一时刻内允许多个进程同时访问的设备  
D. 在分配共享设备和独占设备时都可能引起进程死锁  

### 661

下列关于 I/O 端口和接口的说法中，正确的是（ ）。

A. 采用独立编址方式时，通过地址线来区别存储单元和 I/O 设备  
B. 采用独立编址方式时，需要设置专门的 I/O 指令访问端口  
C. 按照数据传送格式，接口可以分为同步传送接口和异步传送接口  
D. 采用统一编址方式时，通过指令格式来区别存储单元和 I/O 设备  

### 662

为了便于上层软件的编制，设备控制器通常需要提供（ ）。

A. 数据寄存器、状态寄存器和控制寄存器  
B. I/O 地址寄存器、工作方式状态寄存器和控制命令  
C. 中断寄存器、控制寄存器和控制命令  
D. 控制寄存器、编程空间和控制逻辑寄存器  

### 663

下列关于 DMA 方式的描述中，正确的是（ ）。

A. DMA 是一个专门负责输入/输出的处理机  
B. CPU 负责初始化传送参数，随后 DMA 控制器可在设备接口与内存之间承担数据块搬运；完成后再由中断等机制通知 CPU 做收尾处理  
C. CPU 通过程序的方式给出 DMA 可以解释的程序  
D. DMA 不需要 CPU 指出所取数据的地址与长度  

### 664

在计算机系统中，DMA 方式是一种用于批量数据传送的高效 I/O 机制。若系统具有多个可独立工作的 DMA 通道或控制资源，下列说法正确的是（ ）。

A. DMA 方式使用 CPU 的中断处理程序来实现数据传输  
B. DMA 方式可以同时进行多个设备的数据传输  
C. DMA 方式无须使用设备驱动程序  
D. DMA 方式不需要使用系统总线进行数据传输  

### 665

关于通道、设备控制器和设备之间的关系，以下叙述中正确的是（ ）。

A. 设备控制器和通道可以分别控制设备  
B. 对于同一组输入/输出命令，设备控制器、通道和设备可以并行工作  
C. 通道控制设备控制器，设备控制器控制设备工作  
D. 以上答案都不对  

### 666

将系统调用参数翻译成设备操作命令的工作由（ ）完成。

A. 用户层 I/O  
B. 设备无关的操作系统软件  
C. 中断处理  
D. 设备驱动程序  

### 667

一个典型的文本打印页面有 50 行，每行 80 个字符。假定一台标准打印机每分钟能打印 6 页，向打印机输出寄存器中写一个字符的时间很短，可忽略不计。若每打印一个字符都需要花费 $50\mu s$ 的中断处理时间（包括所有服务），使用中断驱动 I/O 方式运行这台打印机，中断的系统开销占 CPU 的百分比为（ ）。

A. 2%  
B. 5%  
C. 20%  
D. 50%  

### 668

假设采用**中断驱动 I/O 且不使用 DMA**。在接收和处理一个输入设备中断的过程中，下列哪项工作不是由中断入口硬件自动完成，而需要由中断处理/驱动软件显式完成？（ ）

A. 判断产生中断的类型  
B. CPU 模式由用户态切换到内核态  
C. 主机获取设备输入  
D. 保存用户程序的断点  

### 669

按经典同步 I/O 教材模型，程序直接控制方式采用 CPU 忙等，而中断方式和 DMA 方式在设备工作期间允许请求进程阻塞。下列会使请求进程进入阻塞态的是（ ）。

I. 程序直接控制  
II. 中断方式  
III. DMA 方式

A. II  
B. I、III  
C. II、III  
D. I、II、III  

### 670

在阻塞 I/O 中，当用户程序发起一个 I/O 请求后（ ）。

A. 请求进程会进入阻塞态，直到操作完成后被唤醒并重新进入可运行集合  
B. 用户程序可以继续执行其他任务，而不必等待操作完成  
C. 用户程序可以并行执行多个 I/O 操作  
D. 用户程序不需要关心操作是否成功完成  

### 671

按本题的**中断驱动程序 I/O 模型（不使用 DMA）**，一个进程请求 I/O 后进入阻塞，设备完成操作后通过中断通知 CPU。下列关于中断处理程序的说法中，错误的是（ ）。

A. 中断处理程序将设备控制器中的数据传送到内存缓冲区（读入），或将要输出的数据传送到设备控制器（输出）  
B. 对于不同的设备，有不同的中断处理程序  
C. 中断处理结束后，需要恢复 CPU 现场，此时一定会返回到被中断的进程  
D. I/O 操作完成后，驱动程序必须检查本次 I/O 操作中是否发生了错误  

## 缓冲、SPOOLing 与设备独立性

### 672

设从磁盘将一块数据传送到缓冲区所用的时间为 80 微秒，将缓冲区中的数据传送到用户区所用的时间为 40 微秒，CPU 处理一块数据所用的时间为 30 微秒。若有多块数据需要处理，并采用单缓冲区传送某磁盘数据，则处理一块数据所用的总时间为（ ）。

A. 120 微秒  
B. 110 微秒  
C. 150 微秒  
D. 70 微秒  

### 673

设备独立软件中的缓冲区管理与设备分配与回收之间有什么联系？（ ）

A. 缓冲区管理用于分配和管理设备所需的缓冲区，而设备分配与回收负责将设备分配给任务并在任务完成后回收设备  
B. 缓冲区管理是设备分配与回收的子功能，用于管理设备的输入和输出缓冲区  
C. 设备分配与回收是缓冲区管理的一部分，用于分配和回收设备所需的缓冲区  
D. 缓冲区管理和设备分配与回收是两个独立的功能，两者没有直接联系  

### 674

假设将一个数据块从外部设备传送到系统缓冲区的时间为 25 微秒，从系统缓冲区传送到用户区的时间为 40 微秒，CPU 处理一个数据块的时间为 40 微秒。总共需要读入和分析 2 个数据块。按经典单/双缓冲时间线计算，两种方案的总处理时间之差为（ ）。

A. 0 微秒  
B. 10 微秒  
C. 15 微秒  
D. 20 微秒  

### 675

某操作系统采用双缓冲区传送磁盘上的数据。设从磁盘将数据传送到缓冲区所用的时间为 $T_1$，将缓冲区中的数据传送到用户区所用的时间为 $T_2$（假设 $T_2$ 远小于 $T_1$），CPU 处理数据所用的时间为 $T_3$。假设一个磁盘块和一个缓冲区的大小相等，某系统在一段时间内连续处理大批数据，则处理一个磁盘块数据，系统平均所用的时间为（ ）。

A. $\max(T_1,T_2+T_3)$  
B. $\max(T_2,T_3)+T_1$  
C. $\max(T_1,T_3)+T_2$  
D. $\max(T_1,T_3)$  

### 676

若 I/O 所花费的时间比 CPU 的处理时间短得多，**仅从提升长期稳态吞吐率的角度看**，增加缓冲区通常（ ）。

A. 最有效  
B. 几乎无效  
C. 均衡  
D. 以上均错  

### 677

按教材对缓冲的广义定义（包括用于设备与软件之间速率、粒度和相位解耦的缓冲），考虑下列 I/O 操作，需要使用缓冲技术的是（ ）。

I. 图形用户界面下使用鼠标  
II. 多任务操作系统下的磁带驱动器（假设没有设备预分配）  
III. 包含用户文件的磁盘驱动器  
IV. 使用存储器映射 I/O、直接和总线相连的图形卡

A. I、III  
B. II、IV  
C. II、III、IV  
D. 全选  

### 678

下面关于 SPOOLing 系统的说法中，正确的是（ ）。

A. 构成系统的基本条件是有外围输入机与外围输出机  
B. 构成系统的基本条件仅是要有高速的大容量硬盘作为输入井和输出井  
C. 当输入设备忙时，系统中的用户程序暂停执行，待 I/O 空闲时再被唤醒执行输出操作  
D. 系统中的用户程序可以随时将输出数据送到输出井中，待输出设备空闲时再由系统完成数据的输出操作  

### 679

下面关于 SPOOLing 的叙述中，不正确的是（ ）。

A. SPOOLing 系统中不需要独占设备  
B. SPOOLing 系统加快了作业执行的速度  
C. SPOOLing 系统使独占设备变成共享设备  
D. SPOOLing 系统提高了独占设备的利用率  

### 680

当用户要求使用打印机打印某文件时，用户的要求是由操作系统的（ ）实现的。

A. 文件系统  
B. 设备管理程序  
C. 文件系统和设备管理程序  
D. 打印机启动程序和设备管理程序  

### 681

下列设备管理工作中，适合由设备独立性软件来完成的有（ ）。

I. 向设备寄存器写命令  
II. 检查用户是否有权使用设备  
III. 将二进制整数转换成 ASCII 码格式打印  
IV. 缓冲区管理

A. I、II、III  
B. II、III、IV  
C. II、IV  
D. I、III、IV  

### 682

按经典教材的 I/O 软件分层，将设备相关中断处理和磁盘请求调度归入设备驱动层。下列关于设备驱动程序的说法中，正确的是（ ）。

I. 设备驱动程序负责处理与设备相关的中断处理过程  
II. 驱动程序全部使用汇编语言编写，没有使用高级语言编写  
III. 设备驱动程序负责处理磁盘调度  
IV. 设备驱动程序与设备密切相关，可以在任意操作系统运行

A. II、III、IV  
B. I、III  
C. III、IV  
D. I、II、III  

### 683

按经典教材的 I/O 软件分层，下面不属于设备驱动程序核心职责的是（ ）。

A. 接收设备无关层下交的具体 I/O 请求和参数，并完成设备相关的必要检查  
B. 查询 I/O 设备的状态  
C. 发出 I/O 命令，启动 I/O 设备  
D. 对设备返回的数据进行应用层语义分析，并负责通用的用户态数据格式转换  

### 684

对设备驱动程序的处理过程进行排序，正确的处理顺序是（ ）。

① 对服务请求进行校验  
② 传送必要的参数  
③ 启动 I/O 设备  
④ 将抽象要求转化为具体要求  
⑤ 检查设备的状态

A. ①④⑤②③  
B. ④①⑤②③  
C. ①④②⑤③  
D. ④①②⑤③  

## 磁盘、格式化、调度与 SSD

### 685

磁盘上的文件以（ ）为单位读/写。

A. 块  
B. 记录  
C. 柱面  
D. 磁道  

### 686

按传统 BIOS/MBR 教材模型，硬盘上的操作系统引导扇区通常在（ ）阶段建立。

A. 对硬盘进行分区时  
B. 对硬盘进行低级格式化时  
C. 硬盘出厂时自带  
D. 对硬盘进行高级格式化时  

### 687

在对磁盘分区进行**高级（逻辑）格式化、建立文件系统**的过程中，以下哪种操作是必需的？（ ）

A. 删除所有数据  
B. 创建文件系统  
C. 分区  
D. 设置文件权限  

### 688

在下列有关旋转延迟的叙述中，不正确的是（ ）。

A. 若采用只决定柱面/磁道服务顺序、而不进行扇区级旋转优化的经典磁盘调度算法，则该算法本身不决定某一给定扇区的平均旋转等待  
B. 旋转延迟的大小取决于磁盘空闲空间的分配程序  
C. 旋转延迟的大小与文件的物理结构有关  
D. 扇区数据的处理时间对旋转延迟的影响较大  

### 689

假设磁盘有 256 个柱面、4 个磁头（盘面），每个磁道有 8 个扇区，编号均从 0 开始，并按**扇区号先递增；扇区用尽后磁头号递增；同柱面所有盘面用尽后柱面号递增**的顺序线性编号。文件 A 按该线性顺序连续存放。若其中一个块位于 5 号柱面、1 号磁头下的 7 号扇区，则下一块应存放在（ ）。

A. 5 号柱面、2 号磁头下的 7 号扇区  
B. 5 号柱面、2 号磁头下的 0 号扇区  
C. 6 号柱面、1 号磁头下的 7 号扇区  
D. 6 号柱面、1 号磁头下的 0 号扇区  

### 690

假设磁盘有 100 个柱面，每个柱面上有 8 个盘面/磁道，每个磁道有 8 个扇区，编号均从 0 开始，并按**扇区号 → 盘面号 → 柱面号**的顺序线性编号。文件 A 含有 6400 个逻辑记录，每个逻辑记录恰占一个扇区，并按该线性顺序连续存放。若第 0 个逻辑记录位于（0 号柱面、0 号盘面、0 号扇区），则（78 号柱面、6 号盘面、6 号扇区）中存放的是第（ ）个逻辑记录。

A. 5045  
B. 5046  
C. 5047  
D. 5048  

### 691

已知某磁盘转一周需要 $r$ 秒，平均寻道时间为 $T$ 秒，忽略等待目标起始扇区转到磁头下的额外旋转延迟。每个磁道可连续传输 $N$ 字节，因此完整传输一个磁道需一周时间 $r$。现以随机寻道方式读/写 $b$ 字节，并按每个磁道独立寻道后连续传完整磁道近似，则平均访问时间是（ ）。

A. $(r+T)b/N$  
B. $b/(NT)$  
C. $(b/N+T)r$  
D. $bT/N+r$  

### 692

假设磁盘有 1000 个磁道，编号从 0 到 999，当前磁头正在 734 号磁道，且向磁道号增大的方向移动。磁道请求依次为 164、845、911、165、788、432、396、700、25。采用 **SCAN（到达 999 端点后再反向）** 和 SSTF 算法完成上述请求时，磁头移动的距离（磁道数）分别是（ ）。

A. 1865，1543  
B. 1688，1738  
C. 1239，1131  
D. 1239，1738  

### 693

假定磁带的记录密度为 400 字符/英寸（1in=0.0254m），每条逻辑记录为 80 字符。按教材近似，**每条记录连同其后的块间隙一起计长**，块间隙为 0.4 英寸。现有 3000 个逻辑记录需要存储，存储这些记录约需要长度为（ ）的磁带，磁带利用率约为（ ）。

A. 1500 英寸，33.3%  
B. 1500 英寸，43.5%  
C. 1800 英寸，33.3%  
D. 1800 英寸，43.5%  

### 694

下列说法正确的是（ ）。

I. 当发现系统中存在抖动时，应更换一块更大的磁盘用于页面置换  
II. 内存分页管理方式不会产生外部碎片  
III. 磁盘访问时间主要由旋转时延和传输时延组成  
IV. FCFS 算法可用于实现磁盘调度

A. I、II  
B. III、IV  
C. II、IV  
D. I、III  

### 695

下列关于固态硬盘及磨损均衡的说法中，正确的是（ ）。

A. 固态硬盘因为写入机制复杂，所以整体性能通常弱于机械硬盘  
B. 相比机械硬盘，固态硬盘的主要优势只体现在连续存取，随机访问并无明显优势  
C. 静态磨损均衡会把长期不改动的冷数据也纳入搬迁与均衡，使全盘擦写次数分布通常比仅在动态可用块之间均衡更均匀  
D. 静态磨损均衡的含义是每次写入都直接选择长期存放冷数据的块覆盖写入  
