# 06｜文件系统｜选择题

> 来源题号：634–659  
> 来源：用户提供的 `操作系统.pdf_by_PaddleOCR-VL-1.6(1).md`。  
> 本文件完整收录该范围全部题号。OCR/排版错误、现代语义冲突和编号前提均已完成最终校订；答案与必要边界见对应《答案与解析》。

[查看答案与逐题解析](06_文件系统_答案与解析.md)

## 打开文件、目录与文件对象

### 634

按经典打开文件表模型，`open` 在完成路径解析后，主要需要建立文件的内存打开状态。下列描述最准确的是（ ）。

A. 将相应文件控制信息调入/关联到内存，并建立或取得打开文件表中的相应状态  
B. 把整个文件的数据内容复制到内存指定区域  
C. 只在存储介质上找到目录项即可，不需要建立任何内存打开状态  
D. 只在内存中搜索文件名，不需要解析目录或文件对象  

### 635

某用户程序发起 `open()` 系统调用，下列对该过程的描述中最准确的是（ ）。

A. 调用必然导致文件 I/O  
B. 调用的参数含有需要打开的文件的名称  
C. 调用完成后，系统打开文件表将增加一个表目  
D. 调用的参数的文件名不同时，必然会打开不同的文件实体  

### 636

文件系统中执行 `close` 的主要工作是（ ）。

A. 无条件把整个文件的全部最新数据立即写回磁盘  
B. 撤销当前打开引用，并在最后引用释放时按需更新、回收相应的内存打开状态和文件控制信息  
C. 无条件把整张空闲空间位示图立即写回磁盘  
D. 无条件把超级块的全部信息立即写回磁盘  

### 637

读文件操作的正确次序应该是（ ）。

I. 向设备驱动程序发出 I/O 请求，完成数据交换工作  
II. 按存取控制说明检查访问的合法性  
III. 根据目录项中该文件的逻辑和物理组织形式，将逻辑记录号转换成物理块号  
IV. 按文件描述符定位该文件对应的打开文件状态/文件控制信息

A. II、IV、III、I  
B. IV、II、III、I  
C. IV、III、II、I  
D. II、IV、I、III  

### 638

当文件被打开时，需要将磁盘索引节点拷贝到内存的索引节点。下列属于内存索引节点中有而磁盘索引节点中没有的内容是（ ）。

A. 访问计数值  
B. 文件物理地址  
C. 文件长度  
D. 文件类型  

### 639

下列选项中，（ ）**不属于传统文件系统内部的数据/元数据访问优化机制**。

A. 目录项分解  
B. 文件高速缓存  
C. 磁盘调度算法  
D. 异步 I/O  

## 文件组织、分配与索引计算

### 640

有一个含 10000 条记录的顺序文件。现建立一级索引顺序结构：把记录平均分成 $m$ 组，每组建立一个索引项；查找时**先顺序查索引项，再在目标组内顺序查找**，并假定目标位置等可能。选择最优的 $m$ 后，平均比较记录/索引项的次数约为（ ）。

A. 1000  
B. 10000  
C. 100  
D. 500  

### 641

设有一个包含 1000 个记录的索引文件，每个记录占用一个磁盘块。一个磁盘块可存放 10 个索引表目。建立索引时，为每一个数据块建立一个索引表目，则索引及其文件本身应占（ ）个磁盘块。

A. 1000  
B. 1001  
C. 1011  
D. 1111  

### 642

若文件的物理结构采用连续分配，则 FCB 中有关文件物理位置的信息应包括（ ）。

I. 首块地址  
II. 文件长度  
III. 索引表地址

A. 仅 I  
B. I、II  
C. II、III  
D. I、III  

### 643

文件系统采用两级索引分配方式。若每个磁盘块的大小为 1KB，每个盘块号占 4B，则该系统中单个文件的最大长度是（ ）。

A. 64MB  
B. 128MB  
C. 32MB  
D. 以上都错误  

### 644

文件系统为每个文件创建一张（ ），存放文件数据块的磁盘存放位置。

A. 打开文件表  
B. 位图  
C. 索引表  
D. 空闲盘块链表  

### 645

某 500 个盘块的文件的目录项已调入内存（若为索引分配，其索引块也在内存中）。若需要在文件中增加一块，下列分配方式中最坏情况下磁盘 I/O 次数最多的是（ ）。

A. 连续分配  
B. 隐式链接分配  
C. 显式链接分配  
D. 索引分配  

### 646

设有一个记录文件，采用隐式链接分配方式，逻辑记录的固定长度为 100B，在磁盘上存储时采用记录成组分解技术。盘块长度为 512B。若该文件的目录项已经读入内存，则对第 22 个逻辑记录完成修改后，共启动磁盘（ ）次。

A. 3  
B. 4  
C. 5  
D. 6  

### 647

在一个采用一级索引结构的文件系统中，磁盘块大小为 1KB。若某文件的大小为 1300B，则该文件需要占用的磁盘块数是（ ）。

A. 0  
B. 1  
C. 2  
D. 3  

### 648

某文件共有 8 个记录 $L_1\sim L_8$，采用隐式链接分配，每个记录及链接指针占一个磁盘块，主存中的磁盘缓冲区大小与磁盘块大小相等。假设文件目录已读入内存。为了在 $L_5$ 和 $L_6$ 之间插入一个记录 $L_x'$（已在内存中），需要进行的磁盘操作有（ ）。

A. 4 次读盘和 2 次写盘  
B. 5 次读盘和 1 次写盘  
C. 5 次读盘和 2 次写盘  
D. 4 次读盘和 1 次写盘  

### 649

某文件存放在 $N$ 个数据块中，假设管理文件所必需的文件控制块、索引块或索引信息都驻留在内存中，且**不计最终异步元数据写回，只统计为完成这次逻辑重排立即需要的磁盘 I/O**。那么若（ ），可以不立即读写任何磁盘块。

A. 采用连续分配，将最后一个数据块搬到文件头部  
B. 采用单级索引分配，将最后一个数据块插入文件头部  
C. 采用隐式链接分配，将最后一个数据块插入文件头部  
D. 采用隐式链接分配，将第一个数据块插入文件尾部  

### 650

某文件系统使用类似 Linux 的 inode 存储结构，文件块和磁盘块的大小都是 4KB，磁盘地址是 32 位。一个文件包含 10 个直接指针和 1 个一级间接指针，则这个文件所占用的数据磁盘块数目最多是（ ）块（不考虑索引块）。

A. 128  
B. 512  
C. 1024  
D. 1034  

### 651

一个文件系统中，其 FCB 占 64B，盘块大小为 1KB，采用一级目录，目录项按顺序存放。假定目录中有 3200 个目录项，查找目标文件时目录块初始均未缓存，且目标文件在目录中的位置等可能。则顺序查找一个文件平均约需要（ ）次磁盘块访问。

A. 50  
B. 54  
C. 100  
D. 200  

### 652

下面的说法中，错误的是（ ）。

I. 一个文件在同一系统中、不同的存储介质上的复制文件，应采用同一种物理结构  
II. 对一个文件的访问，常由用户访问权限和用户优先级共同限制  
III. 文件系统采用树形目录结构后，对于不同用户的文件，其文件名应该不同  
IV. 为防止系统故障造成系统内文件受损，常采用存取控制矩阵方法保护文件

A. II  
B. I、III  
C. I、III、IV  
D. 全选  

## 共享、链接、文件系统与空闲空间

### 653

若多个进程共享同一个文件 $F$，则下列叙述错误的是（ ）。

A. 各进程可以用“读”或“写”方式打开文件 $F$  
B. 在系统打开文件表中有多个表项包含 $F$ 的属性，每个表项对应一个打开文件的进程  
C. 各进程的用户打开文件表中关于 $F$ 的表项内容可能不同  
D. 进程关闭 $F$ 时，系统删除相应的用户打开文件表项；若此后已不存在任何指向该打开实例的打开引用，则相应系统级打开状态可以被回收  

### 654

设文件 $F_1$ 的当前引用计数为 1，先建立 $F_1$ 的硬链接文件 $F_2$，再建立 $F_1$ 的符号链接文件 $F_3$，然后删除 $F_2$，则此时文件 $F_1$、$F_3$ 的引用计数值分别是（ ）。

A. 1、1  
B. 1、2  
C. 1、0  
D. 2、2  

### 655

设文件 $F_1$ 的当前引用计数值为 1，先建立 $F_1$ 的硬链接文件 $F_2$，再建立 $F_2$ 的符号链接文件 $F_3$。现有两个进程 $P_1$ 和 $P_2$ 分别打开了 $F_1$ 和 $F_2$，则下列说法中正确的是（ ）。

A. $F_1$ 与 $F_2$ 是指向同一文件对象（同一 inode/FCB 身份）的两个硬链接名称  
B. 进程 $P_1$ 和 $P_2$ 对 $F_1$ 必然具有完全相同的访问权限  
C. 若删除符号链接文件 $F_3$，则目标文件对象的硬链接计数必然减 1  
D. 进程 $P_1$ 已经打开 $F_1$ 后，每次 `read` 都仍必须重新提供 $F_1$ 的绝对路径  

### 656

下列关于文件系统的说法中，正确的是（ ）。

A. 一个文件系统可以存放的文件数量受限于文件控制块的数量  
B. 一个文件系统的容量一定等于承载该文件系统的磁盘容量  
C. 一个文件系统中单个文件的大小只受磁盘剩余空间大小的限制  
D. 一个文件系统不能将数据存放在多个磁盘上  

### 657

按传统 BIOS/MBR 磁盘布局，硬盘的主引导扇区（ ）。

A. 包含引导记录  
B. 包含分区表和主引导程序  
C. 只包含主引导程序  
D. 只包含分区表  

### 658

若用 8 个字（字长 32 位）组成的位示图管理内存，**内存块号从 0 开始编号**，位示图中的字号和位号均从 1 开始。假定用户归还块号为 100 的内存块，则它对应位示图的位置为（ ）。

A. 字号为 3，位号为 5  
B. 字号为 4，位号为 4  
C. 字号为 3，位号为 4  
D. 字号为 4，位号为 5  

### 659

某计算机字长 32 位，使用位示图管理磁盘空间。某系统磁盘共有 $2^{32}$ 块，每个磁盘块容量为 1KB。若位示图存放在从第 0 号块开始的连续磁盘块中，则最后一个存放位示图的磁盘块号是（ ）。

A. $2^{19}-1$  
B. $2^{19}$  
C. $2^{22}$  
D. $2^{22}-1$  
