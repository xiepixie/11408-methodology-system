---
type: exam-source
exam_id: 408-2026
exam_profile: 408
year: 2026
status: ready
total_score: 150
question_count: 47
metadata_file: exam.json
---

# 2026 年全国硕士研究生招生考试

## 计算机学科专业基础综合

## 一、单项选择题

> 第 1～40 小题，每小题 2 分，共 80 分。下列每小题给出的四个选项中，只有一个选项是最符合题目要求的。

### 1

当存储空间有足够的空闲空间时，在保持表内元素顺序相对不变的情况下，下列操作中，必然导致产生元素移动的是（ ）。

I. 表头插入一个元素  
II. 表头删除一个元素  
III. 表尾插入一个元素  
IV. 表尾删除一个元素  

A. 仅 I、II  
B. 仅 I、III  
C. 仅 II、IV  
D. 仅 III、IV

### 2

设有一个双向链表 `L`，结点结构为 `[p2, p1]`，头指针为 `head`。初始时 `cu = head`。现要将每个结点的 `p2` 指向 `p1` 所指结点的直接后继，应该执行的操作是（ ）。

A. `while (cu != NULL) { cu->p2 = cu->p1->p1; cu = cu->p1; }`  
B. `while (cu != NULL && cu->p2 != NULL) { cu->p2 = cu->p1->p1; cu = cu->p1; }`  
C. `while (cu != NULL) { if (cu->p1 != NULL) { cu->p2 = cu->p1->p1; cu = cu->p1; } }`  
D. `while (cu != NULL) { if (cu->p1 != NULL) { cu->p2 = cu->p1->p1; } else { cu->p2 = NULL; } cu = cu->p1; }`

### 3

已知二叉树 $T$ 的中序遍历序列为 b, e, d, f, c, a, g，层序遍历序列为 a, b, g, c, d, e, f，则其后序遍历序列是（ ）。

A. c, e, d, f, b, g, a  
B. c, e, f, d, b, g, a  
C. e, f, d, c, b, g, a  
D. e, g, f, d, b, c, a

### 4

森林 $F$ 中有 5 棵树，其结点个数分别为 2、3、4、5、7。若森林中树的排列次序可以任意，则 $F$ 转换得到的二叉树的最小高度是（ ）。

A. 5  
B. 6  
C. 8  
D. 10

### 5

假设二叉树中各叶结点的权值分别为 $a=1, b=2, c=4, d=5, e=8, f=10, g=12$。当该树的带权路径长度（WPL）最小时，与结点 $e$（权值 8）处于相同深度的结点是（ ）。

A. 仅 d  
B. 仅 g  
C. 仅 d、f  
D. 仅 f、g

### 6

有向图 $G=(V, E)$ 采用邻接表存储，求某一指定顶点的入度，其时间复杂度为（ ）。

A. $O(|V|)$  
B. $O(\min(|V|, |E|))$  
C. $O(|E|)$  
D. $O(\max(|V|, |E|))$

### 7

设有向图 $G=(V, E)$，其中顶点集 $V$ 的大小为 $n=|V|$，每条边 $e\in E$ 都标记有一个字符（不同边可标记相同字符）。定义字符串集合 $S$ 为：由 $G$ 中任意一条路径（路径可包含单个顶点，对应空字符串）上的边标记按顺序拼接而成的字符串的集合。下列说法中，错误的是（ ）。

A. 若 $G$ 无环，则 $S$ 是有限集  
B. 若 $G$ 无环，则 $S$ 中存在长度为 $n$ 的字符串  
C. 若 $G$ 有环，则 $S$ 中存在长度大于 $n$ 的字符串  
D. 若 $G$ 有环，则 $S$ 中存在长度小于 $2n$ 的字符串

### 8

已知平衡二叉树（AVL 树）中任意结点的左右子树高度差的绝对值不超过 1，且左右子树均为平衡二叉树。若某平衡二叉树的高度为 4（根结点的高度记为 1），则其根结点的左右子树的结点数之差最大为（ ）。

A. 1  
B. 2  
C. 3  
D. 5

### 9

使用直接插入排序算法对下列序列进行升序排序，关键字比较次数最少的是（ ）。

A. 30, 27, 56, 41, 80, 95, 69  
B. 31, 43, 26, 55, 63, 99, 77  
C. 61, 84, 51, 23, 34, 91, 40  
D. 93, 32, 48, 81, 50, 21, 72

### 10

现有 $n$ 名学生的成绩记录，每位学生的记录包含两门课程的成绩：课程 1（记为 $C_1$）和课程 2（记为 $C_2$）。排序规则如下：首先依据 $C_1$ 成绩升序排列；若两名学生的 $C_1$ 成绩相同，则依据其总分（即 $C_1 + C_2$）升序排列。从下列排序算法中，最适合实现上述需求的是（ ）。

A. 基数排序  
B. 快速排序  
C. 希尔排序  
D. 简单选择排序

### 11

在外部排序的 $k$ 路平衡归并过程中，归并趟数为 $d$。下列关于 $k$、$d$、初始归并段及内存大小的叙述中，正确的是（ ）。

I. $k$ 越大，$d$ 越小  
II. 初始归并段数不影响 $d$  
III. 内存大小限制初始归并段的最大长度  

A. 仅 I  
B. 仅 I、II  
C. 仅 I、III  
D. 仅 II、III

### 12

下列关于计算机系统层次结构的叙述中，错误的是（ ）。

A. 最上层是应用程序软件层  
B. 指令集体系结构（ISA）是软件与硬件的交界面  
C. 计算机组成（微架构）属于指令集体系结构的物理实现层  
D. 操作系统可通过 ISA 进行抽象，向上层软件提供系统调用服务

### 13

对 8 位机器数 `1010 0110B` 先执行算术右移 3 位，再执行算术左移 2 位，最终得到的机器数是（ ）。

A. `1101 0000B`  
B. `1101 0011B`  
C. `0101 0000B`  
D. `0101 0011B`

### 14

已知采用 IEEE 754 单精度浮点数格式表示浮点型变量，采用就近舍入模式（中间值向偶数舍入）。若浮点型变量 $x$ 的十进制值为 12.1，则 $x$ 的 IEEE 754 机器数用十六进制表示是（ ）。

A. `4141 9999H`  
B. `4141 999AH`  
C. `41E0 CCCCH`  
D. `41E0 CCCDH`

### 15

用 8 个 64M×8 bit 的 DRAM 芯片按低位交叉编址方式构成主存储器，并与一个宽度为 64 bit 的存储器总线相连。主存每次最多读写 64 bit，且按字节编址。下列地址中，与主存物理地址 `0018 001DH` 位于同一个 DRAM 芯片中的是（ ）。

A. `0000 01D5H`  
B. `000F A020H`  
C. `0018 001EH`  
D. `0101 0011B`

### 16

下列选项中，不属于指令集体系结构（ISA）规定内容的是（ ）。

A. 输入输出（I/O）指令格式与寻址方式  
B. 向量中断的处理机制与入口地址  
C. 虚拟存储管理的地址转换格式  
D. 指令流水线是否采用超流水线技术

### 17

下列指令在执行过程中，可能不改变程序下一条执行指令地址的是（ ）。

I. 条件转移指令  
II. 过程调用指令  
III. 陷入指令  
IV. 过程返回指令  

A. 仅 I、II  
B. 仅 I、IV  
C. 仅 II、III  
D. 仅 II、IV

### 18

某计算机按字节编址，数据 Cache 共有 1024 行，采用 4 路组相联映射，主存块大小为 32 B。若访问主存十进制地址为 1028 的 4 字节数据，则该数据所在主存块对应的 Cache 组号是（ ）。

A. 4  
B. 16  
C. 32  
D. 64

### 19

某计算机按字节编址，虚拟地址为 16 位，页大小为 256 B，主存页表项中包含装入位（P）、页框号（PPN）等字段。TLB 采用 4 路组相联映射，共有 16 个页表项，TLB 表项包含标记（Tag）、有效位（V）、页框号（PPN）等字段。在主存页表与 TLB 同步后，若主存页表中页号 22 对应的页表项中 $P=0, \text{PPN}=2\text{AH}$，则下列不可能出现在组号为 2 的 TLB 表项中的是（ ）。

A. $\text{Tag}=05\text{H}, V=1, \text{PPN}=1\text{CH}$  
B. $\text{Tag}=06\text{H}, V=1, \text{PPN}=2\text{AH}$  
C. $\text{Tag}=16\text{H}, V=0, \text{PPN}=2\text{AH}$  
D. $\text{Tag}=1\text{AH}, V=0, \text{PPN}=1\text{CH}$

### 20

在不考虑异常、中断处理和访存额外开销的理想情况下，下列关于处理器数据通路结构与 CPI 之间关系的叙述中，正确的是（ ）。

I. 单周期数据通路计算机的 CPI 等于 1  
II. 多周期数据通路计算机的 CPI 大于 1  
III. 指令流水线数据通路计算机的理想 CPI 等于 1  

A. 仅 I、II  
B. 仅 I、III  
C. 仅 II、III  
D. I、II、III

### 21

在 I/O 子系统中，设备驱动程序和中断服务程序直接控制外设与主机之间的数据传输，这一过程需要执行特权指令。下列指令中，不属于特权指令的是（ ）。

A. I/O 指令  
B. 关中断指令  
C. 中断返回指令  
D. 系统调用指令

### 22

在中断控制 I/O 方式下，实现 I/O 操作需要硬件和软件协同完成。在中断响应和处理过程中所包含的下列工作中，必须由硬件直接完成的是（ ）。

A. 开中断  
B. 中断服务程序寻址  
C. 保存断点（PC）  
D. 保存通用寄存器内容

### 23

下列操作系统程序或操作中，必须在内核态（内核模式）下执行的是（ ）。

A. 编译程序  
B. 链接程序  
C. 装入程序  
D. 命令解释程序

### 24

在支持虚拟存储器系统的指令执行过程中，下列叙述中正确的是（ ）。

A. 虚拟地址到物理地址的转换由操作系统完成  
B. 页表项的内容由编译器在编译时确定  
C. 缺页中断由硬件直接完成调页处理  
D. 缺页异常由操作系统负责响应和处理

### 25

下列关于进程与线程的叙述中，正确的是（ ）。

A. 内核级线程和用户级线程均由操作系统内核负责创建  
B. 多个内核级线程可以映射到一个用户级线程  
C. 同一个进程下的多个内核级线程共享该进程的用户栈  
D. 同一个进程下的所有线程共享该进程的堆空间

### 26

系统中有 8 个并发进程，执行题 26 图所示的操作，互斥信号量 S 的初始值为 5。若当前时刻 S 的值为 -2，其中 $m$ 表示当前正在访问资源的进程个数，$n$ 表示当前处于阻塞队列中的进程个数，则 $m$ 和 $n$ 的值分别是（ ）。

![[assets/q26_fig1.svg]]

A. 5, 2  
B. 5, 1  
C. 6, 2  
D. 7, 1

### 27

假设进程 $P$ 的读变量集合与写变量集合分别是 $R(P)$ 和 $W(P)$，进程 $Q$ 的读变量集合与写变量集合分别是 $R(Q)$ 和 $W(Q)$。则进程 $P$ 和 $Q$ 并发执行不会发生竞争错误的充要条件（Bernstein 条件）是（ ）。

I. $R(Q) \cap W(P) = \emptyset$  
II. $R(P) \cap R(Q) = \emptyset$  
III. $W(P) \cap W(Q) = \emptyset$  
IV. $R(P) \cap W(Q) = \emptyset$  

A. 仅 I、II  
B. 仅 I、II、III  
C. 仅 I、III、IV  
D. 仅 II、III

### 28

若 64 位系统采用三级虚拟分页存储管理方式，其虚拟地址结构如题 28 图所示。则第三级页表所占用的页框数最多是（ ）。

```text
| 补充位（25 位） | 一级页表（9 位） | 二级页表（9 位） | 三级页表（9 位） | 页内偏移（12 位） |
```

A. 1  
B. 256  
C. 256K  
D. 256M

### 29

下列方法或机制中，能够有效降低系统平均访存时间的是（ ）。

I. 引入快表（TLB）  
II. 采用多级页表  
III. 引入工作集模型指导页面置换  
IV. 引入页表缓冲队列  

A. 仅 I、III  
B. 仅 II、III  
C. 仅 I、III、IV  
D. 仅 I、II、IV

### 30

进程 P1 和 P2 共享同一个文件 R，该文件在两进程中对应的页表项分别是 R1 和 R2，其在两进程中的虚拟地址分别是 W1 和 W2。则下列说法中正确的是（ ）。

A. 页表项 R1 和 R2 的内容完全不同  
B. W1 和 W2 映射到的物理地址相同  
C. 进程 P1 对 W1 的写入修改不会影响进程 P2 对 W2 的读取  
D. W1 和 W2 的虚拟地址数值必须相同

### 31

下列关于设备驱动程序的叙述中，错误的是（ ）。

A. 设备驱动程序是硬件与操作系统内核之间的接口程序  
B. 驱动程序通常需要根据具体硬件特性定制开发  
C. 操作系统需为同类设备驱动程序定义统一的抽象接口  
D. 字符设备与块设备在操作系统中采用完全相同的 I/O 控制方式

### 32

下列操作中，由鼠标设备中断处理程序完成的是（ ）。

A. 解析鼠标输入事件的高层业务含义  
B. 将鼠标数据直接同步写入用户应用程序缓冲区  
C. 将数据从鼠标硬件设备传输到设备控制器的数据寄存器  
D. 将数据从设备控制器的数据寄存器读取并传输到内核缓冲区

### 33

下列关于计算机网络分层体系结构的叙述中，错误的是（ ）。

A. 每一层都有明确独立的功能边界和服务定义  
B. 划分的层次越多，网络传输的整体效率越高  
C. 分层有利于各层协议和技术的独立演进与更新  
D. 上层协议无需了解下层协议的具体实现细节

### 34

若在信道带宽为 200 kHz、信噪比 $S/N=1023$ 的信道上发送一个长度为 1500 B 的数据分组，则发送该分组所需的最小传输时延是（ ）。

A. 1 ms  
B. 2 ms  
C. 3 ms  
D. 6 ms

### 35

假设采用 CSMA/CA 协议的 IEEE 802.11 无线局域网，其数据传输速率为 300 Mbps，$\text{DIFS} = 128\ \mu\text{s}$，$\text{SIFS} = 28\ \mu\text{s}$。忽略除数据帧外的其他控制帧传输时延及信号传播时延。主机 H 发送一个总长度为 1500 B 的数据帧，则从开始发送数据帧到确认接收方已正确接收所需的最少时间是（ ）。

A. 40 μs  
B. 68 μs  
C. 168 μs  
D. 196 μs

### 36

支持 VLAN 划分的以太网交换机已按端口划分为两个 VLAN。VLAN 划分配置及各端口所连主机的 MAC 地址如题 36 图所示。下列具有不同目的 MAC 地址（DA）和源 MAC 地址（SA）的以太网帧 F1～F4 中，主机 H3 会接收到的是（ ）。

- F1: （DA）`00-1A-2B-3C-4D-03`；（SA）`00-1A-2B-3C-4D-01`  
- F2: （DA）`00-1A-2B-3C-4D-04`；（SA）`00-1A-2B-3C-4D-05`  
- F3: （DA）`FF-FF-FF-FF-FF-FF`；（SA）`00-1A-2B-3C-4D-02`  
- F4: （DA）`00-1A-2B-3C-4D-06`；（SA）`00-1A-2B-3C-4D-03`  

![[assets/q36_fig1.svg]]

A. 仅 F2、F4  
B. 仅 F1、F3  
C. 仅 F1、F2  
D. 仅 F3、F4

### 37

某网络在 $t_0$ 时刻的网络拓扑与路由器 $R_1$ 的路由表如题 37 图所示。$R_1\sim R_4$ 为路由器，基于链路状态路由算法进行路由计算。$S_0\sim S_4$ 为路由器 $R_1$ 的接口，链路上的数值为链路开销。若在 $t_1(t_1>t_0)$ 时刻，$R_1$ 检测到 $R_1$ 与 $R_2$ 之间的链路断开，则 $R_1$ 重新计算路由并进行充分路由聚合后，表中路由条目的数量为（ ）。

![[assets/q37_fig1.svg]]

题 37 图中 $t_0$ 时刻的 $R_1$ 路由表为：

| 目的网络 | 接口 |
|---|---|
| `199.10.20.0/27` | S1 |
| `199.10.20.32/27` | S2 |
| — | — |
| `199.10.20.128/25` | S3 |
| `199.10.21.125/32` | S4 |
| `0.0.0.0/0` | S0 |

A. 3  
B. 4  
C. 5  
D. 6

### 38

下列路由协议中，支持将一个自治系统划分为多个区域的内部网关协议（IGP）是（ ）。

I. OSPF  
II. RIP  
III. BGP  

A. 仅 I  
B. 仅 II  
C. 仅 I、III  
D. 仅 II、III

### 39

若将 IP 网络 `123.4.4.0/22` 划分为规模相等的 32 个子网，则 IP 地址 `123.4.5.11` 所在的子网网络地址是（ ）。

A. `123.4.4.0/27`  
B. `123.4.4.32/27`  
C. `123.4.5.0/27`  
D. `123.4.5.32/27`

### 40

下列应用场景中，不属于 HTTP Cookie 技术典型用途的是（ ）。

A. 网站用户会话跟踪  
B. 个性化内容推荐  
C. 电子商务网站虚拟购物车状态维护  
D. 缩短 Web 对象的网络传输与响应时间

---

## 二、综合应用题

> 第 41～47 小题，共 70 分。

### 41（本题 13 分）

假定二叉搜索树使用二叉链表存储，结点的数据类型定义如下：

```c
typedef struct BSTNode {
    int data;
    struct BSTNode *left, *right;
} BSTNode;
typedef BSTNode BTNode;
```

给定一棵二叉搜索树 $T$ 和整数 $K$，查找树中关键字与 $K$ 之差的绝对值最小的所有结点，并输出该绝对值与结点中的关键字。要求：

1. 给出算法的基本设计思想。（4 分）
2. 根据设计思想，采用 C 或 C++ 语言描述算法，关键之处给出注释。（7 分）
3. 说明你所设计算法的时间复杂度和空间复杂度。（2 分）

### 42（本题 10 分）

栈的基本操作有入栈（Push）和出栈（Pop）。将序列 $1, 2, 3, \ldots, n$ 依次进栈，请回答下列问题：

1. 当 $n=9$ 时，可以得到出栈序列 $\{2, 3, 1, 6, 4, 7, 5, 8, 9\}$ 吗？可以得到出栈序列 $\{2, 3, 1, 4, 6, 5, 7, 8, 9\}$ 吗？（2 分）
2. 假设 $1, 2, \ldots, n$ 的任意合法出栈序列为 $P_1, P_2, \ldots, P_n$。若序列中存在下标 $i < j < k$，导致该序列无法由合法的栈操作产生，则元素 $P_i, P_j, P_k$ 之间的大小关系是什么？（2 分）
3. 若 $n=4$，则以 2 开头的合法出栈序列共有多少个？（2 分）
4. 若 $n=k-1$ 时合法出栈序列总共有 $M$ 个。当 $n=k$ 时，以 1 开头的合法出栈序列有多少个？以 2 开头的合法出栈序列有多少个？总共的合法出栈序列有多少个？（4 分）

### 43（本题 10 分）

某 16 位计算机按字节编址，通用寄存器 R0～R15 的编号为 0～15，存储器地址为 16 位，采用 16 位定长指令字，指令格式有 R 型、I 型、M 型三种，如题 43 表 1 所示。

题 43 表 1：指令格式说明

| 类型 | 指令编码格式 | 功能说明 |
| ----- | ------------------------------ | ------------------------- |
| **R 型** | `0000` \| `rt` (4位) \| `rs/num` (4位) \| `OP1` (4位) | 加指令：`R[rt] <- R[rt] + R[rs]`<br>左移指令：`R[rt] <- R[rt] << num` |
| **I 型** | `OP2` (4位) \| `rt` (4位) \| `imm8` (8位) | 加立即数指令：`R[rt] <- R[rt] + imm8` |
| **M 型** | `OP3` (4位) \| `offset` (12位) | 取数指令：`R[0] <- M[R[15] + offset]`<br>存数指令：`M[R[15] + offset] <- R[0]` |

其中：
- `OP1` 为 `0001`、`0010` 分别表示加指令、左移指令；
- `OP2` 为 `0100` 表示加立即数指令；
- `OP3` 为 `1110`、`1111` 分别表示取数指令、存数指令；
- `R[r]` 表示寄存器 `r` 中的内容；`mm` 为移位位数；`M[addr]` 表示存储器地址 `addr` 中的内容。

请回答下列问题：

1. 主存单元和通用寄存器的宽度各为多少位？（2 分）
2. `OP1` 和 `OP2` 的编码是否可以相同？`OP2` 和 `OP3` 的编码是否可以相同？（2 分）
3. 若 `R[2] = 0xABCD`, `R[9] = 0xF001`，则指令机器码 `0000 0010 1001 0001` 执行后，寄存器 R2 和 R9 中的内容分别是什么？（2 分）
4. 若变量 $x, y$ 均为 16 位带符号整数，在存储器中依次从低地址向高地址连续存放，$x$ 的地址保存在 R15 中。实现计算 $y = 16x - 5$ 的 4 条指令 11～14 如题 43 表 2 所示，写出 ①～④ 处的内容。（4 分）

题 43 表 2：程序指令序列

| 指令地址 | 二进制机器码 |
|---|---|
| 11 | ① `0000 0000 0000` |
| 12 | `0000` ② `0010` |
| 13 | `0100 0000` ③ |
| 14 | `1111` ④ |

![[assets/q43_fig1.svg]]

### 44（本题 13 分）

假定题 43 中计算机 C 的部分数据通路如题 44 图所示。

图中带箭头虚线代表控制信号，`IR.rt`、`IR.rs` 分别表示 IR 中的 rt、rs 字段，$IR_{11\sim 0}$ 为 IR 的低 12 位，要求取指令周期完成 PC 增量操作。请回答下列问题：

1. 图中 ① 和 ② 属于同一类部件，其名称是什么？（1 分）
2. I 型指令中 `imm8` 可以是带符号或无符号整数，M 型指令中 `offset` 是带符号整数，则控制信号 EXTOP 至少有几位？为什么？（2 分）
3. 取指周期中控制信号 `MARSrc`、`ALUASrc`、`ALUBSrc`、`RegWr` 的取值各是什么？（4 分）
4. 左移指令执行周期中控制信号 `ALUBSrc`、`RegWSrc`、`RegDst`、`RegWr` 的取值各是什么？EXTOP 是否可以与 M 型指令中的 EXTOP 相同？为什么？（6 分）

![[assets/q44_fig1.svg]]

### 45（本题 7 分）

系统采用优先级（优先级数值越大表示优先级越高）与时间片轮转调度算法，仅当发生时钟中断时才触发抢占 CPU 操作，时钟中断间隔为 10 ms。进程首次进入就绪队列时，其时间片为 50 ms。若进程因时间片用完而返回就绪队列，其优先级数值减 1；若进程被更高优先级进程抢占而返回就绪队列，其优先级数值保持不变。当多个进程优先级相同时，先进入就绪队列的进程优先被调度。四个进程的到达时刻、初始优先级与 CPU 运行时间如下表所示：

| 进程 | 到达就绪队列时间（ms） | 优先级 | CPU 运行时间（ms） |
|---|---|---|---|
| P1 | 10 | 3 | 95 |
| P2 | 10 | 4 | 20 |
| P3 | 12 | 2 | 40 |
| P4 | 14 | 5 | 60 |

请回答下列问题：

1. 从 10 ms 开始进程调度，直至所有进程执行结束，此时时钟中断次数与 CPU 调度次数分别为多少？P1、P2、P3、P4 各自的首次调度发生在哪个时刻？（5 分）
2. 若时间片由 50 ms 改为 100 ms，CPU 调度次数将增大、不变还是减少？若时钟中断间隔由 10 ms 改为 1 ms，系统开销将增大、不变还是减少？（2 分）

### 46（本题 8 分）

文件系统的目录项包括文件名和索引节点号。磁盘包含索引节点表、位示图、目录、文件数据等元数据。若盘块大小为 4 KB，盘块号占 4 B，索引节点表存放了系统的所有文件，从 0 开始编号，存放在从盘块号 100 开始连续的 4096 个盘块中。每个索引节点占用 128 B，包含直接地址项 5 个，一级间接地址项、二级间接地址项、三级间接地址项各 1 个。磁盘位示图和索引节点位示图分别记录磁盘盘块和索引节点的使用情况，0 表示未使用，1 表示已使用。文件系统目录结构与文件的索引节点表如题 46 图所示，其中文件 `file` 的大小为 30 KB。

请回答下列问题：

1. `file` 的索引节点所在的盘块号是多少？若 `file` 的索引节点已经读取到内存，要访问 `file` 文件中逻辑偏移地址为 21460 的一个字节数据，则最多需要读多少个盘块？如果文件系统中有足够的磁盘空间，则最多可以存放多少个文件？（3 分）
2. 如果要删除目录 `dir1`，则需要对元数据进行哪些操作？（5 分）

![[assets/q46_fig1.svg]]

### 47（本题 9 分）

假设客户端 C 建立一条 TCP 连接，向服务器 S 上传一个总长度为 2000 B 的计算任务描述文件。已知 C 的拥塞窗口初始阈值为 8 MSS，MSS = 500 B，S 对收到的每个 TCP 段进行确认，且确认段不封装数据。接收窗口始终为 1000 B，RTT = 5 ms，C 建立连接时选择的初始序号为 1000，S 选择的初始序号为 2000，SYN、ACK、FIN 为标志位，seq 为序号，ack_seq 为确认序号。在整个文件传输过程中未出现任何重传或报文丢失。如题 47 图所示。

请回答下列问题：

1. C 与 S 建立 TCP 连接过程需要几次握手？C 收到的第二次握手确认段（SYN=1, ACK=1）的确认序号（ack_seq）是多少？（2 分）
2. 当 C 接收到 S 发送的 ACK=1, seq=2001, ack_seq=2001, rwnd=1000 的确认段后，C 的拥塞窗口增加到多少？C 的发送窗口设置为多少？（2 分）
3. C 与 S 释放 TCP 连接过程需要几次挥手？C 收到最后一个 TCP 报文段的序号（seq）、确认序号（ack_seq）、FIN 的值分别是多少？（3 分）
4. 忽略报文段传输时延，且时间从 C 请求建立 TCP 连接时刻算起，则 C 确定 S 已成功接收到文件的时间是多少？（2 分）

![[assets/q47_fig1.svg]]
