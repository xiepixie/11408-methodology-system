---
title: 多处理器的基本概念
source: https://www.codebrick.tech/co-full/posts/cpu/multiprocessor.html
---

# 多处理器的基本概念

## 大纲定位

**五（七）多处理器基本概念**

- SISD、SIMD、MIMD、向量处理器的基本概念
- 硬件多线程的基本概念
- 多核（multi-core）处理器的基本概念
- 共享内存多处理器（SMP）的基本概念

大纲这一条只到"**基本概念**"——能区分几种类型、说清各自含义即可。**互连网络、Cache 一致性协议的具体实现不在要求内**。

## 知识框架

本节回答四个问题：

- 并行有哪几个层次？Flynn 是按什么标准分类的？
- 硬件多线程和多核，都是"同时跑多个线程"，区别在哪？
- 三种硬件多线程方式的分界线是什么？
- UMA 和 NUMA 的差别从哪来？

> [!TIP]
>

一条贯穿全篇的主线

本节所有技术都在回答同一个问题：**处理器闲着的时候，怎么让它别闲着。**

- **SIMD**：一条指令喂多份数据，让多个 ALU 同时干活
- **硬件多线程**：某个线程卡住时（如 Cache 缺失），换另一个线程上
- **多核**：干脆多放几套完整的处理器

前两者是"把现有部件用满"，后者是"直接加部件"。

## 一、Flynn 分类法

根据**指令流**和**数据流**的数量，Flynn 将计算机体系结构分为四类：

| 类型 | 全称 | 指令流 | 数据流 | 典型代表 |
| --- | --- | --- | --- | --- |
| SISD | 单指令流单数据流 | 1 | 1 | 传统单处理器 |
| SIMD | 单指令流多数据流 | 1 | 多 | 向量处理器、GPU |
| MISD | 多指令流单数据流 | 多 | 1 | 理论存在，实际几乎没有 |
| MIMD | 多指令流多数据流 | 多 | 多 | 多核处理器、多处理器系统 |

### SISD

传统的串行计算机，一个处理器 + 一个存储器，按指令流顺序逐条执行。部分 SISD 计算机采用指令流水线技术来提升性能，但本质上仍是单指令流。

### SIMD

采用**数据级并行**技术：一个指令控制单元 + 多个处理单元，所有处理单元同时执行同一条指令，但各自操作不同的数据。

适用场景：规则、同构的数据处理（如数组运算）。一条 SIMD 指令可以在 16 个 ALU 中并行处理 16 对数据，效率极高。

局限：遇到条件分支（switch/case）时效率显著下降，因为各处理单元需要执行不同操作，难以保持同步。

**向量处理器**是 SIMD 的典型实现，通过专用指令直接操作向量（一维数组），以流水化方式批量处理，在数值模拟等规则计算场景中性能优势明显。

### MISD

多个指令流同时处理同一数据流。**理论上存在，实际几乎没有**。

原因不难想：让多条不同的指令去处理同一份数据，结果该以谁为准？这个组合缺乏自然的应用场景，因此四类里只有它是空的。Flynn 分类之所以仍然列出它，是因为分类法本身是"指令流 × 数据流"的完整二维组合——**MISD 是分类的产物，不是需求的产物**。

### MIMD

同时执行多条指令，分别处理多个不同数据流，是目前主流的并行计算模型。MIMD 进一步分为两类：

| 子类型 | 存储组织 | 通信方式 | 别称 |
| --- | --- | --- | --- |
| 多计算机系统 | 每个节点有私有存储器，地址空间独立 | 消息传递 | 消息传递型 MIMD |
| 多处理器系统 | 共享全局地址空间 | 共享存储（访存指令） | 共享存储型 MIMD |

总结：SIMD 属于数据级并行，适用于规则数据处理；MIMD 支持线程级或任务级并行，并行程度更高，适用范围更广。

## 二、硬件多线程

传统 CPU 的线程切换需要把寄存器上下文**存到内存再读回来**，开销很大（上百个时钟周期）。

硬件多线程的做法是：为每个线程配备**独立的寄存器组和 PC**，切换时只需激活对应线程的那一套硬件状态，**完全不必读写内存**。

> [!TIP]
>

硬件多线程复制什么、不复制什么

| 复制（每个线程一套） | 共享（所有线程公用） |
| --- | --- |
| **寄存器组** | ALU 等执行部件 |
| **PC** | Cache |
| 页表基址寄存器等线程状态 | 流水线本身 |

**只复制"状态"，不复制"算力"**——这正是它比多核便宜得多的原因，也是它性能提升有限（而不是翻倍）的原因。

三种实现方式的分界线，是**什么时候切换线程**：

### 细粒度多线程（Fine-grained）

每个时钟周期切换线程，交替执行不同线程的指令。

```
时钟 i  ：发射线程 A 的指令 j, j+1
时钟 i+1：发射线程 B 的指令 k, k+1
时钟 i+2：发射线程 A 的指令 j+2, j+3
时钟 i+3：发射线程 B 的指令 k+2, k+3
```

优点：功能部件利用率高。缺点：单个线程的执行速度被拉低（每隔一个周期才轮到自己）。

### 粗粒度多线程（Coarse-grained）

连续执行同一线程的指令，仅当遇到**长延迟事件**（如 Cache 缺失）导致流水线阻塞时才切换线程。

```
时钟 i  ：发射线程 A 的指令 j, j+1
时钟 i+1：发射线程 A 的指令 j+2, j+3 → 发现 Cache 缺失
时钟 i+2：线程调度，从 A 切换到 B
时钟 i+3：发射线程 B 的指令 k, k+1
```

切换时需要清空被阻塞的流水线并重新填充，切换开销大于细粒度多线程。

### 同时多线程（SMT / 超线程）

单个时钟周期内**同时发射并执行来自多个不同线程的多条指令**，既利用了指令级并行，又实现了线程级并行。

```
时钟 i  ：发射线程 A 的指令 j, j+1 + 线程 B 的指令 k, k+1
时钟 i+1：发射线程 A 的指令 j+2 + 线程 B 的指令 k+2 + 线程 C 的指令 m
```

SMT 通常构建于超标量 + 乱序执行的微架构之上，通过共享执行单元和缓存，在维持高单线程性能的同时提升整体吞吐率。

Intel 的**超线程（Hyper-Threading）** 就是 SMT 的典型实现：一个物理核心维护两套线程状态部件（寄存器组、PC），而缓存和 ALU 等执行资源由两个逻辑核心共享，实际性能提升约 10%~30%。

### 三种方式对比

| 特性 | 细粒度多线程 | 粗粒度多线程 | 同时多线程（SMT） |
| --- | --- | --- | --- |
| 切换时机 | 每个时钟周期 | 长延迟事件（如 Cache 缺失） | 不切换，同时执行 |
| 同一周期执行的线程数 | 1 | 1 | 多个 |
| 切换开销 | 极小 | 较大（需清空流水线） | 无切换开销 |
| 并行层次 | 线程级（时间交错） | 线程级（时间交错） | 指令级 + 线程级 |

> [!WARNING]
>

前两种是"轮流"，SMT 是"同时"

这是三者最本质的区别：

- **细粒度、粗粒度**：任一时刻流水线里**只有一个线程**的指令，只是切换得快或慢——本质是**时间上交错**
- **SMT**：同一个时钟周期内，流水线里**同时有多个线程**的指令在发射——本质是**空间上共存**

因此 SMT 必须建立在**超标量**之上：只有多发射的流水线才有多余的发射槽，能同时容纳来自不同线程的指令。单发射流水线做不了 SMT。

## 三、多核处理器

多个独立处理核心集成在**同一芯片**上，也称片上多处理器（CMP）。

典型的 Cache 层次结构：

- 每个核拥有**私有的 L1 Cache**（有时也包括 L2）
- 多个核**共享**更高层级的 Cache（如 L3）
- 所有核通过互连网络共享主存储器

多核 vs 单核多线程的本质区别：多核架构下，每个核独立运行一个线程，实现**物理上的真正并行**。而单核的细粒度/粗粒度多线程只是时间交错执行，任一时刻仅有一个线程在运行（SMT 除外）。

一个直观的类比：搬 4 块石头到马路对面（每块 1 分钟）。串行处理器需要 4 分钟；双核处理器相当于两个人同时搬，2 分钟；向量处理器（SIMD）相当于用长木板同时推 4 块，1 分钟。**多核靠增加处理单元实现任务级并行，向量处理器靠单指令多数据实现数据级并行。**

> [!TIP]
>

多核不会带来线性加速

即使核数翻倍，程序也不会快一倍——因为程序中总有**无法并行的部分**（串行段），它的耗时不随核数减少。

设可并行部分占比 p，核数为 n，则加速比上限为S=1(1−p)+pn

即使 n→∞，S 也只能趋近 11−p。若有 5% 的代码无法并行，加速比永远超不过 20 倍。

这就是 [[03_计算机组成原理_CO/overview/performance-metrics.md|Amdahl 定律]] 在多核上的直接应用——**加速比被串行部分卡死**，这是多核路线的根本天花板。

## 四、共享内存多处理器（SMP）

多个处理器共享**统一的物理地址空间**，任意 CPU 可通过普通访存指令访问内存中的任意位置，通过读/写共享变量进行通信。

根据内存访问延迟的特性分为两类：

### UMA（统一存储访问）

所有 CPU 访问任意内存单元的延迟**基本相同**，与发起请求的 CPU 及目标地址无关。

### NUMA（非统一存储访问）

内存访问延迟**取决于请求 CPU 与目标内存的物理位置关系**。主存被划分为多个区域，分别连接到不同的 CPU。

| 特性 | UMA | NUMA |
| --- | --- | --- |
| 内存控制器位置 | 集中式（早期北桥架构） | 分布式（集成到各 CPU 内部） |
| 访问延迟 | 均匀 | 本地内存快，远程内存慢 |
| 互连方式 | 前端总线（FSB） | 高速互连总线（如 Intel QPI） |
| 瓶颈 | 总线争用 | 数据布局敏感 |
| 可扩展性 | 较差 | 较好 |

NUMA 的出现是为了解决多核/多处理器系统中前端总线争用的性能瓶颈：把内存控制器集成到 CPU 内部，每个 CPU 直接连接一部分物理内存（本地内存），CPU 之间通过高速互连总线访问远程内存。

### SMP 中的同步与一致性

多个 CPU 可能同时访问同一共享变量，必须引入**同步机制**保证原子性和数据一致性，常用方法是**互斥锁**。

SMP 系统的 Cache 一致性问题比单处理器更严格：多个 CPU 的 Cache 可能同时缓存同一物理地址的副本，任意时刻所有副本必须保持一致。这由硬件一致性协议保障，通过传播写操作或无效化其他 CPU 中的副本来维护全局一致性。

单处理器上的"Cache 与主存一致"问题（写直达/写回）见 [[03_计算机组成原理_CO/storage/cache-write-policy.md|Cache 写策略]]。多处理器把这个问题从"两级之间"扩展成了"多个 Cache 之间"，难度上了一个量级——但具体协议不在大纲要求内。

## 易混淆知识点

**1. 多核和多处理器是一回事吗？**

不完全是。**多核**（CMP）指多个核心集成在**同一块芯片**上；**多处理器**是更广的概念，多个物理 CPU 芯片组成的系统也算。

现在两者界限模糊了——一块多核芯片本身就构成一个共享内存多处理器系统。区别主要在物理封装，逻辑上都是 MIMD。

**2. 超线程能让性能翻倍吗？**

**不能**，实际提升约 10%~30%。

因为 SMT **只复制线程状态，不复制执行部件**。两个线程共享同一套 ALU 和 Cache，一个线程能占满执行资源时，另一个就没得用了。它的收益来自"填补空隙"——某个线程因 Cache 缺失或依赖而停顿时，另一个线程正好顶上。

**程序的指令级并行度越低（空隙越多），SMT 收益越大。**

**3. SIMD 和 MIMD 哪个更快？**

问法本身有问题，两者的适用场景不同：

|  | 擅长 | 不擅长 |
| --- | --- | --- |
| **SIMD** | 规则、同构的大批量数据（数组、图像、矩阵） | 条件分支多的代码 |
| **MIMD** | 各干各的、逻辑不同的任务 | 无（但硬件成本高得多） |

SIMD 遇到条件分支时，各处理单元要执行不同操作却又必须同步，只能让不满足条件的单元空转，效率大幅下降。

**4. 向量处理器属于哪一类？**

属于 **SIMD**。它通过专用指令直接操作向量（一维数组），以流水化方式批量处理，是 SIMD 最典型的实现形式。

**5. NUMA 比 UMA 好吗？**

**各有取舍**。NUMA 解决了总线争用的瓶颈，可扩展性好得多，但代价是**访问延迟不再均匀**——程序若频繁访问远程内存，反而更慢。

因此 NUMA 系统对**数据布局**很敏感，操作系统需要尽量把线程和它常用的数据安排在同一节点上。**UMA 简单但扩展不动，NUMA 能扩展但需要软件配合。**

## 本节小结

- 全篇主线：**让闲着的部件别闲着**。SIMD 喂满 ALU，多线程填补停顿空隙，多核直接加部件
- Flynn 分类的标准：**指令流数量 × 数据流数量**
- **MISD 是分类的产物，不是需求的产物**——多条指令处理同一数据缺乏应用场景
- SIMD 适合规则数据处理（向量处理器、GPU），**遇条件分支效率大幅下降**
- MIMD 分为消息传递型（多计算机）和共享存储型（多处理器）
- 硬件多线程**只复制状态（寄存器组、PC），不复制算力（ALU、Cache）**
- 三种方式的分界是**何时切换**：细粒度每周期切、粗粒度长延迟才切、**SMT 不切而是同时执行**
- **SMT 必须建立在超标量之上**，单发射流水线做不了
- 超线程提升约 10%~30% 而非翻倍，因为执行部件是共享的
- 多核 **不会带来线性加速**，被 Amdahl 定律中的串行部分卡死
- UMA 访问延迟均匀但扩展性差；NUMA 可扩展但**对数据布局敏感**
- SMP 中 Cache 一致性由硬件协议保障（具体协议不在大纲要求内）

## 真题练习

### 相关真题（1题）
[2022Q222分](https://www.codebrick.tech/practice/q/co-2022-22)

## 教材出处

- Flynn 分类与多处理器基本概念：袁春风《计算机组成与系统结构》第 3 版 §9.1 并行处理系统概述、§9.2 多处理器系统（按大纲裁剪，体系结构细节超纲）[[03_计算机组成原理_CO/cpu/pipeline-performance.md|上一篇流水线性能分析与多发射技术]][[03_计算机组成原理_CO/cpu/instruction-lifecycle-simulator.md|下一篇一条指令在CPU里的一生（模拟器）]]

### 相关文章

- [[03_计算机组成原理_CO/cpu/cpu-overview.md|CPU 的功能和基本结构]]
- [[03_计算机组成原理_CO/cpu/instruction-cycle.md|指令执行过程]]
- [[03_计算机组成原理_CO/cpu/single-cycle.md|单周期数据通路]]
- [[03_计算机组成原理_CO/cpu/multi-cycle.md|多周期数据通路]]
- [[03_计算机组成原理_CO/cpu/single-bus-datapath.md|单总线数据通路]]
- [[03_计算机组成原理_CO/cpu/hardwired-control.md|硬布线控制器]]
- [[03_计算机组成原理_CO/cpu/microprogrammed.md|微程序控制器]]
- [[03_计算机组成原理_CO/cpu/exception-interrupt.md|异常和中断机制]]
- [[03_计算机组成原理_CO/cpu/pipeline-basic.md|指令流水线的基本概念与实现]]
- [[03_计算机组成原理_CO/cpu/pipeline-hazard.md|流水线冒险及其处理]]
- [[03_计算机组成原理_CO/cpu/pipeline-performance.md|流水线性能分析与多发射技术]]
- [[03_计算机组成原理_CO/cpu/instruction-lifecycle-simulator.md|一条指令在CPU里的一生（模拟器）]]
- [[03_计算机组成原理_CO/cpu/pipeline-compare-simulator.md|流水线 vs 单周期对比（模拟器）]]
- [[03_计算机组成原理_CO/cpu/data-hazard-simulator.md|流水线数据冒险（模拟器）]]