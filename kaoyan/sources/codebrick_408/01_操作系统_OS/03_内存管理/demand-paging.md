---
title: 请求页式管理 ​
source: https://www.codebrick.tech/os-blog/posts/memory/demand-paging.html
---

# 请求页式管理

2026 大纲 **三（二）2 请求页式管理**。

## 速查

| **它是什么** | 请求分页 = **基本分页 + 请求调页 + 页面置换**，虚拟内存最常用的实现 |
| --- | --- |
| **页表项扩出的四项** | **状态位 P**（在不在内存）、**访问字段 A**（该淘汰谁）、**修改位 M**（换出要不要写回）、**外存地址**（去哪儿取） |
| 🔴 **A 和 M 由硬件自动置 1** | 访存每秒上亿次，任何软件参与都无法承受；软件只负责在合适时机清 0（A 的清零时机见 [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 篇]]） |
| **一次缺页含两次中断** | 先**内中断**（缺页故障，CPU 内部条件触发），后**外中断**（磁盘 I/O 完成，外设信号触发） |
| 🔴 **缺页期间进程阻塞、让出 CPU** | 数据由**磁盘控制器 / DMA** 搬运，CPU 不参与。所以一次缺页的代价除磁盘 I/O 外还含**两次上下文切换**——这是 tfault 比 tmem 大四五个数量级的另一半原因 |
| **缺页属内中断的故障（Fault）** | 判据是**返回位置**：陷入 Trap 主动触发、返回**下一条**；故障 Fault 被动但**可修复**、返回**重新执行当前条**；终止 Abort 不可恢复、直接终止进程。调页就是修复，修复后原指令能成功 ⇒ 故障 |
| **缺页中断的两处特殊** | ① 在**指令执行期间**产生（普通中断是执行完毕后才检测）⇒ 必须**重新执行**该指令而非下一条；② **一条指令可能触发多次**（指令本身跨页、多个操作数分落不同页）⇒ 直接决定[[01_操作系统_OS/03_内存管理/page-allocation.md|最少页框数]] |
| 🔴 **TLB 一致性必须由 OS 显式维护** | 页面换出时**必须让它的 TLB 项失效**，否则后续访问会在 TLB 里命中一条指向**已被别的页面占用**的页框号、绕过页表读到错误数据；调入后要把新表项**写入 TLB**。通用判据：**凡会让页表项内容失效的动作（换出、改页框号、进程切换换页表）都要同步作用到 TLB 上**。进程切换的处理是整表清空或用 ASID 区分 |
| **换出要不要写回，看 M** | M=1 内存中的副本才是最新的，必须写回；M=0 与外存副本一致，直接丢弃，省一次磁盘写 |
| 🔴 **缺页率 f=F/A，分母是访存次数不是指令条数** | 题目给"每执行 k 条指令缺一次"时，须先乘每条指令的平均访存次数 c：f=1/(kc)。忘了折算，结果整整差 c 倍 |
| **EAT 简式** | EAT=(1−f)tmem+ftfault，本质是一次访存耗时的**期望值** |
| **tfault 的组成**（全站统一口径） | ① 缺页中断处理（保存现场、分析原因、查页表定位外存地址、跑置换算法、更新页表与快表、恢复现场）② **磁盘 I/O（主要开销）**，脏页还要**多一次写回** ③ 重新启动指令 |
| **按脏页概率加权** | tfault=βta+(1−β)tb（ta 脏页情形、tb 干净页情形）。这条解释了[[01_操作系统_OS/03_内存管理/clock-replace.md|改进 CLOCK]] 优先淘汰干净页的收益落在哪：它压低的正是 β |
| **含快表的展开式** | EAT=λ+at+(1−a)[t+f(ε+λ+t)+(1−f)(λ+t)]，ε 为缺页中断处理时间。**令 λ=0、a=0 即退化回简式** |

## 一、缺页中断的处理流程

访问的页面不在内存中（状态位 P=0）时产生**缺页中断（Page Fault）**：

流程图上那句"从外存调入页面"是一次**磁盘 I/O**，毫秒量级，这段时间 CPU 不会干等：

| 阶段 | 进程状态 | CPU 在做什么 | 数据由谁搬 |
| --- | --- | --- | --- |
| 缺页中断被触发 | 运行 → **阻塞** | 执行缺页中断处理程序，启动磁盘 I/O | —— |
| 磁盘读取中 | **阻塞**（挂在该 I/O 的等待队列上） | **被调度去运行别的进程** | **磁盘控制器 / DMA**，CPU 不参与搬运 |
| I/O 完成 | **阻塞 → 就绪** | 响应磁盘的 **I/O 完成中断**（外中断），唤醒该进程 | —— |
| 再次被调度 | 就绪 → 运行 | **重新执行**引起缺页的那条指令 | —— |

## 二、有效访存时间：口径先对，公式才有意义

缺页率的定义是：设访问页面成功的次数为 S、失败（需从外存调入）的次数为 F，总的页面访问次数 A=S+F，则 f=F/A。分母 A 是**页面访问次数**，也就是访存次数——这一点直接决定算例对不对：

| 题目给的说法 | 它是什么口径 | 怎么折算成 f |
| --- | --- | --- |
| "缺页率为 f" | 每次访存 | 直接用 |
| "每访问 n 次内存缺页一次" | 每次访存 | f=1/n |
| **"每执行 k 条指令缺页一次"** | **每条指令** | 先乘上**每条指令的平均访存次数** c，再取倒数：f=1/(kc) |

c 从哪来：一条指令至少要**取指**访存 1 次，再加上操作数的访存次数。所以"一条指令 = 一次访存"只在"零地址指令且不访问内存操作数"时成立，一般题目会给出或可从指令格式推出。从"每 k 条指令缺一次页"折算到 EAT 的三步完整算例，含两条改进路线的敏感度对比（想看口径折算与期望加权怎么落到数上时展开）

某请求分页系统，一次内存访问 tmem=100 ns。缺页处理时：若被换出页未被修改，共需 tb=5 ms；若被换出页是脏页需先写回，共需 ta=8 ms。实测被换出页是脏页的概率 β=0.4。程序**平均每条指令访存 2 次**，实测**每执行 1250 条指令发生 1 次缺页**。

**第一步：把"每条指令"折算成"每次访存"**A=1250×2=2500 (次访存),f=FA=12500=4×10−4

EAT 公式里的 f 是**每次访存**的缺页概率，而题目给的是**每条指令**的口径。不乘那个 2，f 会大出一倍，最后的 EAT 也跟着错一倍。

**第二步：按 β 加权求 tfault**tfault=βta+(1−β)tb=0.4×8+0.6×5=3.2+3.0=6.2 ms=6.2×106 ns

一次缺页到底花 5 ms 还是 8 ms，取决于被淘汰的那一页脏不脏——这是个随机事件，只能取**期望**，β 就是这个事件的概率。

**第三步：代入 EAT**EAT=(1−f)tmem+ftfault=0.9996×100+4×10−4×6.2×106=99.96+2480=2579.96≈2580 ns

结论：EAT≈2580 ns，是纯内存访问（100 ns）的 **约 25.8 倍**，而缺页率只有万分之四。

**敏感度**：若把 β 从 0.4 降到 0（全靠改进 CLOCK 淘汰干净页），tfault 降到 5 ms，EAT 降到 99.96+2000=2100 ns——**只优化"脏不脏"就省下 18%**；而若把缺页率减半（f=2×10−4），EAT 降到约 1340 ns，**省下 48%**。两条改进路线的收益量级，这里一目了然。

## 三、请求分页的地址变换

在基本分页地址变换的基础上，增加了缺页中断处理和 TLB 管理：

- CPU 发出逻辑地址 → 拆分为页号 P 和偏移 W
- 查 TLB → 命中则直接得到页框号（**命中时顺便由硬件置访问位 A；若是写操作还置修改位 M**）
- TLB 未命中 → 查内存中的页表
- 页表项 P=1（在内存）→ **把该页表项写入 TLB**（快表满则按某种算法淘汰一项），计算物理地址
- 页表项 P=0（不在内存）→ 触发缺页中断 → 调入页面（必要时换出一页并使其 TLB 项失效）→ 更新页表和 TLB → **重新执行指令**

第 5 步末尾的"重新执行"意味着这条流程会**整个再走一遍**，第二遍时在第 2 步就会命中 TLB。

## 本节小结

- 页表项在"页号 → 页框号"之外扩出**状态位 P / 访问字段 A / 修改位 M / 外存地址**四项，各回答一个问题；**A 与 M 由硬件在访存时自动置 1**，软件只负责清 0。
- 缺页处理的完整链条是：置换选页 → **使其 TLB 项失效** → M=1 写回、M=0 丢弃 → 调入 → 更新页表与快表 → **重新执行原指令**；期间进程**阻塞让出 CPU**、数据由 DMA 搬运，因此一次缺页含**两次中断**（内中断的缺页故障 + 外中断的 I/O 完成）与**两次上下文切换**。缺页属内中断中的**故障**，判据是返回后重执行当前指令。
- EAT=(1−f)tmem+ftfault 是期望值；**f 的分母是访存次数**，给"每 k 条指令缺一次"时须乘每指令访存次数 c。tfault=βta+(1−β)tb——缺页率是虚存性能的第一敏感量。教材出处

- 缺页率的定义 f=F/A（A=S+F 为总的页面访问次数）、以及缺页中断处理时间按脏页概率加权的公式 t=βta+(1−β)tb：汤小丹《计算机操作系统》5.2.3 节「缺页率」，p162
- 页面调入过程（保留 CPU 环境 → 分析中断原因 → 查页表得外存物理块 → 内存已满则先按置换算法选出换出页 → 修改位为 0 可不写回、为 1 必须写回 → 调入后修改页表项并**将此页表项写入快表**）：同书 5.2.3 节「页面调入过程」，p162
- 地址变换中"先检索快表，找到则修改页表项中的访问位；对写指令还须将修改位置 1"以及快表未命中时的处理：同书 5.2.1 节，p159
- 含快表命中率 a 与缺页率 f 的有效访问时间展开式 EAT=λ+at+(1−a)[t+f(ε+λ+t)+(1−f)(λ+t)]：同书 5.3.5 节「访问内存的有效时间」，p168–169

## 相关知识

[[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|虚拟内存基本概念]]｜[[01_操作系统_OS/03_内存管理/paging.md|基本分页（地址变换）]]｜[[01_操作系统_OS/03_内存管理/fifo-replace.md|FIFO 页面置换算法]]｜[[01_操作系统_OS/01_操作系统概述/interrupt-exception.md|中断和异常的处理]]｜[[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]

## 真题练习

### 相关真题（6题）
[2026Q242分](https://www.codebrick.tech/practice/q/os-2026-24?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2023Q282分](https://www.codebrick.tech/practice/q/os-2023-28?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2022Q302分](https://www.codebrick.tech/practice/q/os-2022-30?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2020Q282分](https://www.codebrick.tech/practice/q/os-2020-28?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2012Q457分](https://www.codebrick.tech/practice/q/os-2012-45?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2009Q468分](https://www.codebrick.tech/practice/q/os-2009-46?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|上一篇虚拟内存基本概念]][[01_操作系统_OS/03_内存管理/fifo-replace.md|下一篇FIFO 页面置换算法]]

### 相关文章

- [[01_操作系统_OS/03_内存管理/memory-concept.md|内存管理基本概念]]
- [[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配（首次/最佳/最坏适应）]]
- [[01_操作系统_OS/03_内存管理/paging.md|基本分页（地址变换）]]
- [[01_操作系统_OS/03_内存管理/segmentation.md|基本分段]]
- [[01_操作系统_OS/03_内存管理/seg-paging.md|段页式管理]]
- [[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|虚拟内存基本概念]]
- [[01_操作系统_OS/03_内存管理/fifo-replace.md|FIFO 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/lru-replace.md|LRU 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/opt-replace.md|OPT 最佳置换算法]]
- [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 与改进 CLOCK 算法]]
- [[01_操作系统_OS/03_内存管理/page-replace-simulator.md|页面置换算法对比模拟器]]
- [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]
- [[01_操作系统_OS/03_内存管理/memory-mapped-file.md|内存映射文件]]
- [[01_操作系统_OS/03_内存管理/virtual-memory-perf.md|虚拟存储性能与改进]]