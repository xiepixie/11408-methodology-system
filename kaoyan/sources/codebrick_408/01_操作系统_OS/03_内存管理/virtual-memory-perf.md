---
title: 虚拟存储性能与改进 ​
source: https://www.codebrick.tech/os-blog/posts/memory/virtual-memory-perf.html
---

# 虚拟存储性能与改进

2026 大纲 **三（二）6 虚拟存储器性能的影响因素及改进方法**。

## 速查

| 🔴 **"OPT > LRU > CLOCK > FIFO"比的是性能，不是缺页次数** | 两个序方向正好相反：**性能** OPT > LRU > CLOCK > FIFO；**缺页率 / 缺页次数** FIFO > CLOCK > LRU > OPT。判据是"性能越好 ⇔ 缺页率越低"，认准 **OPT 是最好的那一端**，另一端就跟着定了。具体数字见 [[01_操作系统_OS/03_内存管理/opt-replace.md|OPT 篇 §六]] |
| --- | --- |
| 🔴 **"页面越大越好"只在缺页率一个维度上成立** | 大页压低缺页率、缩小页表，但**内碎片增大**（平均浪费半页）、**单次调页要传更多数据且未必都用得上**。所以实际取折中值 |
| **抖动的根本原因** | 同时运行的进程太多 ⇒ 每进程分到的物理块太少 ⇒ 不能满足正常运行的基本要求 ⇒ 频繁缺页。写成判据：∑i\|WSi\|> 可用页框总数（左边需求、右边供给） |
| 🔴 **CPU 利用率低有两种完全相反的成因** | 真的没进程可跑（**该加**）／ 进程全堵在换页 I/O 上（**该减**）。只看利用率无法区分，加错了就是抖动恶性循环的入口。三条可操作判据任选其一：看下降**是否伴随缺页率飙升**、比 ∑\|WSi\| 与可用页框数、算 L 与 S |
| **L=S 准则** | **L = 缺页之间的平均时间**（到达间隔），**S = 平均缺页服务时间**（置换一页所需时间）。L≫S 磁盘空闲、可**提高**多道程度；L≈S 是磁盘与处理机同时满负荷的**目标状态**；L≪S 缺页速度已超过磁盘处理能力、须**降低**多道程度 |
| 🔴 **为什么 L 小反而是坏事** | L<S 意味着**请求到达比服务完成还快**，排队队列会**无限增长**——这正是抖动在排队论上的样子。L=S 是"到达率 = 服务率"的临界点。⚠️ 题目常给"每秒缺页多少次"，那是**频率**，**取倒数**才是 L |
| **PFF（缺页频率）策略** | 缺页率 > **上限** ⇒ 给进程**增加**页框；< **下限** ⇒ **回收**部分页框；已无法再增加而仍高 ⇒ **挂起**该进程。它从**单进程页框数**一侧入手，是"可变分配 + 局部置换"的落地形式 |
| 🔴 **PFF 的上下限由性能目标反解** | EAT 是 f 的单调递增一次函数，故"EAT 不超过某值"等价于"f 不超过某值"：f=EAT−tmemtfault−tmem。**两条水平线本质是性能目标在缺页率轴上的投影** |
| **工作集怎么测** | 不可能逐次记录访存页号（开销等同精确 LRU）。实际是**访问位 + 定时采样**：每页一个访问位（硬件访问时自动置 1），OS 每隔固定周期扫描全部页面，**A=1 的记入工作集并清 0**，**连续 k 个周期未被访问的移出**；窗口 Δ 就体现为"k 个采样周期"。用的是与 [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK]]、[[01_操作系统_OS/03_内存管理/lru-replace.md|LRU 附加引用位法]] **同一个访问位**——一位硬件，三处复用 |
| **挑谁挂起** | 优先级最低 → 优先级较低 → 进一步挑"**并不十分重要但体积较大**"（一次能释放较多物理块）或"**剩余执行时间最多**"（挂起它损失的已投入时间最少）。两条判据：**释放收益最大、沉没成本最小** |
| **性能总账** | EAT=(1−f)tmem+ftfault；tfault = 缺页中断处理 + 磁盘 I/O（主要开销，脏页多一次写回）+ 重启指令，按脏页概率加权得 tfault=βta+(1−β)tb（与[[01_操作系统_OS/03_内存管理/demand-paging.md|请求页式管理]]同一口径） |
| 🔴 **所有改进最终只落在三个量上** | 要么压 f，要么压 tfault，要么压 tmem 的等效值。**记住这条，就不需要背改进方法的清单** |

## 一、影响缺页率的四项因素

把因素与"往哪个方向变"分开列——这两件事混在一列里最容易读反：

| 因素 | 该因素**变大**时 | 缺页率 | 判据 / 代价 |
| --- | --- | --- | --- |
| **页面大小** | 页面划分较大 | **降低** | 一次调入的内容更多，覆盖的空间局部性更广；**代价是内碎片增大** |
| **分配的物理块数** | 分得越多 | **降低** | 驻留集越接近工作集；但边际收益递减，超过工作集大小后再加块几乎无效 |
| **页面置换算法** | 算法越好 | **降低** | 性能序与缺页率序方向相反，见速查 |
| **程序固有特性** | 局部化程度越高 | **降低** | 由**程序的编制方法**决定，是四项里**唯一由应用自己控制**的 |

页面大小这一行值得单独展开，因为它是一组取舍而不是单调关系：

| 页面变大 | 好的一侧 | 坏的一侧 |
| --- | --- | --- |
| 缺页率 | **降低** | —— |
| 页表规模 | 页数减少，**页表变小** | —— |
| **内碎片** | —— | 进程末页填不满的部分变大，**平均浪费半页** |
| 单次调页的 I/O 量 | —— | 一次缺页要传的数据更多，**单次缺页更贵**；且调进来的内容未必都用得上 |

最后一项因素则可以直接看到代码上——同样的数组赋值，按行优先和按列优先的缺页率可能差异巨大：c

```
// 按行优先 —— 局部性好，缺页少
for (int i = 0; i < 1024; i++)
    for (int j = 0; j < 1024; j++)
        a[i][j] = 0;

// 按列优先 —— 局部性差，缺页多
for (int j = 0; j < 1024; j++)
    for (int i = 0; i < 1024; i++)
        a[i][j] = 0;
```

C 语言数组按行存储，`a[i][0..1023]` 在内存里连续。按行遍历时一次调入的页面会被连着用完才换下一页；按列遍历则每访问一个元素就跳过一整行的距离，页框数不够就会不停缺页。

## 二、抖动为什么会自我强化

**抖动**（也叫颠簸）指进程频繁地换入换出页面，大部分时间都花在页面调度上，几乎无法推进实际计算。

环路的入口正是 E → F 那一步：CPU 利用率低这个现象有两种完全相反的成因，只看它无法区分，加错了就正反馈起飞。

```
CPU利用率
    ↑
    |        ╱‾‾╲
    |      ╱      ╲
    |    ╱          ╲
    |  ╱              ╲ ← 抖动开始
    |╱                  ╲____
    +------------------------→ 多道程度
```

随着进程数增加，CPU 利用率先急剧上升，到某一点后增速放缓，到**最大值**后先缓慢下降，越过某个临界点后**加速下降并趋于 0**——趋于 0 的那一段就是系统已经进入抖动。所以存在一个**最优多道程度**。

## 三、四种预防方法作用在不同环节

| 方法 | 做什么 | 位置与局限 |
| --- | --- | --- |
| ① **采取局部置换策略** | 可变分配下规定进程缺页时**只能在自己的空间内置换**，不许抢别人的页框 | 只是把伤害**局部化**：抖动的那个进程仍会**长期挂在磁盘 I/O 等待队列上**，拉长队列、**延长其他进程缺页的处理时间**。治标不治本 |
| ② **把工作集算法融入处理机调度** | 发现 CPU 利用率低时**不要直接调入新作业**，先查每个进程的驻留页面够不够：都够 → 可以调入；有进程不足 → **先给缺页率高的补物理块，此时不引新作业** | 正是针对"CPU 利用率低有两种成因"这个分歧点，**用"驻留页面够不够"这个额外信号把两种成因区分开** |
| ③ **利用 L=S 准则调节缺页率** | 比较缺页到达间隔与缺页服务时间，据此决定加还是减多道程度 | 给出一个**可算**的判据；道理是排队论的到达率与服务率 |
| ④ **选择暂停的进程** | 挂起一部分进程、把腾出的内存分给缺页率偏高的进程，挑选顺序与调度策略保持一致 | 这一条才是**真正减少多道程度** |

有了各进程的 ∥WSi∥，方法②的判据就可算了。举例：系统可用页框 **100** 个，四个进程实测工作集大小为 30、25、28、24，则 ∑∥WSi∥=107>100——需求超过供给，必须挂起一个；挂起工作集最小的那个（释放 24 块）后降到 83 ≤ 100，其余三个进程都能吃饱。至于"挂起谁"另有讲究，见方法④与速查。两道算例：用 L=S 判断该加还是该减进程、由性能目标反解 PFF 的上下限（想看频率与间隔怎么互转、阈值怎么被缺页处理时间的量级压得极小时展开）

**算例一：L=S 判断多道程度**（数据自造）

某系统处理一次缺页的平均时间 S=5 ms，当前内存中有 10 个进程，实测**全系统每秒共发生 400 次缺页**。

*第一步：把"每秒缺页次数"折算成 L。* L 的定义是**两次缺页之间的平均时间**，而题目给的是**频率**，两者互为倒数——这是整道题唯一需要转换口径的地方。L=1 秒400 次=1000 ms400=2.5 ms

*第二步：与 S 比较。* L=2.5 ms<S=5 ms，L/S=0.5：缺页请求的到达间隔只有服务时间的一半，**磁盘忙不过来、队列在发散** ⇒ 系统已经处于（或正在滑向）**抖动**，应当**降低多道程度**。

*第三步：算目标。* 要 L=S=5 ms，即每秒缺页次数降到 1000/5=200 次，也就是当前的 **50%**。

*第四步：折算多道程度。* 若 10 个进程的缺页情况大致相当，每个约贡献 40 次/秒，要把总数压到 200 次/秒，**多道程度应降到约 5**。这里只能说"大致"——挂起进程后留下的进程会分到更多页框，各自的缺页率还会**进一步下降**，所以真实需要挂起的进程数通常**少于 5 个**。这个估算给的是上界，实际系统靠反复测量逼近。

**算例二：反解 PFF 的上下限**（数据自造）

某请求分页系统 tmem=100 ns，一次缺页处理平均 tfault=5 ms。设计目标是**有效访存时间不得超过纯内存访问的 2.5 倍**（EAT≤250 ns）；同时规定当**再增加页框已几乎无收益**（增幅不足 1%，即 EAT≤101 ns）时就该回收页框。

*第一步：写出关系并反解。* EAT 是 f 的一次函数且单调递增，所以两者的"不超过"是等价的：EAT=(1−f)tmem+ftfault⟹f=EAT−tmemtfault−tmem

*第二步：算上限。*f上限=250−1005×106−100=1504999900≈3.0×10−5≈133333

校验：f=3.0×10−5 时 EAT=0.99997×100+3.0×10−5×5×106=100+150=250 ns ✓

*第三步：算下限。*f下限=101−1005×106−100=14999900≈2.0×10−7

*结论：* 缺页率高于 3.0×10−5（约每 3.3 万次访存缺一次）→ **加页框**；低于 2.0×10−7（约每 500 万次访存缺一次）→ **回收页框**给别人；两者之间不动。

注意 tfault 有 5 个数量级的杠杆，所以两条阈值都小得惊人：**万分之一的缺页率在虚存里已经算"高"了**。这也解释了为什么上下限相差 150 倍还是一条很窄的带——因为 EAT 对 f 的斜率是 tfault−tmem≈5×106。

## 四、改进方法汇总

考纲这一条写的是"影响因素**及改进方法**"。防抖动只是其中一条线，其余改进散落在全章各处，这里收拢成一张表——每一项都写清它作用在哪一环、降低了 EAT 公式里的哪个量：

| 改进方法 | 作用环节 | 降低的是什么 | 详见 |
| --- | --- | --- | --- |
| **TLB（快表）** | 地址变换 | 命中时省掉**访问页表的那一次访存**，直接压低 tmem 的等效值 | [[01_操作系统_OS/03_内存管理/paging.md|页式管理]] |
| **多级页表 / 反置页表** | 页表本身的存储 | 页表不必整体驻留，**减少页表占用的页框**，等于把页框让给数据 | [[01_操作系统_OS/03_内存管理/paging.md|页式管理]] |
| **合理选择页面大小** | 缺页率 ↔ 内碎片 | 大页压低 f、缩小页表，代价是内碎片与单次调页开销 | 本篇 §一 |
| **预调页策略** | 调入时机 | 一次调入多个相邻页，**减少缺页中断的次数**（降低 f 的等效值） | [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]] |
| **页面缓冲算法（PBA）** | 换出路径 | 换出页数据仍保留可直接取回、脏页攒批写盘，**减少磁盘 I/O 次数**，直接压低 tfault | [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]] |
| **改进 CLOCK（优先淘汰干净页）** | 置换选择 | 压低脏页概率 β，从而压低 tfault=βta+(1−β)tb | [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 篇]] |
| **更好的置换算法（LRU / CLOCK 取代 FIFO）** | 置换选择 | 直接压低 f | [[01_操作系统_OS/03_内存管理/opt-replace.md|四篇置换算法]] |
| **按比例 / 按优先权分配页框** | 分配 | 让驻留集贴近工作集，压低 f | [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]] |
| **局部置换、工作集融入调度、L=S、暂停进程、PFF** | 多道程度与页框数 | **防抖动**——避免 f 失控 | 本篇 §三 |
| **改善程序局部性（按行遍历）** | 应用程序侧 | 压低 f，是**唯一不由 OS 控制**的一项 | 本篇 §一 |

## 本节小结

- 影响缺页率的四项因素是**页面大小、分配的物理块数、置换算法、程序固有特性**（唯一由应用自己控制的一项）；页面越大只在缺页率这一个维度上更好，"OPT > LRU > CLOCK > FIFO"比的是性能而非缺页次数。
- **抖动**的根本原因是 ∑∥WSi∥ 超过可用页框总数，它会因"CPU 利用率低有两种相反成因"而自我强化，故存在**最优多道程度**；四种预防方法分别是局部置换（治标）、工作集融入调度（补上区分成因的信号）、L=S 准则（可算判据）、选择暂停进程（真正减多道程度），另有 PFF 从单进程页框数一侧入手。
- 全章性能总账是 EAT=(1−f)tmem+ftfault——**所有改进最终都落在 f、tfault、tmem 这三个量上**，记住这条就不必背改进清单。教材出处

- 缺页率 f=F/A 的定义与**影响缺页率的四个因素**（① 页面大小：**页面划分较大则缺页率较低**；② 进程所分配物理块的数目：越多缺页率越低；③ 页面置换算法；④ 程序固有特性：程序编制的局部化程度越高，执行时缺页程度越低）：汤小丹《计算机操作系统》5.2.3 节「缺页率」，p162
- 抖动的定义"刚被换出的页很快又要被访问……一个进程在运行中把大部分时间都花费在页面置换工作上"：同书 5.3 节开头，p163
- 多道程序度与处理机利用率的曲线（到 Nmax 达最大，越过 N2 后加速下降趋于 0）、以及产生抖动的根本原因"同时在系统中运行的进程太多，由此分配给每一个进程的物理块太少"：同书 5.4.1 节，p169–170
- 抖动的四种预防方法（① 采取局部置换策略；② 把工作集算法融入到处理机调度中；③ 利用"L=S"准则调节缺页率，L 是缺页之间的平均时间、S 是平均缺页服务时间，由 Denning 于 1980 年提出；④ 选择暂停的进程，通常先选优先级最低的）：同书 5.4.3 节「"抖动"的预防方法」，p172
- 缺页率与物理块数的关系曲线（物理块数超过某个数目后再增加对缺页率的改善已不明显）：同书 5.4.2 节图 5-10，p170–171

## 相关知识

[[01_操作系统_OS/03_内存管理/memory-mapped-file.md|内存映射文件]]｜[[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]｜[[01_操作系统_OS/03_内存管理/demand-paging.md|请求页式管理]]｜[[01_操作系统_OS/03_内存管理/opt-replace.md|OPT 页面置换算法]]｜[[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 与改进 CLOCK 算法]]

## 真题练习

### 相关真题（6题）
[2026Q242分](https://www.codebrick.tech/practice/q/os-2026-24?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2023Q282分](https://www.codebrick.tech/practice/q/os-2023-28?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2022Q302分](https://www.codebrick.tech/practice/q/os-2022-30?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2020Q282分](https://www.codebrick.tech/practice/q/os-2020-28?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2012Q457分](https://www.codebrick.tech/practice/q/os-2012-45?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[2009Q468分](https://www.codebrick.tech/practice/q/os-2009-46?ctx=os-2026-24,os-2023-28,os-2022-30,os-2020-28,os-2012-45,os-2009-46)[[01_操作系统_OS/03_内存管理/memory-mapped-file.md|上一篇内存映射文件]][[01_操作系统_OS/04_文件管理/file-concept.md|下一篇文件的基本概念]]

### 相关文章

- [[01_操作系统_OS/03_内存管理/memory-concept.md|内存管理基本概念]]
- [[01_操作系统_OS/03_内存管理/contiguous-allocation.md|连续分配（首次/最佳/最坏适应）]]
- [[01_操作系统_OS/03_内存管理/paging.md|基本分页（地址变换）]]
- [[01_操作系统_OS/03_内存管理/segmentation.md|基本分段]]
- [[01_操作系统_OS/03_内存管理/seg-paging.md|段页式管理]]
- [[01_操作系统_OS/03_内存管理/virtual-memory-concept.md|虚拟内存基本概念]]
- [[01_操作系统_OS/03_内存管理/demand-paging.md|请求页式管理]]
- [[01_操作系统_OS/03_内存管理/fifo-replace.md|FIFO 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/lru-replace.md|LRU 页面置换算法]]
- [[01_操作系统_OS/03_内存管理/opt-replace.md|OPT 最佳置换算法]]
- [[01_操作系统_OS/03_内存管理/clock-replace.md|CLOCK 与改进 CLOCK 算法]]
- [[01_操作系统_OS/03_内存管理/page-replace-simulator.md|页面置换算法对比模拟器]]
- [[01_操作系统_OS/03_内存管理/page-allocation.md|页框分配与回收]]
- [[01_操作系统_OS/03_内存管理/memory-mapped-file.md|内存映射文件]]