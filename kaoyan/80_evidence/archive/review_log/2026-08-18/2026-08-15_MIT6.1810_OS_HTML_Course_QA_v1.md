# MIT 6.1810 × 408 OS HTML Course QA v1

日期：2026-08-15  
范围：`30_408/30_操作系统/80_实战课程/html/`  
结论：**15 个教学单元第一版主线已建立；进入 Source Refresh + Learner Testing 阶段。**

## 1. 课程资产

- `index.html` + Lesson 01--15，共 16 个 HTML；
- 所有本地页面链接已静态检查，无 broken href；
- 所有 HTML 已用 Python `HTMLParser` 解析；
- 所有含 `<script>` 页已用 `node --check` 做 JavaScript 语法检查；
- 无重复 `id`；
- index 中 01--15 已全部从 Planned 转为真实链接。

## 2. Quality Gate 审计

### Source Freshness

已显式区分：

```text
2026 schedule/lab
inherited 2024/2025 lecture material
current xv6 main
lab branch
fallback official older lab
```

当前已知不确定项：

- 2026 schedule 自述未来日期部分 notes/videos 暂沿用旧版；
- Page Fault、Thread Switching、Locking、Coordination、File System 等 schedule-linked lecture 文件标题仍显示 2025；
- 2026 `mmap` lab 页面本轮 web fetch 持续 timeout，因此只把“2026 schedule 分配 mmap lab”当 2026 已确认事实，task details 使用 MIT 官方 2025 mmap lab fallback；
- 2026 FS lab 页面本轮抓取超时，Lesson11 不声称逐字核对不可见页面；实验主线与课程稳定任务/相邻版本交叉核验，后续页面恢复后优先 refresh。

### Architecture Boundary

本轮发现并修正两类“教学系统把具体实现泛化”的问题：

1. X-B01 / OS-01/02：RISC-V/xv6 trap 不自动由硬件完成 kernel stack/page-table/full-trapframe 切换；
2. X-B03：DMA 不应跨平台绝对化为 `raw PA + interrupt + CPU 完全不参与 + caller 一定 blocking`。

对应 Canonical 已完成 Source Diff 与发布。

### 408 Transfer

15 页均保留：

- Mother Question；
- 真实对象/状态；
- xv6 code path 或自建模拟器；
- 408 Bridge；
- `A != B` 边界；
- Retrieval Check / Quick Attack；
- Stop Boundary 或实验取舍。

### Retrieval / Adversarial

已建立的交互训练：

- fork/exec/wait state trace；
- syscall entry/return trace；
- trap classifier；
- Sv39 VA splitter；
- Page Fault classifier + COW frame/refcount trace；
- FCFS/SJF/SRTF/Priority/RR scheduler simulator；
- lost-update lock trace；
- lost wakeup / correct lock-handoff trace；
- Banker safety/request simulator；
- I/O task/data/completion triple trace；
- inode LBN direct/single/double-indirect calculator；
- logging crash-point recovery simulator；
- mmap first-touch trace；
- blocking read hit/miss lifecycle switch；
- fork+COW+file-reference relation trace。

## 3. Canonical Update 结果

### 已更新

- OS-04：Page Fault `!=` 必然 I/O/Blocked；COW `RO != COW`；frame lifetime/refcount 边界。
- X-B01 + OS-01/02：RISC-V/xv6 暴露的 privilege/stack/page-table/context-switch 分层边界。
- X-B03：DMA-visible mapping、completion evidence、interrupt/poll、blocking/async 四轴分离。

### No Update

- OS-03：Locking / Banker 新 HTML 只增加教学投影和 simulator，Canonical 已拥有稳定机制；
- OS-B01：Sleep/Wakeup HTML 沿现有 predicate→wait→wake→Ready→recheck Bridge；
- OS-06/07：FS/Logging HTML 主要重新组织已有机制与实验路径；
- OS-B04：mmap HTML 沿已有 VM×File handoff；
- OS-I01 / OS-I02：Integration HTML 直接投影现有 Canonical lifecycle。

## 4. 下一阶段质量工作

课程第一版“结构完成”不等于“学习效果成熟”。下一阶段按优先级：

1. 用真实 408 真题/陌生题做 Lesson-by-Lesson Retrieval Test，验证能否从 HTML 模型无提示复原题目第一动作；
2. 2026 Fall 课程实际推进后 refresh inherited notes/lab pages，更新 Source Manifest；
3. 优先对 P0 页做浏览器视觉验收和手机窄屏检查；
4. 实际运行 MIT selected labs 后，把“代码能过测试”和“模型能复原”分开验收；
5. 只有新证据暴露 Canonical 事实/边界错误时才回写 Handbook，不因 HTML 增长继续扩知识树。
