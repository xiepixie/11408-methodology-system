# 概率专题手稿下册第 4–7 章：训练层 Source Diff

> 日期：2026-08-19  
> 类型：Evidence / Source Diff / Training Audit，不是知识 Owner  
> 输入：当前会话上传的 `概率专题手稿_下册_第4-7章_完整无遗漏清稿.md`  
> 审阅范围：`10_数学一/30_概率论/04--08` Canonical / Practice 与 `90_学科做题规则/概率统计.md`  
> 核销目标：不再重复证明“知识点是否出现”，而逐项检查源稿是否贡献了新的检索意图、母题、变式轴、第一动作、检查条件或永久错误阻断。

## 1. 审阅口径

上一轮旧九章核销已经确认 Topic01–08 没有独立理论语义处于无 Owner 状态。本轮换用训练层口径：

1. **理论已在 Canonical，不代表训练已完成**：若源稿的题型组织提供独立检索意图或稳定动作，必须进入对应 Practice；
2. **同构例题不逐题复制**：若多题只验证同一决策链，压成母题与变式轴；
3. **源稿 OCR / 课堂口诀不直接升级为真相**：数学式先与当前 Canonical 校准；明确错误只记录为 Source Anomaly；
4. **训练 Markdown 不重新成为理论 Owner**：定义、定理和完整证明继续由 `.tex` 拥有。

---

## 2. 文件开头承接第三章综合题

源稿开头保留一道二维均匀分布综合题：先由区域面积归一得到联合密度，再判断由 $(X,Y)$ 定义的示性变量 $U$ 与 $X$ 是否独立，最后求 $Z=U+X$ 的分布函数。

### 去向

- 区域均匀、联合支撑、边缘/条件/独立性：Topic04 `联合、边缘、条件与极值分布.md`；
- $U$ 是由 $(X,Y)$ 生成的新随机变量、$Z=U+X$ 的分层原像：Topic04 `随机变量变换.md` / `和的分布与卷积.md`；
- “联合支撑先于积分、函数依赖先于协方差独立性检验、条件分层再混合输出”均已存在。

**结论**：Merge Evidence；不为单题新建文件。

---

## 3. 第四章：数字特征

### 3.1 期望、方差、协方差、相关系数、矩

源稿覆盖：

- 离散/连续一维期望；
- 二维离散/连续 LOTUS；
- 期望线性；
- 方差定义与 $E(X^2)-[EX]^2$；
- 协方差、相关系数与 $|\rho|=1$；
- 独立与不相关边界、二维正态特例；
- 原点矩、中心矩、混合矩；
- 常见分布均值方差；
- 条件期望、全期望、全方差；
- 连续、离散、二维密度、观测次数、绝对偏差、$F(X)$、计数等训练题。

### 已有 Owner

- Canonical：Topic05 `数字特征与依赖摘要.tex`；
- Practice：`期望、方差与协方差的计算边界.md`。

已有 Practice 已承接：LOTUS、线性期望、完整方差展开、独立/不相关边界、相关系数退化、全期望/全方差、函数摘要绕过完整分布、绝对偏差、示性变量计数、尾积分、混合分布与多类别计数。

### 3.2 专题 13：伽马函数、半轴积分、正态绝对矩与配方

源稿把以下动作连续组织在一起：

- $\int_0^\infty x^a e^{-\lambda x}\,dx$；
- $\int_0^\infty x^a e^{-\lambda x^2}\,dx$；
- 半整数伽马值；
- $E|Z|$（标准正态）；
- 指数里带线性项时完成平方；
- $E(Ze^{2Z})$；
- 二维高斯型密度边缘化时按当前积分变量配方。

这些理论片段在 Canonical 中有去向，但此前没有独立的训练检索入口。

### 本轮新增

`05_数字特征与依赖摘要/伽马型积分与正态绝对矩.md`

训练责任：

1. 把线性指数核和二次指数核统一成两条伽马母积分；
2. 标准正态绝对矩先标准化整个线性组合；
3. 指数中出现线性项先完成平方；
4. 只问矩留在 Topic05，问完整边缘/条件分布转 Topic04；
5. 用母题而不是 OCR 口诀恢复尺度幂次与半整数伽马值。

### Source Anomaly

源稿伽马/高斯积分段存在明显 OCR 根号、系数和“阶乘”记号污染。稳定 Practice 不复制这些残缺式，统一回到 $\Gamma(s)$ 定义和当前 Canonical 的数学记号。

---

## 4. 第五章：大数定律与中心极限定理

源稿按专题 18–20 组织：

- Chebyshev 不等式及事件改写；
- 依概率收敛；
- Chebyshev / Khinchin / Bernoulli 大数律；
- 样本矩 $n^{-1}\sum g(X_i)$ 的稳定中心；
- 经典 i.i.d. CLT；
- De Moivre–Laplace；
- 二项的精确 / Poisson / 正态近似分流；
- 多个标准化选择题。

### 已有 Owner

`06_大数定律与中心极限定理/大数定律与中心极限定理分流.md` 已覆盖：

- LLN vs CLT 分工；
- Chebyshev 只给界；
- 条件包匹配；
- 收敛类型；
- $g(X_i)$ 先重命名；
- 二项正态近似；
- 精确 / 渐近 / 近似 / 上界身份分流。

### 本轮补强

新增局部规则：**CLT 不能把一般非正态总体的有限样本和直接写成精确正态**。只有总体本身正态时，样本和/均值在有限 $n$ 下精确正态；一般 CLT 应保留 $\xrightarrow d$ 或明确“近似”。

### Source Anomaly

源稿存在“无论 $X_i$ 服从什么分布，$n$ 大时 $\sum X_i\sim N(n\mu,n\sigma^2)$”一类课堂压缩语句。其训练意图保留为“标准化”，但严格性按 Canonical 修正。

Chebyshev 段中若由概率要求直接把上界公式取等号反解，也继续按上一轮结论阻断：只有题目明确要求按 Chebyshev 保证界设计时，才对保证界表达式取等号。

---

## 5. 第六章：总体、样本、统计量与三大抽样分布

源稿专题 21–22 覆盖：

- 总体、简单随机样本、样本观察值；
- 统计量、样本均值、样本方差；
- $\chi^2,t,F$ 的定义、性质、矩、可加/倒数关系；
- 分位点及上下尾换算；
- 正态总体下 $\bar X,S^2$ 的精确分布与独立性；
- 两总体抽样标尺。

### 已有 Owner

Canonical `总体样本与抽样分布.tex` 已完整拥有三大分布的生成、密度、矩存在边界、分位点与正态抽样分布。`抽样分布与标准化.md` 已训练尺度与自由度，但此前把“查分位点/平方/倒数”压得过短。

### 本轮新增

`07_总体样本与抽样分布/三大抽样分布与分位点.md`

单独训练：

- $\chi^2$：先校准为独立标准正态方向再平方求和；
- $t$：标准正态除以独立卡方尺度；
- $F$：两个独立标准化卡方比；
- $T^2\sim F(1,\nu)$；
- $1/F$ 必须交换自由度；
- 项目统一上尾 $\alpha$ 分位点约定；
- $t$ 用对称换尾，$F$ 用倒数 + 交换自由度，$\chi^2$ 禁止负号对称。

**结论**：源稿专题 21–22 的理论无 Owner 缺口，但训练层新增了一个真实检索入口。

---

## 6. 第七章：矩估计、最大似然与估计量评价

### 6.1 专题 23：矩估计

源稿覆盖一个参数、多个参数、第一矩失效时改用更高矩、估计量/估计值区分等。

已有 `参数估计与似然边界.md` 已明确：

- 先数自由参数；
- 归一化是模型约束，不是样本信息；
- 不机械使用前 $k$ 阶矩；
- 一阶矩不含参数时换二阶/其他矩；
- 解后回参数域。

**结论**：Existing Practice，源稿例题作为 Merge Evidence。

### 6.2 专题 24：最大似然估计

源稿覆盖离散/连续似然、对数似然、内部驻点、参数支撑依赖时 max/min 边界、两参数与两样本联合似然。

已有 `参数估计与似然边界.md` 已比源稿课堂流程更严格：

- 参数域与样本可行域先于求导；
- 支撑含参数时先看样本极值；
- 对数不会消除可行域；
- 驻点只生成候选，MLE 是全局优化问题。

**结论**：Existing Practice；不复制“求导等于零就结束”的弱规则。

### 6.3 专题 25：估计量的评选标准

源稿把无偏性、有效性、一致性单独组织成专题，并通过正态 $\mu^2$ 修偏、指数总体两个无偏估计量比较、样本矩相合等题训练。

此前 `参数估计与似然边界.md` 只有一个较短的评价段，不足以承担独立检索意图。

### 本轮新增

`08_参数估计与假设检验/估计量评价.md`

训练：

- 无偏：直接对齐 $E\hat\theta$ 与目标参数函数；
- 非线性变换不继承无偏性；
- 有效性先对齐同一参数与可比资格，再比较方差；
- MSE 显式处理偏差—方差权衡；
- 相合性优先重写成样本矩，调用 LLN / Chebyshev；
- 母题：$\bar X^2-S^2/n$ 对 $\mu^2$ 的修偏；指数总体 $\bar X$ 与 $nX_{(1)}$ 的效率比较；样本二阶矩相合性。

---

## 7. 置信区间

源稿覆盖：

- 置信水平与显著性水平；
- 枢轴量；
- 正态均值 $Z/t$ 区间；
- 方差在均值已知/未知下的 $\chi^2$ 区间；
- 样本量、区间长度；
- 由 $\mu$ 的区间变换得到 $e^\mu$ 或其他参数函数的区间。

### 已有 Owner

`置信区间.md` 已有：枢轴优先、均值双侧中心、方差自由度分流、尺度/单调性复核，并已明确置信覆盖率不是观测后参数的后验概率。

### 本轮补强

新增局部规则：**单调参数变换直接变换置信事件端点**。递增函数按 $(g(L),g(U))$，递减函数交换端点；非单调函数不能只变端点，必须回事件原像。

该动作吸收源稿“由 $\mu$ 区间得到 $e^\mu$ / 变换参数区间”的独立训练价值。

---

## 8. 假设检验

源稿专题 27 覆盖：

- $H_0/H_1$；
- 显著性水平与拒绝域；
- 第一类/第二类错误；
- 均值 $Z/t$、方差 $\chi^2$、两总体检验；
- 单侧/双侧；
- 改变 $\alpha$ 后拒绝结论的逻辑；
- 给定拒绝域计算 $\alpha,\beta$。

### 已有 Owner

`假设检验.md` 已训练：

- 备择方向先于尾部；
- 正态方差检验自由度；
- 第一、第二类错误在不同参数世界；
- 功效/样本量；
- $p$ 值；
- 检验结果复核。

### 本轮补强

新增两条局部规则：

1. **复合原假设的单侧检验在最不利边界校准**：例如正态均值单调模型中的 $H_0:\mu\le\mu_0$ / $H_1:\mu>\mu_0$；
2. **显著性水平变化先看拒绝域嵌套**：$\alpha$ 越小，固定检验族的拒绝域越小；小 $\alpha$ 下拒绝可推出大 $\alpha$ 下拒绝，大 $\alpha$ 下不拒绝可推出小 $\alpha$ 下不拒绝。

同时继续统一使用“拒绝 / 不拒绝 $H_0$”，不继承源稿“接受 $H_0$”的过强语言。

---

## 9. 训练层最终路由矩阵

| 源稿专题 / 题群 | 当前训练入口 | 本轮状态 |
|---|---|---|
| 开头二维均匀 + 示性变量综合题 | Topic04 联合/变换/和分布 | 已有，Merge Evidence |
| 数字特征基础与刷题 | Topic05 `期望、方差与协方差的计算边界.md` | 已有 |
| 伽马函数 / 高斯积分 / 正态绝对矩 / 配方 | Topic05 `伽马型积分与正态绝对矩.md` | **新建** |
| Chebyshev / LLN / CLT | Topic06 `大数定律与中心极限定理分流.md` | 已有 + **严格性补强** |
| 统计量与标准化 | Topic07 `抽样分布与标准化.md` | 已有 |
| $\chi^2/t/F$ / 分位点 | Topic07 `三大抽样分布与分位点.md` | **新建** |
| 顺序统计量 | Topic07 `顺序统计量与估计接口.md` | 已有 |
| 矩估计 / MLE | Topic08 `参数估计与似然边界.md` | 已有 |
| 无偏 / 有效 / MSE / 相合 | Topic08 `估计量评价.md` | **新建** |
| 置信区间 | Topic08 `置信区间.md` | 已有 + **参数变换补强** |
| 假设检验 | Topic08 `假设检验.md` | 已有 + **复合边界 / α 嵌套补强** |
| 多子问推断题 | Topic08 `推断任务分流.md` | 已更新导航 |

---

## 10. 本轮结论

按源稿第 4–7 章（含开头承接第三章综合题）的**独立训练语义**逐项核销后：

1. 未发现仍处于“无理论 Owner”状态的独立知识块；
2. 发现 3 个此前训练层解释压力明显、值得独立检索的入口，现已新建：
   - `伽马型积分与正态绝对矩.md`；
   - `三大抽样分布与分位点.md`；
   - `估计量评价.md`；
3. 发现 4 处无需新建文件、但应补成局部规则的训练缺口：CLT 精确/近似身份、单调参数区间变换、复合原假设边界校准、显著性水平拒绝域嵌套；现已补入对应 Practice；
4. 源稿的 OCR/课堂快捷话术只作为 Source Evidence 使用，明确错误或过强表达没有回灌稳定资产；
5. 本轮没有修改 Canonical `.tex`，因为新增信息都属于训练路由/母题/边界，而不是新的理论机制。

下一轮若继续深化，应优先用陌生题攻击这 3 个新训练入口与 4 条新局部规则；只有暴露理论缺口时才回修 Canonical。
