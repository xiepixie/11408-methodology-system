# AGENTS

> **系统目标**：把题目、学生作答与长期画像组织成一套**教师真正能在备课、上课、课后追踪三个时刻高效使用**的教学交付系统。
>
> **核心原则**：公共基础设施与题库（`common/`）负责“可复用的数学与教学知识”，学生个人目录负责“这个学生此刻为什么要学、课堂上发生了什么、下一次要验证什么”。两者严禁混写。

---

## 〇、底层元准则：心智模型演进与渐进式修改铁律（最高优先级）

本准则作为所有笔记整理、心智模型构建、技术推演与文档重构的**底层通用准则**，优先于所有局部执行逻辑。

### 1. 思想无损与密度增量法则（严禁越改越少）
- **核心定位**：对现有笔记、知识库和心智模型的修改，本质是在已有认知基石上的**演进、深化与提升**，绝非“推倒重来”或“简化摘要”。
- **严禁信息与思想降维**：
  - 修改过程中，**篇幅与思想颗粒度绝对不得有遗漏**；
  - 严禁借“精炼/整理”之名删除原有的关键推导、背景脉络、细分 Case、边界条件或核心直观解释；
  - 原有版本中的有效认知与推导逻辑必须 **100% 完整继承**。
- **增量演进与升华**：修改后的内容在逻辑严密性、心智模型完备度、表述清晰度上必须实现**正向增量（Additive & Enriching）**，内容密度与认知价值只能提升，绝不能缩水。

### 2. 严格渐进式演化法则（严禁一步到位）
- **全流程渐进演进**：所有改进、修改、执行必须遵循“渐进式（Progressive / Incremental）”节奏，禁止任何一次性大拆大建或盲目颠覆式重构。
- **动笔前“三步推演”规范（Pre-modification Impact Analysis）**：
  在执行任何具体修改之前，必须在心智与计划中完成三项推演：
  1. **精准定位（Where）**：明确具体要修改哪些模块、段落、概念节点或文件，列出改动范围清单；
  2. **增量目标（What & Why）**：明确本次修改是在哪个具体维度（深度、精度、结构、关联）进行提升，新旧差异是什么；
  3. **后果评估（Consequence）**：预先推演该改动可能带来的连锁反应（是否影响已有推导链、是否破坏下游引用、是否造成心智断层），并确立防御方案。
- **小步验证，阶段推进**：
  - 复杂任务拆解为小步快跑的子阶段（Step-by-step）；
  - 每完成一步改动，立即核对与旧版的一致性与自洽性，确认无信息丢失后再推进下一步。

---

## 一、系统边界与三个使用时刻

本系统服务于 120 分钟一对一数学辅导。目标不是机械整理所有题目，而是：
1. 找到学生当前最值得解决的 1–3 个问题；
2. 用最少但足够的题目验证、纠偏和迁移；
3. 让教师在课堂中能快速做“继续 / 降阶 / 跳过 / 追加变式”的决策；
4. 课后只把有证据支持的稳定问题写入长期画像。

### 三个使用时刻

#### 1. 课前备课：5–15 分钟快速扫描
教师首先需要看清：
- 学生是谁、本次课专题与已知证据；
- 本节只解决哪 1–3 个可观察目标；
- 每题处理方式、时间预算与取舍依据；
- 学生答到什么程度算“达标并停止基础讲解”；
- 如果学生达标，下一步跳到哪里；如果未达标，使用哪一级脚手架。

#### 2. 课堂进行：以“课堂教学导引表”为主视图
教师不应在长篇解析中寻找课堂动作。教案第一页与题目页必须优先呈现：
- 当前题处理方式与时间预算；
- 核心检查点 (Checkpoint)；
- 关键追问与提示梯度；
- 学生卡住时的分支处置策略；
- 可板书的标准得分链与关键推理句。

#### 3. 课后追踪：3–8 分钟完成证据闭环
课后记录与更新：
- 学生实际表现与目标达成情况；
- 哪些问题只是一次性失误（仅留存当次记录）；
- 哪些问题已经有足够证据晋升为长期画像（`profile.md`）；
- 安排精准的课后重做与复测任务。

---

## 二、数据分层：公共知识与学生证据严格分离

### 1. `common/`：只放可复用内容

```text
common/
  latex/                # 全局 LaTeX Design System Owner、未来 Core/Family classes 与 regression specimens
  ipara.sty             # 现有 Lesson/Teaching 实现与兼容入口；不再作为全局架构 Owner
  templates/            # 4页精排学案、教案、questions.tex 起始模板
  pool/                 # 公共题库，按 [学科专题]/[分层难度]/q_[description].tex 组织
  topics/               # 公共专题课，存放可复用讲义与配套题
  scripts/              # compile_tex.py 等自动化编译清理工具
```

#### `common/pool/` 规范
公共题目只存与任何具体学生无关的内容：
- `Stem`、`Options`、`Figure`、`Answer`、`Solution`、`AltSolution`、`CommonErrors`、`TeachingNotes`。
- **严禁**在公共题目中写具体学生姓名、个体诊断或某次课的实际课堂表现。
- 文件头部标注难度（★ 基础 / ★★ 中档 / ★★★ 难题）与知识点标签。

### 2. 学生目录：只放个性化证据与交付产物

```text
学生姓名/
  profile.md                    # 长期画像（只存稳定事实）
  bank/                         # 可选：该学生专属错题/试卷题
  sessions/
    01_专题名称/
      plan.md                   # 课前计划与决策表
      diagnosis.md              # 课前已有诊断与证据
      outcome.md                # 课后实际结果记录
      questions.tex             # 本课引用题目清单
      学案.tex
      教案.tex
      学案.pdf
      教案.pdf
```

### 3. 三类课次文档职责划分

- `diagnosis.md`：**课前已知**。来自历史作业、试卷、上次课或用户反馈。
- `plan.md`：**课前计划**。决定本节讲什么、怎么判断学会、分支处置逻辑。
- `outcome.md`：**课后事实**。记录课堂真实表现与达标证据，绝不允许提前预填为既定事实。

---

## 三、学生画像 `profile.md`：证据链原则

`profile.md` 是学生的长期档案，绝不能成为所有一次性错题标签的垃圾桶。

### 推荐结构

```markdown
# 学生画像

## 当前阶段
- 年级/目标：

## 稳定优势
- ...

## 已确认薄弱点
| 能力/知识点 | 状态 | 证据 | 最近验证 | 下次检查 |
|---|---|---|---|---|
| 线面角射影构造 | confirmed | S01-Q4, S03-Q2 | 2026-08-03 | 新图形能否独立作出射影 |

## 候选问题 (Candidate)
- 仅有一次证据、尚未确认的问题。

## 已解决/降级观察
- 曾经薄弱，但最近连续通过验证的问题。

## 教学偏好与有效讲法
- 只记录长期稳定且真正影响讲课方式的信息。
```

### 画像晋升与降级规则

- **一次失误**：仅写入当次 `outcome.md` 或 `profile.md` 的【候选问题】，不入稳定薄弱点；
- **两次 independent 场景重复出现**：方可晋升为 `confirmed`（已确认薄弱点）；
- **连续 2 次独立复测/新课验证通过**：从薄弱点降级到【已解决/降级观察】。
- 严禁使用“粗心”“可能没看清”等主观猜测直接建立长期标签。

---

## 四、题目输入、PDF 处理与 `uv` 环境规范

### 1. 输入材料处理原则
- **干净题面/去手写版**：是题面与图形转写的唯一依据。
- **学生作答扫描件**：只用于分析解题过程与卡点，不覆盖干净题面。
- **转写为 LaTeX**：所有题目最终沉淀为 LaTeX 题目文件与 TikZ 图形。

### 2. 成套 PDF 输入流程
当用户提供“去手写试卷 + 带学生书写扫描件”时：
1. 按页码和题号建立配对清单：
   ```text
   | QID | 干净题面页码 | 学生作答页码 | 题型 | 知识点 | 作答状态 | 题库文件 | 诊断状态 |
   ```
2. 拆分任务：干净题面给 `question_ingester` 转写，作答件给 `diagnosis_solver` 诊断。
3. 保证同一题只生成一个最终文件名与一组驼峰宏名。

### 3. PDF 与 Python 环境管理规范 (使用 `uv`)
本工作区统一使用 **`uv`** 作为 Python 环境与包管理工具：
1. **虚拟环境隔离**：
   - 严禁直接全局 `pip3 install` 强制写入系统环境。
   - 需要分析 PDF 时，在当前工作目录下执行：
```bash
     uv venv
     uv pip install pypdf
```
   - 或使用隔离运行：
```bash
     uv run --with pypdf python3 -c "import pypdf; ..."
```
2. **扫描型 PDF 识别**：若 `pypdf` 提取结果为 `[No text extracted]`，表明该 PDF 为纯扫描图层，必须依赖视觉转写。
3. **双重数学逻辑与数值验证**：转写后必须使用 Python 或特值代入法检查坐标系、几何模型、向量代数关系、正负号与角范围，确保零 OCR/转写错误。

### 4. 自动化编译与辅助文件清理规范 (`compile_tex.py`)
- **强制使用编译脚本**：所有的 LaTeX 编译统一使用：
```bash
  python3 common/scripts/compile_tex.py <目标.tex>
```
- **多遍编译与 Log 解析**：脚本会自动执行多遍编译（Double/Triple Pass），精确解析 `xelatex` Log 中的 Warning/Error/Undefined References，确保 `\zpageref{LastPage}` 完美解析为确切页数（如 `4 / 4`），消除 `??` 问号。
- **自动清理**：编译成功后自动清理 `.aux` / `.log` / `.synctex` 等冗余辅助文件。也可使用 `python3 common/scripts/compile_tex.py --clean-all` 一键清理全项目临时文件。

---

## 五、120 分钟课次的决策模型

### 1. 每节课只设 1–3 个可观察目标
目标必须具体可观察，例如：
- “能独立完成线面垂直 4 条件证明链（不漏写相交与在面内条件）”；
- “面对新图形，能准确找出线面角的高与射影，并口述‘高度看正弦，影子看余弦’”；
- “解三角形边角互化能在‘化边’与‘化角’中做出合理决策”。

### 2. 每题处理方式与时间预算
- `必讲`：暴露核心漏洞，本次课必须解决；
- `精讲`：方法迁移价值高，适合展开标准解答与模型总结；
- `快讲`：单点纠偏，通常 3–8 分钟；
- `自练`：学生当堂独立书写，教师观察卡点；
- `跳过`：已经掌握、重复或教学价值低；
- `课后复练`：适合间隔复测，不占课堂主时段。

### 3. 每题显式设立“停止条件”
避免教师机械讲完所有预置步骤。例如：
- “学生能独立找出垂足与射影 → 停止基础定义讲解，直接检查计算”；
- “学生 60 秒内选对方法并说明理由 → 跳过完整板书，只检查易错收尾”。

---

## 六、`plan.md` 格式规范

每次课生成正式学案与教案前，必须先产出 `plan.md` 供用户审查调整：

```markdown
# 课次计划

学生：
专题：
总时长：120 min

## 本次课成功标准（1–3 项）
1. ...
2. ...

## 已知证据
- 来自哪次作业/试卷/诊断：...

## 120 分钟课堂路线表
| 顺序 | 题目 | 处理 | 时间 | 讲解目的 | 检查点 (Checkpoint) | 达标后分支 | 未达标分支 |
|---|---|---|---:|---|---|---|---|
| 1 | q_xxx | 快讲 | 10min | 唤醒公式 | 能否口述正余弦定理 | 进入大题 A | 补充公式默写 |
| 2 | q_xxx | 精讲 | 35min | 建立线面角模型 | 能否独立找准垂足与射影 | 进入迁移 B | 回归垂直判定 |

## 取舍策略
- **【本节绝不能跳过】**：...
- **【时间不足优先删】**：...
- **【课后出口检测项】**：...
```

---

## 七、学案与教案排版规范

> **全局 LaTeX 架构边界**：Family/Profile/Variant、KOMA/CTeX/IPARA 分工、Semantic API、依赖与兼容迁移由 `common/latex/README.md` 唯一拥有。本节只拥有一对一 Lesson/Teaching 的业务排版要求，不得重新定义全局 Design System。

教案与学案采用 **4 页精排（一界一页）** 标准结构：

### 1. 教案 (面向教师) 4 页标准布局

- **第 1 页：【第一部分：课堂教学导引】**
  - 页首元信息框 + 3项可观察成功标准 + 课前证据；
  - 120 分钟课堂路线表 + 动态 Checkpoint 与分支处置；
  - 时间不足删减顺序与出口检测项。
- **第 2 页：【第二部分：备课知识库与几何直观模型】**
  - 2–3 个经典 TikZ 几何模型图示 + 定理得分推导链。
- **第 3 页：【第三部分：核心题目精讲与诊断】**
  - **单栏呈现**：题干与 TikZ 几何原图；
  - **题下双栏 (`paracol` 比例 `0.68 / 0.32`)**：
    - Left Column (68%)：标准解答（可板书得分版）+ 得分公式与关键推理句；
    - Right Column (32%)：课前证据 + Checkpoint 检查点 + 追问梯度 + 学案思考对应。
- **第 4 页：【第四部分：课后总结与复练落实】**
  - **【一、课堂评价与错因记录】**：含 $5.5\text{cm}$ 高对比度独立边框手写框 (`tcolorbox`)，供教师现场书写错因记录，**绝不坍塌为 0 高度**；
  - **【二、课后复练与画像跟进】**：固定吸附在最底部，明确重做题、默写项与 `profile.md` 跟踪建议。

### 2. 学案 (面向学生) 规范
- 保持干净题面与大面积演算空间；
- 选择/填空题留足演算区，解答题独占半页或一页；
- 包含“课前公式复习”与“课后公式默写（隐藏答案）”；
- 题目区不写教师提示与策略；自查反思问题放在题目下方。

### 3. 黑白打印与文本排版原则
- 纯净高对比度黑白排版，不使用极浅背景色，防止复印成脏灰；
- 统一使用适度淡灰色（`rulecolor`）作为边框与分割线；
- 避免墙式大段文字，每 1–2 句主动换行；
- 复杂数学公式一律使用展示环境（`aligned`、`cases`），长公式在 ` = `/`+`/`-` 处主动断行。

---

## 八、题目文件与 LaTeX 宏命名规范

### 1. 文件命名与存放
公共题目统一保存在：
```text
common/pool/[学科专题]/[分层难度]/q_[description].tex
```
示例：`common/pool/立体几何/难题/q_solid_cuboid_section.tex`
禁止在文件名中使用学生名字缩写或日期。

### 2. 驼峰宏命名与向下兼容
题目文件内部宏一律使用**纯英文驼峰命名（严禁包含数字字符）**：
```latex
\qSolidCubeAngleStem
\qSolidCubeAngleFigure
\qSolidCubeAngleAnswer
\qSolidCubeAngleSolution
```
为了向下兼容已有的学生课次教案/学案，可在文件末尾添加 `\newcommand` 别名定义：
```latex
\newcommand{\qLYBZeroOneThreeStem}{\qSolidCubeAngleStem}
```

---

## 九、TikZ 图像与流程图绘制规范

### 1. 立体几何与数学图形规范
1. **前实后虚**：背面顶点与被遮挡的线段一律使用虚线 (`dashed`)，避免透视矛盾。
2. **标签避让**：顶点字母不得被线段穿过，标签位置根据图形实际避让。
3. **印刷安全**：少画微小直角符号与小角弧线，复印易糊；关键几何条件写在题干或解析中。
4. **教/学分层**：学生端只画题目已知结构；教师端可通过 `\ifteacher` 增加灰色虚线辅助线与投影点。
5. **图内文字克制**：图内只放顶点名与极短角标（如 $A, B, O, \theta$），解释性长句写在正文中。

### 2. 计算机系统、软件架构与逻辑拓扑流程图通用规范（五大黄金原则）

本规范适用于所有计算机科学（OS、计组、网络、数据结构、算法）、软件架构、逻辑拓扑与状态机流程图绘制。

#### (1) 连线路由优先级 (Routing Priority Order)
严格遵循：
\[
\boxed{
\text{Direct} \arr \text{One-bend} \arr \text{Orthogonal multi-bend} \arr \text{Outer routing}
}
\]
- 能直连（如 `s6.north -- s1.south`）绝不绕右侧；
- 能一折完成，绝不用三折；
- 外围绕线只能作为防重叠的最后手段（若出现大量外围折线，说明节点矩阵分组本身设计不合理）。

#### (2) 网格先于节点 (Grid-First Top-Down Design)
- 绘制前先定义矩阵网格（如 $2\times 3$ 或 $3\times 3$ 等 $M\times N$ 矩阵），划分 Column 与 Row 再填充逻辑；
- 避免“临时想到节点即随意摆放”的无序拓扑。

#### (3) 包络盒预算 (Bounding Box Budget)
- 放弃硬编码页面绝对宽度，以当前 `\linewidth` 作为唯一真值；
- 核心机制图包络宽度需满足：
\[
W_{\text{figure}} \le \min(13.6\text{cm},\ 0.90\linewidth)
\]
常规核心图黄金宽度控制在 $12.6 \sim 13.6\text{cm}$，全景图上限不超过 $0.95\linewidth$。

#### (4) 节点固定尺寸与文案分层 (Standard Dimensions \& Text Layering)
- 普通流程节点标准尺寸统一为 $2.6\text{cm} \times 0.95\text{cm}$；
- 节点内部文本严格遵守“两行极简分层”：
  - 第一行：对象 / 动作（例如 `Open File`、`Submit Request`）
  - 第二行：状态 / 意义（例如 `offset + flags`、`Blocked`）
- 任何长篇解释性描述一律移出节点。

#### (5) 线型与形状统一语义系统 (Semantic Line \& Shape System)
\begin{center}
\small
\begin{tabular}{ll}
\toprule
视觉图形 / 线型 & 统一固定系统含义 \\
\midrule
$\rightarrow$ 实线 & 动态发生的控制流 / 数据流 / 状态转换 \\
$\rightarrow$ 粗实线 & 系统 Main Path 主路径 / 正常运行流 \\
$\dashrightarrow$ 虚线 & 静态引用 / 指针 / 逻辑映射关系（例如 \code{fd} $\dashrightarrow$ \code{struct file}） \\
$\cdots\rightarrow$ 点线 & Cache 缓存 / 可选 Fast Path 路径 / 推测执行 \\
双线框 (\code{double}) & 持久化对象 (Persistent Object) / 存储介质 / 数据库 \\
灰框 (\code{softgray}) & 教材/理论高层抽象层 \\
小角标 \code{[E]} & 现代工程扩展 (Engineering Extension) / 平台特定硬件扩展 \\
\bottomrule
\end{tabular}
\end{center}

---

## 十、可观察行为诊断语言规范

诊断必须描述“可观察解题行为”，严禁主观标签化。

- **推荐表达（可观察行为）**：
  - “证明线面垂直时漏写 $AB \cap PA = A$ 相交条件”；
  - “能求出 $\cos\theta = \frac{1}{2}$，但特殊角误写为 $\frac{\pi}{6}$”；
  - “求线面角时未先寻找点在平面上的垂足与射影”。
- **严禁表达（主观标签）**：
  - ❌ “学生粗心” / ❌ “空间想象力差” / ❌ “基础太差” / ❌ “态度不认真”。

每个诊断尽量包含：1. 发生在哪一步；2. 属于概念、公式、推理还是计算收尾；3. 下一次如何验证。

---

## 十一、子 Agent 职责分工

1. **`question_ingester`**：负责干净题面转写、TikZ 绘图、公共题元数据标注与数学逻辑/数值自洽性检查。
2. **`diagnosis_solver`**：负责标准解答、得分句提炼、通用易错点与课前诊断证据分析。
3. **`session_planner`**：负责 120 分钟课次 1–3 个成功标准设定、题目取舍、时间预算、Checkpoint 与分支处置路由，生成 `plan.md`。
4. **`lesson_builder`**：负责根据 `plan.md` 生成 4 页精排 `学案.tex` 与 `教案.tex`，并调用 `compile_tex.py` 编译 PDF。
5. **`session_reviewer`**：课后根据实际课堂记录生成 `outcome.md`，执行 `profile.md` 画像证据链更新判定。

---

## 十二、质量检查清单

在交付学案与教案前，必须逐项核对：

- [ ] **内容正确性**：题干与图形已与原始材料校对，数学逻辑与数值经过 Python 代入验证；
- [ ] **课堂可用性**：第一页为【课堂教学导引表】，120 分钟时间预算、Checkpoint 与停止条件清晰；
- [ ] **排版规范**：教案恰好 4 页，第 4 页包含 $5.5\text{cm}$ 独立边框手写框，零硬编码塌陷；
- [ ] **编译无误**：使用 `compile_tex.py` 编译，页脚为 `X / 4` 格式，零 `??` 问号与未决引用；
- [ ] **真实性原则**：`diagnosis.md`（课前已知）与 `outcome.md`（课后事实）严格隔离，`profile.md` 更新满足证据链规则；
- [ ] **复用与隔离**：公共题库不含学生个人信息，驼峰宏保持旧别名兼容。
